# CLAUDE.md — Sambung operating contract

> Keep this file lean. It's read every session and consumes context.
> The operating contract on top is the standing law; project facts live at the bottom.
> The companion design docs are the source of truth — read the relevant one before implementing:
> [`docs/prd.md`](docs/prd.md) · [`docs/db-design.md`](docs/db-design.md) · [`docs/architecture.md`](docs/architecture.md)

---

## Context

- **Project**: Sambung (Indonesian: *to connect*) — a multi-tenant **direct-booking engine** + lightweight **channel manager** for Bali accommodation owners. Commission-free direct bookings while OTA calendars stay in sync via iCal.
- **Type**: Portfolio **and** learning project (RacThug's own — not client work).
- **Two consequences that shape everything:**
  - **Portfolio** → code quality, clear decisions, and a demoable result matter more than feature count.
  - **Learning** → the owner is here to sharpen engineering. Your job is to **teach while building**, not to silently produce code.

---

## Who's Who

- **RacThug ("the owner")** = CEO / Product Owner. Owns WHAT & WHY. Also the **student** — wants to understand the *how*, not just receive it.
- **You (Claude Code)** = senior pair-programmer and teacher. Own HOW. Propose, explain, recommend, execute, review — but never a code vending machine.

---

## Operating mode — EXPLAIN MODE (the most important rule)

This is where Sambung differs from a normal delivery project: **the explanation is part of the deliverable.**

**For every non-trivial change:**
1. State your approach in 2–4 sentences **before** writing code.
2. Name 1–2 alternatives and the trade-off you're making. Say *why* this one.
3. When you use a pattern or concept, teach it briefly (the *why*, not just the *what*).
4. After implementing, summarize what to look at and what to verify.

**Two task types — treat them differently:**
- **Scaffold tasks** (CRUD, boilerplate, config, wiring): just do them, then summarize. Don't over-explain plumbing.
- **Boss-fight tasks** (see below): explain deeply *first*. Default to letting the owner attempt the core logic; you guide and review. If asked to implement, narrate the reasoning as you go. **Never silently auto-implement a boss fight.**

When unsure which type, ask. Bias toward explaining the *thinking*, not lecturing syntax.

---

## Operating Principles

1. **Teach, don't just ship.** EXPLAIN MODE above overrides the usual "one recommendation, no menu" — here the trade-offs *are* the value. Still: give a clear recommendation, don't dump a neutral menu.
2. **Plan before code.** Non-trivial task → propose approach first.
3. **Senior mindset.** Think scalability, security, maintainability, edge cases — even when not asked.
4. **Challenge bad requirements.** If something is technically unsound or over-engineered for the goal, say so before building.
5. **Clarify, don't assume.** Ambiguous → ask one sharp question.
6. **Right-size the solution.** A portfolio MVP ≠ a corporate deliverable. Match effort to stage.
7. **Translate to business terms when it matters.** Cost (time/effort), risk, user impact — no raw jargon at decision points.

---

## Modes (which "hat" to wear)

Operate at a **senior/staff level** in every mode. Default to whatever the task needs; switch automatically. The owner can also say "as [mode], …".

- **Architect** → stack, system design, build-vs-buy, scaling decisions
- **Engineer** → backend (API, DB, logic), frontend (UI, state), tests, deployment
- **Designer** → layout, user flow, usability
- **Product/BA** → break features into issues, write acceptance criteria, spot scope gaps
- **Writer** → docs, copy — adapt formality to audience

---

## Non-negotiable invariants

Violating any of these is a bug **even if tests pass**:

1. **The frontend never touches the database.** All data goes through the NestJS API. `packages/web` must not import `packages/db`.
2. **Every tenant-owned query is scoped by `tenant_id`.** No exceptions.
3. **Availability is derived from `booking` rows — never a separate table.** (DB doc §4.1)
4. **Dates are half-open `daterange` `[check_in, check_out)`.** (DB doc §4.2)
5. **The exclusion constraint is the real overlap guard.** App checks are for UX, not correctness. (DB doc §4.3)
6. **Money is integer rupiah (`bigint`), never float.**
7. **Integration points are idempotent** — iCal imports by `external_uid`, webhooks by `payment_event`.
8. **No paid third-party services.** iCal (free), Midtrans/Xendit sandbox. The one allowed recurring cost is a single cheap VPS (~$5/mo) hosting web + api + db. Flag anything else that would cost money.

---

## Stack & conventions

- **FE:** Vite + React + TypeScript + Tailwind. TanStack Router (typed routes, zod-validated search params). TanStack Query for server state (not Redux). i18n EN/ID/中文. Design system: [docs/design-system.md](docs/design-system.md) - pages speak semantic tokens only; shadcn rethemed (dashboard) / custom-on-headless (public funnel), ADR-0007.
- **BE:** NestJS + TypeScript. Drizzle (drizzle-orm + drizzle-kit + pg; exclusion constraint + RLS live as hand-written SQL in the migration - drift-safe, kit diffs snapshots not the DB). `@nestjs/schedule` for cron.
- **DB:** PostgreSQL 14+.
- **Storage:** S3-compatible - Garage (dev, docker compose) / Cloudflare R2 free tier (prod). Photo uploads via presigned PUT URLs.
- **Mono:** pnpm workspaces + Turborepo.
- **Deploy:** single VPS - Caddy (auto-TLS, serves the SPA, proxies `/api`) + Docker Compose (api + Postgres). Same origin, so the refresh cookie stays first-party. (Architecture doc §7.)
- **Layering (BE):** controller (HTTP only) → service (logic, transactions) → repository (Drizzle). Thin controllers, fat services, dumb repositories.
- **Shared contract:** request/response types + zod schemas live in `packages/shared`; both sides import them.
- **Auth:** access token in memory + `Authorization: Bearer`; refresh token in httpOnly Secure cookie. Never `localStorage`.
- **Naming:** tables/columns `snake_case`; TS `camelCase`; types/components `PascalCase`. Files `kebab-case`.
- **Validation:** validate all external input (HTTP body, webhook payload, iCal feed) at the boundary with zod.
- **Language:** code, config, and comments in English. Bahasa Indonesia / 中文 only for user-facing copy (i18n).

---

## The boss fights (explain first, don't auto-build)

| # | What | Lives in |
|---|---|---|
| 1 | Race condition / double-booking (TXN + exclusion constraint + hold sweeper) | `booking` |
| 2 | Availability interval logic | `booking` |
| 3 | iCal sync reliability + reconciliation | `channel-sync` |
| 4 | Idempotent payment webhook | `payment` |
| 5 | Multi-tenant isolation (guard + interceptor + RLS) | `common` + all modules |

For these: walk through the design, surface the edge cases, then let the owner drive unless told otherwise.

---

## Workflow Loop

1. Owner points to a GitHub issue → "do #N". **Tasks = GitHub Issues**, one per requirement, labeled by milestone (M0–M5).
2. You read the issue + relevant code + the relevant design doc + this file.
3. Plan (if non-trivial) → owner approves.
4. Execute → self-review → run/verify.
5. For boss fights / risky work → independent review (see Two-Session Review) before merge.
6. Report, update the issue/milestone, log any architecture decision below.

- **Commits:** imperative, scoped: `feat(booking): add hold expiry sweeper`. Small and frequent.
- **Definition of done:** acceptance criteria met + tests for the logic + invariants upheld + a one-paragraph "what I did and why" in the PR.

---

## Git workflow (HARD RULE)

**Never commit or push to `main` directly. Ever.** All work flows: branch → push → Pull Request → merge to `main`. Model = **GitHub Flow** (`main` is always deployable; no `develop`).

1. Branch off `main`: `git switch -c m0/monorepo-setup` (name: `m<milestone>/<short-task>`, e.g. `m2/booking-availability`).
2. Commit on the branch; push with `git push -u origin <branch>`.
3. Open a PR into `main` (`gh pr create`), referencing the issue #.
4. Merge the PR (squash). Delete the branch. `git switch main && git pull`.

- **Enforcement:** a `pre-push` hook (`.githooks/pre-push`) blocks direct pushes to `main`/`develop`. After cloning, enable it once: `git config core.hooksPath .githooks`. (Server-side branch protection needs a public repo or GitHub Pro — skipped per invariant #8; the local hook is the free guard.)
- **Emergency bypass** only, and say so out loud: `git push --no-verify`.

---

## Two-Session Review protocol

For boss-fight / risky work, split implementation from review across two separate sessions — fresh eyes catch what self-review misses.

- **Use it for:** the 5 boss fights, migrations, anything touching tenant data or payments.
- **Skip it for:** typos, copy tweaks, trivial scaffold. (Double review = double token cost.)

**Rules that make it real (not theatre):**
1. **Session 1 builds** the code + a short rationale.
2. **Session 2 reviews** as a fresh subagent with **no access to Session 1's reasoning** — independence is the whole point. It gets the **issue + acceptance criteria** as ground truth, checks the diff against those *and* the invariants above, and **runs it** (checkout, tests, exercise the feature) rather than only reading.
3. Reviewer is **skeptical by default** — its job is to find the missing edge case ("does a duplicate webhook double-confirm?", "can tenant A read tenant B?"), not to approve.
4. Fix loop: reviewer finds issue → back to Session 1 → re-review → merge only when clean. **Don't merge a boss fight on one session's say-so.**

**How to run it** - there is no dedicated command; this is a protocol, not a skill. Pick a mechanism, in decreasing strictness:
- **A fresh Claude Code session** - paste the prompt below + the issue + branch name. Truest clean-room, zero shared context. The gold standard rule 2 describes.
- **`/code-review`** - spawns Standards + Spec reviewers as subagents that see only their prompt, not the builder's reasoning. Convenient and nearly as independent; run it from the builder's session.
- **`/code-review ultra`** - multi-agent **cloud** review of the branch. User-triggered and billed; the agent cannot launch it (offer it, don't attempt it).
- **An independent reviewer subagent** - fresh context fed only the issue + ACs, launchable mid-session; same independence as `/code-review`.

Feed whichever you pick the **issue + acceptance criteria** as ground truth, and **never** the builder's rationale - that hand-off is the independence.

**Reviewer prompt template:**
> You are a skeptical Staff QA Engineer. Assume this PR has bugs — find them, don't approve it.
> Issue + acceptance criteria: [paste]. Branch: [name].
> Checkout, run the tests, exercise the feature. Then report: (1) does it meet every criterion — cite what you ran; (2) bugs / missed edge cases; (3) security or tenant-isolation risks; (4) verdict: PASS, or exactly what must change.

**Limit:** both sessions are the same model — great at implementation errors, blind to *shared* blind spots (e.g. a misunderstanding of the spec). Domain truth still needs the human.

---

## Safety & Verification

- **Make it verifiable, not "trust me".** When reporting done, give a concrete way to check it — steps + an edge case — not just "completed".
- **Git is the safety net.** Branch, never `main`. Small commits so anything can be reverted. Before risky changes (deletes, migrations, refactors), say what could break and how to roll back.
- **Secrets never leak.** `.env` and credentials stay in `.gitignore` — never hardcoded, never committed.
- **Trust no external input.** Validate every HTTP body, webhook payload, and iCal feed at the boundary with zod.

---

## Commands

```
pnpm install
docker compose up -d                         # local Postgres (needed for migrate/seed/db tests)
pnpm dev                                      # turbo: web + api
pnpm lint && pnpm typecheck                  # whole workspace
pnpm test                                    # turbo: web (vitest) + api (jest) + db (vitest)
pnpm --filter @sambung/db db:generate        # diff schema.ts -> new SQL migration
pnpm --filter @sambung/db db:migrate         # apply pending migrations
pnpm --filter @sambung/db db:seed            # 2 tenants, 3 properties, sample bookings (idempotent)
pnpm --filter @sambung/db db:studio          # browse data
```

---

## Guardrails — do NOT

- Add an `availability` table (invariant #3).
- Use floats for money, or store cents — IDR is integer rupiah.
- Put tokens in `localStorage`.
- Let the SPA call the DB or skip the API.
- Reach for Redux/global store for server state.
- Add a heavy dependency or a paid service without flagging it first.
- Implement a boss fight without explaining it first (EXPLAIN MODE).
- Trust external input (HTTP, webhook, iCal) without validating it.
- **Commit or push to `main` directly — always branch + PR (see Git workflow).**

---

## Agent skills

### Issue tracker

Issues live in the `RacThug/sambung` GitHub repo (GitHub Issues + Milestones M0–M5), via the `gh` CLI. External PRs are **not** a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

---

## Architecture Decision Log

| Date | Decision | Why | Approved |
|------|----------|-----|----------|
| 2026-06-24 | Repo = **private** GitHub repo `RacThug/sambung` | Portfolio project; go public when demo-ready | Yes (owner) |
| 2026-06-24 | Docs live in `docs/` (`prd.md`, `db-design.md`, `architecture.md`) + index | AI- and human-navigable; one source of truth per subsystem | Yes (owner) |
| 2026-06-24 | Git = **GitHub Flow**; never push to `main`, always branch + PR. Enforced by a local `pre-push` hook (`.githooks/`) | Server-side branch protection needs public repo / Pro (invariant #8). Local hook blocks accidental direct pushes, free, keeps repo private | Yes (owner) |
| 2026-07-16 | DB = **PostgreSQL**, reaffirmed vs MySQL | Exclusion constraint + RLS are load-bearing (boss fights #1, #5) and already merged; MySQL has neither. Learning the Postgres delta is the point | Yes (owner) |
| 2026-07-16 | Routing = **TanStack Router**, replacing React Router | Typed routes + zod-validated search params fit the URL-driven booking funnel; pairs with TanStack Query. Swap cost near zero (2 files) | Yes (owner) |
| 2026-07-16 | Deploy = **single VPS** (Caddy + Docker Compose: SPA + api + Postgres); amends invariant #8 | Cron needs an always-on process (free tiers sleep); one origin kills cross-site cookie/CORS complexity; ops skills = portfolio value | Yes (owner) |
| 2026-07-16 | iCal conflict policy: per-VEVENT savepoints, `sync_conflict` inbox, never auto-cancel a confirmed booking, reconcile only on healthy parse (#38) | The exclusion constraint rejects real-world overbookings; a human must pick the loser; a truncated feed must never mass-cancel | Yes (owner) |
| 2026-07-16 | Photos = S3-compatible storage: MinIO (dev) + Cloudflare R2 free tier (prod), presigned PUT uploads (#39) | Dev/prod parity via one client; R2 is forever-free with zero egress; card-on-file caveat flagged | Yes (owner) |
| 2026-07-16 | Composite FKs enforce the `tenant_id` denormalization (property→unit→booking/channel_connection) (#40) | Wrong tenant_id under RLS = silent cross-tenant leak; make it unrepresentable | Yes (owner) |
| 2026-07-16 | ORM = **Drizzle** (drizzle-orm + drizzle-kit + pg), replacing Prisma; migrations re-baselined (#41) | Owner preference + SQL-first fit: composite FKs modeled natively; hand-written SQL is drift-immune (kit diffs snapshots, not the DB); no query-engine binary | Yes (owner) |
| 2026-07-16 | Dev/fallback object storage = **Garage**, replacing MinIO in the photos decision (#39); prod stays R2 | MinIO community edition retired upstream (archived Apr 2026, source-only, no patches); Garage is actively maintained, lightweight, built for small self-hosted. SeaweedFS = documented plan B | Yes (owner) |
| 2026-07-16 | **Dev-fixture credentials may be committed** (Garage keypair + rpc_secret in compose/`garage.toml`/`.env.example`, like the Postgres `sambung/sambung`); "secrets never leak" reads as credentials that guard something real. Prod secrets live only in the VPS env, never in the repo | Fresh clone must work with `.env.example` values (#39 AC); localhost-only, loudly annotated. Real risk is pattern drift, mitigated by comments + gitignored `.env` | Yes (owner) |
| 2026-07-17 | **Transaction seam = the service, not the repository.** `TenantDbService.run` joins an open transaction (dedicated `AsyncLocalStorage`) instead of always opening one; services own the unit of work (#72) | Per-method transactions made composition impossible, so rules migrated *into* the repository (`deleteWithGuard`) and `auth.service` reached past the seam to get a real transaction. api-spec §5.3 needs check-then-insert in one txn; without this, boss fights #1+#2 land in the layer architecture §8 assigns to the service. CLS = who asks / ALS = which txn, because CLS is request-scoped and the M2 sweeper has no request. Flat join, not savepoints: `savepoint()` waits for M4's real caller (#38) | Yes (owner) |
| 2026-07-17 | **One module owns the tenant principal.** `Principal` + the CLS key live with `TenantContext`; `TenantDbService` asks it instead of reading CLS; `run` throws with no principal; repositories drop the `tenantId` param but keep every `WHERE` (#76) | The owner module was bypassed by its most important reader, so a key rename broke RLS scoping to silent zero rows. The param was a second path for an id the implementation already reached - now both read one source and cannot disagree. The `WHERE` stays because it guards RLS *not being in force* (boot on `DATABASE_URL` → owner role → no policies), which is one env var; a test now proves each layer holds alone. Throwing beats "fail-closed" because that claim is false on a warm connection (#74). JWT's `sub` stays in `AccessPayload` - the domain doesn't learn the token's vocabulary | Yes (owner) |
| 2026-07-17 | **RLS policies use `nullif(current_setting('app.tenant_id', true), '')::uuid`**; all 9 rewritten by migration `0002` (#74) | "Unset GUC → NULL → fail-closed" was false: `set_config(..., true)` reverts at COMMIT to the *reset* value, which for a custom GUC already set once is `''`, not NULL. So a no-tenant query returned zero rows on a cold connection but errored 22P02 on a warm one - the second layer of defense-in-depth didn't behave as documented, and which failure you got depended on the pool. `nullif` maps both unset and reset to NULL; the comparison stays `uuid = uuid`, so the tenant indexes survive (a `::text` compare would fail closed too, at the cost of a seq scan per query). `0000_init.sql` left as applied history - migrations are append-only, and `0002` supersedes its comment | Yes (owner) |
| 2026-07-17 | **Constraint violations get one map: constraint NAME → the response it means**, applied by a global interceptor; services no longer catch (#80) | The DB names the domain fact - `booking_no_overlap` IS "those dates are taken" - so mapping the name once is what makes api-spec §5.3's "the client cannot tell which layer refused" true by construction rather than by two copies of a string. Keyed on name, not SQLSTATE: 23505 means "some unique thing exists", which is not an answer; the three `is*Violation` predicates are deleted, having had 0 real callers. Unmapped → 500, loudly: a constraint nobody considered is a bug, not a 409. The interceptor TRANSLATES (rethrows an HttpException) rather than renders, so Nest's exception layer is untouched; a service that wants its own outcome (M3's webhook: duplicate `payment_event` → 200) just catches first, outside the transaction (25P02) | Yes (owner) |
| 2026-07-17 | **A Unit is one sellable thing, not a room type with a quantity** ([ADR-0001](docs/adr/0001-unit-is-one-sellable-thing.md), #45). PRD FR-PROP-2's "units/room types" is corrected; 3 identical rooms = 3 rows | Quantity inventory can't be guarded by the exclusion constraint - you'd count overlaps and compare, which is the read-then-write race boss fight #1 exists to make unrepresentable. Cost: 8 rooms = 8 Units + 8 OTA calendars, so bulk entry drives the UI and `unique(property_id, name)` stops the owner wiring Airbnb's feed into the wrong "Garden Room" | Yes (owner) |
| 2026-07-17 | **Deleting inventory never destroys the ledger** ([ADR-0002](docs/adr/0002-deleting-inventory-never-destroys-the-ledger.md), #45). Guard blocks on *any* booking ever, not just future occupying; both `booking→unit` FKs → `no action`. Supersedes api-spec §4.4 | The guard protected the calendar, not the ledger: `DELETE /properties/:id` returned 204 while cascading away years of past bookings and their `payment` rows. Cancelling is a domain event; deleting is amnesia. Under CASCADE the DB was an accomplice, inverting invariant #5 (app = UX, DB = correctness). Free at M1 (zero bookings exist), a live-data change after M2. **Both** FKs or the survivor cascades first and the guard silently does nothing. `no action` vs `restrict` is only a tie-break (default + defer-able later) - the "restrict would break tenant deletion" rationale was folklore, disproven by measurement in review; the load-bearing change is cascade → not-cascade. Makes archive an M2 blocker (#84) | Yes (owner) |

| 2026-07-17 | **A Visitor is a principal, scoped by the slug they opened** ([ADR-0003](docs/adr/0003-a-visitor-is-a-principal.md), #77/#46). `PublicScope.enterFromSlug` resolves one column on the owner connection, then seeds `{ kind: 'visitor', tenantId }`; everything after runs under RLS unchanged. `Principal` becomes a union | The funnel's entry is cross-tenant - you can't scope by tenant before finding the property, because the property tells you the tenant. #77's `runAsTenant(tenantId, fn)` would put a tenant id back on a parameter list, on the class that IS boss fight #5, one issue after #76 removed it; any caller could then name any tenant, whereas a resolved Visitor is confined to the tenant whose URL the guest already typed. The union makes `principal.role` a compile error until the Visitor case is handled. The unscoped statement reads one column keyed by a deliberately-public value, so a future `payout_account` can't leak through it. `sambung_public` role = documented upgrade path | Yes (owner) |
| 2026-07-17 | **A property's public URL is an address, not a view of its state** ([ADR-0004](docs/adr/0004-a-public-url-is-an-address-not-a-view-of-state.md), #46). Slug minted once at create, never moved by a rename; `publishable` never gates the public page | One principle, two applications: a live URL must not die as a side effect of an edit nobody thought was about the URL. The product IS a link pasted into an OTA profile or forwarded on WhatsApp - a name-tracking slug breaks every one silently, and a `publishable` gate does the same when someone deletes a photo. `publishable` was never a gate anywhere it's used (page-spec §4.4/§4.5 = a checklist); api-spec §4.7 specifies one failure, unknown slug. Slug uniqueness is minted by `ON CONFLICT (slug) DO NOTHING` + random suffix, never a 409: the owner typed a name, and telling them it's taken is a cross-tenant existence oracle. Redirects stay additive; "follows the name" would not have | Yes (owner) |
| 2026-07-18 | **Archived inventory is derived up the hierarchy, not cascaded** ([ADR-0005](docs/adr/0005-archived-is-derived-not-cascaded.md), #84). `archived_at` (nullable) on `unit`+`property`; effective-archived = a Unit's own flag OR its Property's. Archive/unarchive = idempotent POST verb-subresources | ADR-0002 made a booked Unit undeletable *and* permanently public; archive is the retirement verb it deferred. Derive, not cascade, because of unarchive: one `archived_at` per row can't tell self-archived from parent-archived, so cascading either resurrects a one-off retirement on the property's unarchive or needs a second marker - deriving makes the round-trip correct by construction, matching the codebase's derive-don't-store grain (availability, `verified`, `publishable`). The guard against a forgotten `WHERE` is the booking chokepoint (§5.3 availability re-validation), not a partial index (a perf tool, not a correctness guard) or a view (fights Drizzle/RLS, and the owner must still see history). No RLS policy (intra-tenant visibility ≠ cross-tenant isolation); no lock (single-row flag write, in-flight bookings honoured) | Yes (owner) |
| 2026-07-18 | **An archived Property is retired, not just incomplete** ([ADR-0006](docs/adr/0006-an-archived-property-is-retired-not-addressed.md), #84). Archived Property → public page `404` (slug stays reserved); an archived Unit merely drops out of the unit list | Reconciles ADR-0004's "the page always renders". The axis is intent: `publishable` is an incomplete checklist (never a gate - the *surprising* death ADR-0004 forbids); archive is a *deliberate* take-down (a gate, on purpose). `404` not `410`/tombstone - matches the "hidden = 404, never an existence oracle" convention (api-spec §1). Enforced in `findPublicBySlug`, not `enterFromSlug`, which stays a pure tenant-resolver so §5.3 can resolve-then-`409` an archived Unit. The slug row persists → unarchive restores the exact URL, so ADR-0004's permanent-address guarantee holds | Yes (owner) |
| 2026-07-18 | **Design system: one brand, two surfaces** ([ADR-0007](docs/adr/0007-two-surface-design-system.md), [design-system.md](docs/design-system.md), #91). Brand = "the gracious host": stone neutrals + terracotta accent, Plus Jakarta Sans + Fraunces (system CJK fallback for ZH), lowercase `sambung` wordmark; name stays Sambung, closing PRD §10.4. System: dashboard = shadcn/ui rethemed (copy-in, `components/ui/`); public funnel = custom components on headless behavior (react-day-picker, Radix - no shadcn imports in `public-booking`); pages speak semantic tokens only, ramps live only in `packages/config/tailwind.css`; the 7 existing pages migrate in one sweep before M2 UI | The funnel is where a stranger decides to pay: hand-design where guests look, dependable boring components where owners work; copy-in = owned a11y without the templated skin (never hand-write calendar ARIA); semantic-only pages = derive-don't-store applied to CSS, so retheming is a token swap; sweeping at 7 pages is the cheapest it will ever be | Yes (owner) |
| 2026-07-18 | **A public resolver resolves, it does not judge** ([ADR-0008](docs/adr/0008-a-public-resolver-resolves-it-does-not-judge.md), #47). `PublicScope.enterFromUnitId` (the second public entry, for the `GET availability` quote) resolves the tenant for any *existing* unit and never checks archived; the archived judgement is made downstream at the chokepoint - #47 read → `404`, #48 write → `409`. Broadens ADR-0003 (a Visitor is scoped by the public identifier they opened - slug *or* unit id) | A resolver that 404'd archived at the door makes #48's specified resolve-then-`409` impossible and splits one cross-tenant entry point into two that drift; keeping it pure (one column, a public key, judges nothing) is what a reviewer greps and what stops a future `payout_account` leaking through it | Yes (owner) |
| 2026-07-18 | **A hold is cleared by a sweep - at two scopes** ([ADR-0009](docs/adr/0009-a-hold-is-cleared-by-a-sweep-at-two-scopes.md), #48). Pessimistic 15-min hold; a lapsed hold is flipped to `expired` by (1) an **opportunistic intra-tenant** `UPDATE` inside the booking txn (Visitor's RLS scope), run before the re-check, and (2) a **5-min cross-tenant cron** on the owner connection as backstop. Both idempotent (WHERE matches only past-TTL holds) | The exclusion predicate is immutable, so it can't check `now()` - a sweep is unavoidable. Cron-only leaves a ≤5-min false-block at the funnel's decision moment; the opportunistic sweep makes freed dates bookable *there*. The two scopes are forced by isolation (intra-tenant fits the Visitor's scope with no special connection; all-tenants *needs* the owner connection) and each explains why one sweep can't be both. Single VPS = one process = no distributed lock; read stays ≤5-min pessimistic on purpose (a GET must not write) | Yes (owner) |
| 2026-07-18 | **The Calendar is composed, not served** ([ADR-0010](docs/adr/0010-the-calendar-is-composed-not-served.md), #49). The owner's unified calendar is assembled client-side from neutral endpoints - one shared `GET /bookings` (full rows, **repeatable** `status`, the caller names the two occupying statuses via a shared `OCCUPYING_STATUSES` constant), a flat `GET /units` (effective-archived flag, ADR-0005), `GET /properties` - not a bespoke `GET /calendar` aggregate. Disclosure follows the audience: the owner read returns whole rows, the public availability read (#47) clips for privacy - same overlap-window filter, opposite rule | A `GET /calendar` would be a second booking-read path that drifts from #51's; neutral primitives serve #49/#50/#51 while a per-page tree serves one. The exclusion constraint makes each Unit row non-overlapping, so there is no server-side layout to centralize; occupancy is *derived* by joining bookings onto Units in the view, the same grain as invariant #3's derived availability | Yes (owner) |

| 2026-07-19 | **The owner is an authority, not a customer** ([ADR-0011](docs/adr/0011-the-owner-is-an-authority-not-a-customer.md), #50). `POST /bookings` (manual block / walk-in) reuses the guest funnel's *one* overlap chokepoint - the opportunistic in-txn hold-sweep + `quote()`'s re-check + `booking_no_overlap` → the **identical** `409 {reasons:['overlap']}` - but is born `confirmed` with no hold, authenticates (owner RLS connection, **no** `enterFromUnitId`; cross-tenant/unknown unit → 404), and enforces **only** the physical invariant (overlap), **not** the guest-protection policy checks (min_stay, max_guests). A walk-in is `source=direct` (entry-method derived, not a new enum); a block is `manual_block` with `total_price_idr=NULL`; an archived Unit → `409 {reasons:['archived']}`. Cancel (`POST /bookings/:id/cancel`) is an atomic FSM-guarded UPDATE and the universal free-the-dates verb (holds, walk-ins, blocks); `refund:"manual"` iff a paid payment exists (always `"none"` at M2). Detail read is net-new `GET /bookings/:id` (owner full disclosure - phone/email - 404-over-403) | Overlap is physics that binds everyone, so the read-can't-disagree-with-write authority (one `quote()`) must hold for the owner too; a second owner-only check would be a second definition of "taken" that drifts from #48's. min_stay/max_guests are the owner's own rules - forcing them on hand-entry would make the tool refuse to record reality (a 1-night maintenance block, an over-capacity walk-in taken by phone). `source` = channel not entry-method: a walk-in *is* direct business, and M3's "no provider session" is one derivation away (confirmed + direct + no payment row). Corrects the stale §5.4 `guestContact?` body (pre-migration-0007) | Yes (owner) |

| 2026-07-19 | **A 409 carries a code, not a sentence** ([ADR-0012](docs/adr/0012-a-409-carries-a-code-not-a-sentence.md), #82, deferred out of #80). Every 409 - whichever layer refuses - has body `{ statusCode, error, code, message, ...detail }`: `code` is a slug from a closed `conflictCodeSchema` in `packages/shared` imported by both sides (rename = compile error); typed detail rides beside it (delete guard `count`, booking `reasons`, cancel `status`); `message` stays a human default the web NEVER renders. The web owns all copy (`describeConflict`, an exhaustive switch over the shared union). Chose a dedicated `code` over slug-in-`message` (§8.2's literal wording, now refined): the two already-structured errors (`datesUnavailable`/`bookingNotCancellable`) put the machine field beside a human `message`, so `code` generalizes that instead of overwriting the prose; keeps machine-identity and human-default as separate concerns. Delete guards moved inline→factories (`propertyHasBookings`/`unitHasBookings`); `ApiError` gained `body`; `apps/api` pins every factory to `conflictBodySchema` (like §8.6's pgEnum pin); §5.3's both-layers-indistinguishable test still holds (same factory → byte-identical bodies) | "Two conventions split by which layer refused" is what §5.3 forbids; prose-only 409s also blocked i18n (server prose can't be translated client-side) and welded the delete count into an unreadable English sentence. One slug set, one body shape, copy on the client = the contract §8.2 promised, made whole | Yes (owner) |
| 2026-07-19 | **Rate limits are tiered, and a 429 follows the envelope** ([ADR-0014](docs/adr/0014-rate-limits-are-tiered-and-429-follows-the-envelope.md), #59). Two named throttlers: the generous `default` on every route (#48) + a TIGHTER `sensitive` (env-driven, protective 10/60s) gated to the abuse-prone few - `login`/`register`/`POST /public/bookings` - by a greppable `@ThrottleSensitive()` marker + the throttler's `skipIf` (a no-op everywhere else, so a legit owner browsing never trips it); own per-handler bucket each. An `EnvelopeThrottlerGuard` renders the 429 as `{ statusCode, error, message }` (NOT a conflict `code` - ADR-0012's enum is closed to domain 409s; a 429 is infra back-pressure) + a standard `Retry-After`. Both limits stay in the ConfigService factory (not a decorator constant) so they're env-off in test - the throttle spec drops one env var, compiles its own app, fires `limit+1` (no sleep) | A hard-wired tight limit breaks normal dashboard use AND 429s the concurrency suite; a decorator constant can't read ConfigService, so it can't be env-off in test. Marker+`skipIf` keeps the decision next to the route; envelope+`Retry-After` makes a 429 shape-identical to every other refusal, so the web branches on `statusCode`, not a special case | Yes (owner) |

| 2026-07-19 | **The picker advises, the server decides** ([ADR-0013](docs/adr/0013-the-picker-advises-the-server-decides.md), #93). The `/p/:slug` availability calendar (react-day-picker headless + Sambung components, ADR-0007) hard-disables **only** the past (`{before: browser-local-today}` + `startMonth`=this month, so no paging backwards); booked nights are a greyed **modifier**, never `disabled`, so a range spanning one is sent to `GET availability` and the quote card renders the server's `overlap` + the clipped booked nights (AC). Two query modes on one endpoint, one cache key `["availability", unitId, from, to]` (visible-month sweep greys; debounced ~300 ms selection quotes); selection state lives entirely in `?from&to&unit` (a shared link reproduces the quote view). Thin `/p/:slug/book` placeholder added so the Book CTA lands somewhere real (checkout form/payment is a later issue) | A client that disabled booked nights would be a second, drift-prone definition of "taken" contradicting the exclusion-constraint authority (invariants #3/#5, ADR-0009), and would forbid a valid checkout-on-a-changeover (half-open `[from,to)`, db-design §4.2); greying keeps one authority and makes "highlight the clipped nights" possible. Past is the one guard that is genuinely the UI's, keyed to the guest's local day | Yes (owner) |
| 2026-07-19 | **A payment session is one reused row, addressed by its own id** ([ADR-0015](docs/adr/0015-a-payment-session-is-one-reused-row.md), #52). `POST /public/bookings/:id/pay` opens a Midtrans Snap **sandbox** session for a live Hold: one transaction under the tenant `PublicScope.enterFromBookingId` (the third pure resolver, ADR-0008) resolves; the opportunistic hold-sweep (ADR-0009) runs first so "hold lapsed" and "wrong status" become ONE post-sweep read → `409 booking_not_payable {status}` (new closed-set code, ADR-0012); a `FOR UPDATE` on the booking serialises concurrent pays so a retry REUSES the booking's open (`pending`) payment row - **`payment.id` is the Provider `order_id`** (the key #53's webhook resolves by), the session `{token,redirectUrl}` lives in `raw_payload` (a Snap token is Provider vocabulary, not a typed column), and `amount_idr` is snapshotted. Amount = Deposit share, BigInt-floored (invariant #6): `property.deposit_pct smallint 1-100 default 100` (migration 0008), editable on the property edit page (api #10); `deposit` flag = amount < total. Provider behind an injected `PaymentGateway` port (MidtransGateway over `fetch`, no SDK; `FakePaymentGateway` bound in tests via `.overrideProvider` - AC #3). Web: the real checkout form (create → pay → redirect to Snap) + a thin `/booking/:id` landing (reconcile page is #54) | A pay endpoint must be idempotent (invariant #7): one open row + a booking-row lock makes "retry ≠ double-charge" true by construction, not luck. `payment.id` as `order_id` is globally-unique-for-free AND pre-wires #53's lookup; the session in `raw_payload` keeps the Provider-agnostic table from calcifying a Midtrans concept into a column (the §8.6 enum-drift smell in table form); the amount snapshot is the payment-row analogue of a booking snapshotting its price. A port tested by a fake is the only honest way "tests run without live Midtrans" holds - an env stub is a second path that can ship to prod | Yes (owner) |
| 2026-07-19 | **The .ics export feed is addressed - and authenticated - by its unit UUID** ([ADR-0016](docs/adr/0016-the-ics-export-feed-is-addressed-by-its-unit-uuid.md), #55). `GET /public/units/:id/calendar.ics` is unauthenticated (an OTA's subscriber has no token), so the unguessable unit UUID is BOTH the address and the capability: it resolves the tenant via `PublicScope.enterFromUnitId` (the pure resolver, ADR-0008) and reads confirmed bookings UNDER RLS, so the feed is structurally incapable of becoming a cross-tenant read path (invariant #2). Deliberately **archive-blind** (the third downstream judgement over one pure resolver - read→404, write→409, export→none): an archived Unit with bookings MUST keep serving its calendar or the subscribed OTA sees free nights and double-books. PII cannot leak **by construction** - the export selects only `(id, check_in, check_out)` for `status='confirmed'`, and the hand-rolled RFC-5545 serializer's `CalendarEvent` type has no field for a name/email/phone/price; `SUMMARY` is a fixed constant. Half-open `[check_in, check_out)` maps to an all-day `DTEND` (also exclusive) with zero arithmetic. Connect (§7.1) smoke-fetches via an injected `IcalFetcher` port (fake in tests, §8.5; real one refuses private/loopback hosts - SSRF hygiene) and a feed that's down still connects with `error` status; duplicate `(unit, channel)` → `409 channel_already_connected` (new closed-set code, ADR-0012; app pre-check + `channel_connection_unit_channel_uniq` constraint indistinguishable, §5.3, migration 0009). Disconnect keeps imported bookings (FK `set null`) and reports `{ importedBookingsKept }`; never auto-cancels. A per-feed token is the documented hardening path | Yes (owner) |
| 2026-07-19 | **Orphaned photos are swept against the gallery, per tenant** ([ADR-0017](docs/adr/0017-orphaned-photos-are-swept-against-the-gallery.md), #69, deferred from #39). A daily `@Cron` `PhotoGcSweeperService` on the owner connection (cross-tenant, mirroring the M2 hold sweeper) reclaims photo objects no gallery references. The DB gallery is the AUTHORITY: `referenced` = union of every `property.photos` (one owner-connection read); a referenced key is NEVER deleted (the one invariant that matters, tested first). Tenant-scoped listing (`<tenantId>/` prefixes of tenants THIS db knows) is a SAFETY boundary, not an optimisation - a whole-bucket sweep would treat another dev/CI lane's objects (shared Garage store, isolated DBs) or Garage's `index.html` as unreferenced and destroy them. 24h grace window (never race an in-flight upload; presign lives 5 min) via `sweep(now)` - a test passes a future `now` to age a just-uploaded object without faking S3 mtime. Oversize eviction (`> MAX_PHOTO_SIZE_BYTES`) is a grace-independent backstop that also STRIPS the key from its gallery (`array_remove`) + logs loudly - the one place the sweep writes. `StorageService` gained `listObjects`/`deleteObjects`. Test-first against real DB+Garage, every sweep confined to its own tenant | The store drifts from the ledger (abandoned uploads, dropped-but-lingering keys, oversize slipping past a non-enforcing backend), and #39 deferred the reclaim as "GC'd out-of-band". Delete FROM the store to match the gallery, never the reverse - the DB is truth, the store a cache holding extra bytes. A cron on the owner connection is the only shape that crosses tenants; tenant-scoped listing is what makes it safe on a shared store - the "tenant-scoped listing" acceptance criterion in disguise | Yes (owner) |
| 2026-07-20 | **The payment webhook reconciles on the owner connection, not under RLS** ([ADR-0018](docs/adr/0018-the-payment-webhook-reconciles-on-the-owner-connection.md), #53, boss fight #4). `POST /webhooks/payment/:provider` runs entirely on `DbService` (RLS-bypassed) like the cron sweepers - NOT a fifth resolve-then-RLS entry. A webhook is a SYSTEM reconciliation of platform-brokered money, not an actor browsing a tenant; it carries no principal, and every statement is PK-targeted (`order_id = payment.id`), so there is no tenant-scoped WHERE for RLS to backstop. Idempotency: in ONE `db.transaction`, INSERT `payment_event(provider, provider_event_id)` then apply the state change; the unique constraint arbitrates (redelivery / concurrent loser → caught OUTSIDE the txn via `pgError().constraint`, 25P02 → 200 no-op), and a crash between insert and apply rolls back BOTH → clean replay. `provider_event_id = transaction_id:transaction_status` (a redelivery collapses, a real pending→settlement does not); every UPDATE guarded on current status so no re-order un-confirms a paid booking or resurrects a dead one. Late settlement (hold lapsed) → payment paid, booking NOT resurrected + loud WARN (guard on status, not `hold_expires_at`, so a past-TTL-but-still-pending hold DOES confirm - the exclusion constraint held the nights). Signature+parse behind the `PaymentGateway` port (`verifyAndParse`, SHA512 over parsed fields → no raw-body middleware; `FakePaymentGateway` verifier keeps tests off live Midtrans); verified payload on `payment_event.raw_payload` (migration 0010), never on `payment.raw_payload` (the session a retry reads). Unknown provider → 404; verified-but-unknown order → 200+WARN; NO `@ThrottleSensitive` (a 429 to a provider = retry storm). FR-NOTIF-1 email is a post-commit seam (logged no-op, can't fail the webhook), deferred | Overlap-of-authority: the read-can't-disagree-with-write principle demands ONE definition of "processed", and the unique constraint + guarded applies make "duplicate = no-op" true by construction, not by luck (invariant #7). Owner connection because the sweeper category (no principal, crosses to whichever tenant owns the row) fits a system reconciliation, whereas resolve-then-RLS is for principals that browse - and minting a Visitor for a Provider is the lie the Principal union forbids. Two-session-reviewed (no-auth write across the RLS boundary) | Yes (owner) |
| 2026-07-20 | **Link-preview crawlers get real OG tags at the edge, from one shared helper** ([ADR-0019](docs/adr/0019-og-tags-for-crawlers-at-the-edge.md), #87, SEO tier 2). `deploy/Caddyfile` matches a NARROW, explicit allowlist of preview-crawler UAs (WhatsApp/`facebookexternalhit`/Twitterbot/Telegram/LINE/…, never Googlebot - it renders JS, a stub would be cloaking) on `/p/*` and proxies just those to `GET /public/properties/:slug/og`; everyone else gets the SPA. The route reuses the JSON page's `getBySlug` (same `enterFromSlug`+RLS, archived→404 per ADR-0006, malformed→404 via `SlugParamPipe`) - not a second read path - and renders a static, HTML-escaped OG stub with a meta-refresh bounce to `/p/:slug`. OG values come from ONE shared helper `buildPropertyOgTags` (`packages/shared`) that the SPA's `property-meta.tsx` <meta> tags use too, so crawler card and rendered page can't drift. Caddyfile not run in tests; its UA allowlist asserted narrow by `property-og.spec.ts`. Tenant-authored name/description escaped (5 chars) to block attribute/HTML injection into a page on our origin | Yes (owner) |
| 2026-07-20 | **Reconcile-on-read pulls the same event the webhook pushes** ([ADR-0020](docs/adr/0020-reconcile-on-read-pulls-the-event-the-webhook-pushes.md), #54). `GET /public/bookings/:id` (the confirmation page, the FIFTH public route) reconciles on read (risk R3): a `fetchStatus` on the `PaymentGateway` port PULLS the Provider's status as the SAME `ParsedPaymentEvent` a webhook yields, and `PaymentWebhookService.reconcile` feeds it through the IDENTICAL idempotent transition `handle` uses (owner connection, ADR-0018) - so a lost webhook still confirms, and whichever path wins, the `payment_event` unique constraint no-ops the rest BEFORE `afterCommit`. FR-NOTIF-1 email = a best-effort post-commit seam behind a `Mailer` port (`LogMailer`, no paid provider - invariant #8; real Resend/SMTP = one rebind), fired ONLY on the status-guarded confirm that flips a row, so it's once-per-confirmation by construction. Read = `enterFromBookingId` (404 unknown, ADR-0008) → reconcile → opportunistic hold-sweep AFTER (ADR-0009: a just-confirmed booking is skipped, an unpaid past-TTL hold flips to `expired` now) → RLS-scoped view. `waLink` = the guest's own wa.me deeplink (FR-NOTIF-2), server-built. `afterCommit`/`notifyConfirmed` now awaited (deterministic, still can't fail the webhook). New env `MIDTRANS_API_BASE_URL` | The read-can't-disagree-with-write spine demands ONE definition of "processed"; reusing the webhook's transaction (not a second confirm path) makes "email exactly once, duplicate-safe" true by construction, not luck (invariant #7). A `Mailer` port tested by a recording fake is the only honest way "no live email in tests / no paid provider" holds | Yes (owner) |
| 2026-07-20 | **The confirmation email sends through Resend when configured, else logs** ([ADR-0021](docs/adr/0021-the-confirmation-email-sends-through-resend-when-configured.md), #119). Fills in the provider ADR-0020 deferred: a `ResendMailer` over native `fetch` (no SDK, mirroring MidtransGateway/ADR-0015 - keys read at CALL time, 8s timeout, REJECTS on any failure so the best-effort caller logs it) implements the existing `Mailer` port. `MAILER` is bound by a `createMailer` FACTORY that returns `ResendMailer` iff `RESEND_API_KEY` **and** `MAIL_FROM` are set, else the zero-cost `LogMailer` - so dev/test/unconfigured-prod stay off any live provider (no suite reaches Resend: test env has no key → LogMailer, and the e2e test overrides `MAILER` with a recording fake) and real sending is ONE env flip, zero call-site change. Factory-with-fallback, NOT payment's `useClass`+loud-throw: email is best-effort → degrade gracefully to a rendered log line; money must fail loud, a confirmed booking must not depend on a wired mailer. Resend free tier only (invariant #8); `nodemailer`/SMTP = documented plan B, rejected to add ZERO deps. EN-only template (FR-I18N-1 = follow-up). NO migration (owner email already sourced by NotificationsRepository, #54); send-loop per-recipient isolation (#126) left untouched. Deploy: verify a domain in Resend, set `RESEND_API_KEY`+`MAIL_FROM` in VPS env (gitignored, never repo) | The read-can't-disagree spine needs ONE send path bound by config, not a `NODE_ENV` stub that could ship; a fetch adapter matching the gateway grain adds no dependency and is proven against a mocked transport, the only honest way "tested without live creds" holds | Yes (owner) |
| 2026-07-20 | **The funnel and the dashboard are two bundles; libphonenumber is a leaf chunk** ([ADR-0023](docs/adr/0023-the-funnel-and-dashboard-are-two-bundles.md), #125, follow-up to #54 review). Every route component loads through `lazyRouteComponent` (route tree - paths/`validateSearch`/`beforeLoad` - stays static; only leaf components defer), so Vite emits one chunk per page and a guest on `/p/:slug` never downloads the dashboard. `libphonenumber-js` is a **dynamic** import of the checkout page (`phone.ts` unchanged; lazy boundary at its import site), fetched at the phone step, so the property/confirmation pages never carry it. `scripts/check-bundle.mjs` (Node built-ins, reads the Vite manifest) fails the build if the dashboard or the phone chunk is statically reachable from the property route, if `phone.ts` stops being a dynamic import of checkout, or if the property route's initial JS crosses a gzip budget (185 KB). Property page initial JS: ~234 → ~161 KB gzipped, no dashboard/libphonenumber. `lazyRouteComponent` over `.lazy.tsx` stubs (whole tree stays in one file); per-route over coarse funnel-eager/dashboard-lazy (the latter still ships checkout's libphonenumber in the entry). vitest async timeout raised (cold chunk transform vs the 1 s default) - test-transformer concern, invisible in prod | Funnel first-load weight is where ADR-0007's "hand-design where guests look" cashes out (mobile Indonesia); a shared monolith made a guest pay for the whole dashboard + a checkout-only 25 KB dep. Structural guard (what's *reachable*, not just bytes) fences the regression even if a re-merge stayed small | Yes (owner) |
| 2026-07-20 | **The paid-but-lapsed inbox marks, it does not mutate the ledger** ([ADR-0022](docs/adr/0022-the-paid-but-lapsed-inbox-marks-not-mutates.md), #120, follow-up to boss fight #4). The owner-facing surface for the late-settlement case ADR-0018 handles silently: a guest pays AFTER the hold lapsed/was cancelled, so `payment.status=paid` while `booking.status IN (expired,cancelled)`, and a WARN is not a workflow. A nullable `payment.handled_at` marker (migration **0011**) + two authed owner-RLS routes: `GET /payments/lapsed` (paid + lapsed-booking + `handled_at IS NULL`, joined for amount/guest/dates/names - owner disclosure, scoped by `booking.tenant_id` beside RLS since `payment` has no `tenant_id`) and `POST /payments/:id/handle` (a guarded UPDATE that writes ONLY `handled_at`, never `payment.status`/the booking - the item leaves the inbox because the list's predicate excludes it, not because the ledger changed; ADR-0002). Handle is idempotent (already-handled → 200 no-op; unknown/cross-tenant/non-inbox → 404), so no new ADR-0012 conflict code. Amount-mismatch deliberately OUT (that path never produces a `paid` row); unhandle deferred (column is nullable). Tenant isolation proven at the HTTP layer (a reviewer's cross-tenant read AND handle both 404, B's row untouched) | "Handled" is a new operator fact, orthogonal to what the money did and the calendar says - deriving it is impossible, and encoding it by mutating the ledger corrupts the record to satisfy a UI (ADR-0002 generalized). A nullable column inherits `payment`'s row-level RLS for free (no policy migration) and matches the annotate-in-place grain (`archived_at`) | Yes (owner) |
| 2026-07-20 | **The funnel speaks three languages, the wire speaks one** ([ADR-0024](docs/adr/0024-the-funnel-speaks-three-languages-the-wire-speaks-one.md), #58, M5/FR-I18N-1). Public funnel EN/ID/ZH with **no i18n library** (invariant #8): a dependency-free typed message catalog (`apps/web/src/i18n/`) + a tiny `useSyncExternalStore` locale store + a `useI18n()` hook (typed `t(key)` + `fmtDate`/`fmtNights`/`fmtGuests`). Matches the house style (`lib/conflict.ts`/`availability-copy.ts` already compose copy from typed switches with zero libs); platform `Intl` + react-day-picker's already-bundled `id`/`zh-CN` locales cover dates + the calendar. **Untranslated-key guard has two layers**: compile-time (`id`/`zh` annotated `: Messages` from `typeof en` → a missing key fails `tsc`, which runs in `pnpm typecheck`, pre-push, AND the web `build`) + a runnable parity/token test (`pnpm test`) - new copy CANNOT ship EN-only. **Wire stays `YYYY-MM-DD`**; only display follows locale (EN funnel=en-GB, money stays id-ID always). **Availability blocked-reasons stay language-neutral slugs**, the SPA localizes - the approach api-spec §1/§5.1 + the shared schema comment MANDATE ("the SPA composes localized copy from the slug"), the ADR-0012 precedent; no server change. Choice persists in `localStorage` (`sambung.lang`, tokens unaffected) + rides as `Accept-Language`. Switcher rendered at the route-tree root, gated to public routes by pathname (dashboard stays English, page-spec §2); `I18nProvider` at root so every `renderAt` test gets it. Funnel-entry initial JS 161→170 KB gzip (still under the ADR-0023 185 KB budget) | Three languages where only EN inflects nouns don't need react-i18next/react-intl's tens of KB + ICU machinery; a 60-line catalog + Intl is simpler, robust, and adds zero deps. Server-rendered reason prose would duplicate the catalog into a second codebase, defeat response caching, and break the "code not sentence" spine (ADR-0012). Type-level parity is a stronger untranslated-key gate than a lint scan, and it is already wired into the build | Yes (owner) |
| 2026-07-20 | **A healthy feed reconciles; a doubtful one does nothing** ([ADR-0025](docs/adr/0025-a-healthy-feed-reconciles-a-doubtful-one-does-nothing.md), #56, boss fight #3, blocks #38). The iCal IMPORT pipeline: a 30-min cron + "Sync now" pull every `channel_connection` and mirror its VEVENTs into `booking`. Runs on the OWNER connection (RLS-bypassed, the sweeper/webhook category ADR-0009/0018 - a system reconciliation with no principal that crosses tenants), every write scoped EXPLICITLY by `tenant_id` + connection id. Fetch OUTSIDE the txn (never pin a pooled connection across a round-trip); one txn/connection with a **SAVEPOINT per VEVENT** so a `23P01` overlap (a real double-sell) skips that event and the cycle survives - the exact catch #38 drops its `sync_conflict` INSERT into. Idempotent by the `(channel_connection_id, external_uid)` partial unique index (`ON CONFLICT DO UPDATE` → re-pull no-op, changed stay in place, re-appearing UID re-confirmed). **"Healthy" = a whole, TERMINATED `BEGIN…END:VCALENDAR`** (truncation loses the `END`); unhealthy (unreachable / non-2xx / not-terminated / parse-throw) → `last_status=error`, ZERO writes. The absent-UID cancellation runs **only on a healthy feed with ≥1 event** - a healthy-but-empty one stamps `ok` but cancels nothing (empty is indistinguishable from truncated-to-zero → never mass-cancel). Parser HAND-ROLLED (`ical-parse.ts`, the symmetric ADR-0016 call; trust confined to a zod-validated `{uid,start,end}`, bad VEVENTs skipped not fatal; `ical.js` = plan B); `IcalFetcher` gains `fetchFeed` beside `probe` through one SSRF-guarded walk. "Sync now" → **200 synchronous + a summary** (no job queue on one VPS), resolving the connection under owner RLS first (404-over-403). NO migration (`external_uid` + its index shipped with #40/#48) | The read-can't-disagree-with-write spine needs ONE definition of "processed": the exclusion constraint + the partial unique index make idempotency and overlap-refusal true by construction, not by luck (invariant #7). Never-mass-cancel is a SAFETY bias made structural - a broken download is unhealthy by the terminated-envelope test, and even a valid-empty feed cancels nothing, so no truncation can ever wipe real stays. Owner connection because a cron with no principal that crosses tenants fits the sweeper category, not resolve-then-RLS. Two-session review is a hard AC (boss fight) | Yes (owner) |

*Append one row per architecture decision. Keep it terse.*

---

## Project Facts *(this is the part that changes)*

- **Stage**: M4 (channel sync) opened - the export feed + connection lifecycle shipped (#55); M3 (payments) in progress; M2 (availability & direct booking) complete; M1 inventory complete. Done: M0 (monorepo, DB schema + RLS migrations, seed with demo logins, auth API, local pre-push quality gate) + property CRUD with `verified`/`publishable`, dashboard list/edit pages, SPA session plumbing incl. `/login` (#44) + photo pipeline: Garage in compose, presigned uploads, gallery on the edit page (#39) + `/register` signup page (#63) + units CRUD with the inline table, first money-on-the-wire (`toRupiah`), and the delete guard rewritten to protect the ledger (#45, ADR-0001/0002) + **the public property page `/p/:slug` and the first unauthenticated path to the database** (#46, closing #77: `property.slug`, `PublicScope` + the Visitor principal, SEO tier 1, demo photos in the seed; ADR-0003/0004) + **archive - retire inventory that has history** (#84/#89, ADR-0005/0006: nullable `archived_at` derived up the hierarchy, not cascaded; archived Property → public `404` with the slug reserved; delete's 409 now names archive; two-session-reviewed). That cleared the M2 blocker - a booked Unit is no longer immortal-and-public. **M2 now open**: the availability + quote endpoint `GET /public/units/:id/availability` shipped - boss fight #2's READ side (#47/#94, ADR-0008: `PublicScope.enterFromUnitId` as a pure resolver; interval overlap/clip in SQL sharing the exclusion constraint's operators; `blockedRanges` unconditional + coalesced, reasons as slugs; archived Unit → `404`; two-session-reviewed, whose one finding - a `basePriceIdr × nights` overflow that could 500 the no-auth quote - was fixed by the `unit_base_price_max` nightly-rate cap (1e9 IDR, migration 0006, rejected twice over)). **Boss fight #1 shipped** (#48/#96, ADR-0009, two-session-reviewed - whose one finding, a global rate-limiter that collapses to one shared bucket behind Caddy without `trust proxy`, was fixed by env-gated `trust proxy` in `main.ts` + honest `.env.example` guidance; the booking logic itself passed, concurrency independently re-proven at N=10): the guest booking write `POST /public/bookings` (#48, ADR-0009). One transaction does an opportunistic intra-tenant hold-sweep, an in-txn availability re-check via the shared `quote()` (the one interval authority, joined into the txn), a `guest_count ≤ max_guests` check, then the INSERT the `booking_no_overlap` exclusion constraint arbitrates - a racing overlap maps to the *same* 409 the re-check gives (proven with real concurrency **and** a deterministic constraint-force). Pessimistic 15-min hold; dead holds cleared by a sweep at **two scopes** - the opportunistic in-txn one plus a 5-min cross-tenant cron on the owner connection - because the exclusion predicate is immutable and can't check `now()`. Migration 0007 split free-text `guest_contact` → `guest_phone`/`guest_email` and added `guest_count` (so a Unit's `max_guests` is finally enforced); refusal `reasons` = `overlap|min_stay|max_guests|unavailable` (the archived-Unit → 409, ADR-0008's deferred item, now landed); `@nestjs/throttler` guards the no-auth public surface (env-driven, protective default). **Design-system foundation shipped** (#91/#99, ADR-0007): stone+terracotta semantic tokens, Plus Jakarta Sans + Fraunces self-hosted (no CDN), shadcn rethemed in `components/ui/`, the lowercase `sambung` wordmark; every existing page swept off raw ramps onto semantic tokens - the base all M2 UI builds on. **The unified multi-property calendar shipped** (#49/#102, ADR-0010): the dashboard home `/app/calendar` - a hand-rolled CSS-grid timeline (no library; the exclusion constraint makes each Unit row a non-overlapping 1-D sequence), one Unit row each, occupying bookings as bars **coloured by source** with Holds hatched, off-window "continues" affordance. Composed client-side from a new authed `GET /bookings` (whole rows - owner disclosure, the opposite of #47's clipped public read; **repeatable** `status` naming the shared `OCCUPYING_STATUSES`; paired 366-capped overlap window) + a flat `GET /units` + `GET /properties`; property/window filters in typed search params. Two-session-reviewed - the one real finding (the property filter narrowing only the fetch, so other properties still drew as empty rows) was fixed with a regression test; tenant isolation proven 13/13 on real Postgres. **Owner booking ops shipped** (#50/#104, ADR-0011, two-session-reviewed): `POST /bookings` (manual block / walk-in, born `confirmed`, no payment dance), `GET /bookings/:id` (detail, owner full disclosure, 404-over-403), and `POST /bookings/:id/cancel` (FSM as an atomic guarded UPDATE, 0 rows → 404 vs 409 by an existence check). "The owner is an authority, not a customer" - the write reuses the guest funnel's *one* overlap chokepoint (a shared `sweepQuoteOrThrow`: opportunistic hold-sweep + `quote()` + `booking_no_overlap` → the identical `409 {reasons:['overlap']}`) but **skips** the guest-protection policy checks (`min_stay`, `max_guests`); a walk-in is `source=direct` (entry-method derived, not a new enum), a block is `manual_block` with a null price, an archived Unit → `409 {reasons:['archived']}`. Cancel is the universal free-the-dates verb (holds, walk-ins, blocks) with `refund:"manual"` iff a paid payment exists (always `none` at M2). Web: the calendar's empty-day cells are now buttons opening a block/walk-in dialog, and its bars link to the new `/app/bookings/:id` detail page (cancel via a confirm dialog). The review's one finding (AC #3's paid-refund branch implemented but untested) was closed with a seeded-payment test. **The reservations list shipped** (#51/#106, ADR-0010, no new ADR): the operational table `/app/reservations` - a frontend-only composition over the *same* neutral reads the calendar uses (`GET /bookings` + `GET /units` + `GET /properties`), shown as rows of **every** status rather than bars of the occupying ones. Filters (window/property/status/source) are typed, AND-ed, shareable search params - `status`/`source` repeatable set-params robust to a hand-typed single value; each combination its own cache entry sharing the `["bookings"]` prefix. Opens on the default **upcoming** window `[today, today+366)` (owner's call, reversing an initial no-default-window design): a lone/invalid edge falls back to it with a pair hint, never sent as a 400 (the never-send-a-lone-edge guard). Two distinct empty states (the AC) keyed on whether a filter is *touched* (the URL), not the result count: "No upcoming reservations" (default window empty - a past-only tenant isn't truly empty) vs "No matches" (explicit filter empty). Sorted by check-in (server order, never re-sorted). Cleanups along the way: promoted `StatusBadge`/`SourceBadge` out of the detail page (were private, would have drifted), plus `bookingTitle` and `lib/date` (`formatDate`/`addDays`/`todayIso`), and active nav styling; CONTEXT.md gained **Booking** (the base entity, previously undefined) and **Reservation** (the list view - a lens, not an entity). Not two-session-reviewed: a read-only composition over an already-reviewed endpoint. **Operational hardening shipped** (#59, ADR-0014): the reservations **CSV export** `GET /bookings/export.csv` - the list's `text/csv` twin, reusing the SAME filter-condition builder as `GET /bookings` (so "respects the active filters" is true by construction, not a second read path), joining property/unit NAMES in, emitting integer IDR as exact `bigint` digits (no float/separators/e-notation), RFC-4180-escaped with CSV-formula-injection neutralised on the attacker-controlled guest name, UTF-8 BOM for Excel; web "Export CSV" button downloads it via an authed `api.getBlob` (Bearer, 401-retry) as a windowed filename. Plus **tiered rate limiting**: a TIGHTER `sensitive` throttler (env-driven, protective 10/60s) on `login`/`register`/`POST /public/bookings` via a `@ThrottleSensitive()` marker + `skipIf`, on top of #48's generous `default`; an `EnvelopeThrottlerGuard` renders the 429 as `{ statusCode, error, message }` + a standard `Retry-After` (both limits stay in the ConfigService factory so they're env-off in test; the throttle spec drops one env var, fires `limit+1`, no sleep). **The availability picker UI shipped** (#93/#113, ADR-0013): the `/p/:slug` date picker (react-day-picker headless on #91's tokens) quoting against #47 - past-only hard-disable, greyed-but-selectable booked nights (the server stays the one overlap authority), the six picker states, and selection state entirely in `?from&to&unit` so a shared link reproduces the quote; the last M2 guest-funnel UI piece (checkout form + payment is M3). **M3 opened - the payment session + Deposit shipped** (#52, ADR-0015, two-session-reviewed): the guest→pay slice end to end. `POST /public/bookings/:id/pay` opens a Midtrans Snap **sandbox** session for a live Hold in ONE transaction under the tenant `PublicScope.enterFromBookingId` resolves (the third pure resolver, ADR-0008); the opportunistic hold-sweep (ADR-0009) runs first so "hold lapsed" and "wrong status" become one post-sweep read → `409 booking_not_payable {status}` (new `conflictCodeSchema` slug, ADR-0012); a `FOR UPDATE` on the booking serialises concurrent pays so a retry REUSES the booking's open (`pending`) payment row - `payment.id` IS the Provider `order_id` (the key #53's webhook resolves by), the session `{token,redirectUrl}` rides in `raw_payload` (a Snap token is Provider vocabulary, not a typed column), `amount_idr` snapshotted. Amount = the Deposit share, BigInt-floored (invariant #6): `property.deposit_pct smallint 1-100 default 100` (migration 0008), editable on the property edit page (api #10); the `deposit` flag = amount < total. The Provider sits behind an injected `PaymentGateway` port (MidtransGateway over `fetch`, no SDK; a `FakePaymentGateway` bound in tests via `.overrideProvider` - so no suite touches live Midtrans). Web: the checkout placeholder became the real guest-details form (create → pay → redirect to Snap, hold countdown, 409-routed copy) + a thin `/booking/:id` landing (the reconcile confirmation page is #54). Seed: Canggu is 30% deposit to demo the partial vs pay-in-full split. **M4 opened - channel connection lifecycle + iCal export shipped** (#55, ADR-0016): the OTA sync EXPORT side and the connect/list/disconnect lifecycle. `GET /public/units/:id/calendar.ics` (unauthenticated) resolves the tenant from the unit UUID via `PublicScope.enterFromUnitId` and reads confirmed bookings UNDER RLS - so a no-auth feed is structurally not a cross-tenant read path (invariant #2); it is deliberately **archive-blind** (an archived Unit keeps serving its calendar, or a subscribed OTA double-books) and PII-free by construction (a hand-rolled RFC-5545 serializer whose `CalendarEvent` type has no field for a name/price; half-open `[in, out)` → all-day `DTEND` exclusive with zero arithmetic). `POST /units/:id/channels` validates https at the boundary + smoke-fetches once via an injected `IcalFetcher` port (fake in tests, real one blocks private/loopback hosts) → `lastStatus`, a down feed still connects with `error`; `GET /units/:id/channels` lists status (`openConflicts` deferred with the import-side conflict inbox, #38); `DELETE /channels/:id` keeps imported bookings (FK `set null`) and reports `{ importedBookingsKept }` - never auto-cancels. Duplicate `(unit, channel)` → `409 channel_already_connected` (new `conflictCodeSchema` slug, ADR-0012; app pre-check + `channel_connection_unit_channel_uniq` unique constraint indistinguishable, migration 0009). Web: a per-unit Channels panel on the property edit page - copy-export-URL with the paste-into-OTA helper, connect form, status badges, disconnect. (Fixed along the way: the shared conflict closed-set test was missing `booking_not_payable` from #52 - CI is local-only, so it had slipped.) **Photo orphan GC shipped** (#69, ADR-0017): the out-of-band reclaim #39 deferred - a daily `@Cron` `PhotoGcSweeperService` on the owner connection (cross-tenant, mirroring the M2 hold sweeper) deletes photo objects no gallery references. The DB gallery is the AUTHORITY (delete FROM the store to match the ledger, never the reverse); a referenced key is NEVER deleted (the one invariant that matters, tested first). Tenant-scoped listing (`<tenantId>/` prefixes of tenants the DB knows) is a SAFETY boundary, not an optimisation - what makes a destructive sweep safe on a Garage store shared across dispatch/dev lanes whose DBs are isolated. 24h grace window (never race an in-flight upload) via a clock-shiftable `sweep(now)`; oversize eviction (`> MAX_PHOTO_SIZE_BYTES`) is a grace-independent backstop that also strips the key from its gallery + logs loudly. **#55 and #69 shipped in parallel via the dispatch workflow, each independent-reviewed (a fresh same-model reviewer, not the human/two-session gold standard - the caveat stands for #55's no-auth read path + migration)**: #55's one fix round closed an SSRF redirect-follow gap - the smoke-fetch now re-validates the https + private-host block on every redirect hop with a bounded cap, failing closed - plus a disconnect-confirm copy bug; #69's added the foreign-tenant-survives / unreferenced-oversize / real-cron-path safety-net tests before merge. **Boss fight #4 shipped - the idempotent payment webhook → confirmed** (#53, ADR-0018, two-session-reviewed): `POST /webhooks/payment/:provider` on the OWNER connection (RLS-bypassed, like the sweepers - a webhook is a system reconciliation of platform-brokered money, not an actor browsing a tenant; every statement PK-targeted by `order_id = payment.id`, so there is no tenant-scoped WHERE for RLS to backstop). Idempotency in ONE `db.transaction`: INSERT `payment_event(provider, provider_event_id)` then the guarded state change; the unique constraint arbitrates - a redelivery or the concurrent loser hits it, caught OUTSIDE the txn via `pgError().constraint` (25P02) → 200 no-op, and a crash between insert and apply rolls back BOTH → clean replay (all ACs proven on real Postgres incl. real concurrency + a spied crash-fault on the split-out `applyOutcome` seam). `provider_event_id = transaction_id:transaction_status` (a redelivery collapses, a real pending→settlement does not); every UPDATE guarded on current status so no re-order un-confirms a paid booking or resurrects a dead one. Late settlement (hold already swept to `expired`) → payment recorded `paid`, booking NOT resurrected + loud WARN (guarded on status, not `hold_expires_at`, so a past-TTL-but-still-pending hold DOES confirm - the exclusion constraint held the nights the whole time). Signature+parse behind the `PaymentGateway` port (`verifyAndParse`; SHA512 over parsed fields → no raw-body middleware; a `FakePaymentGateway` verifier + a real-crypto `midtrans.gateway.spec` keep suites off live Midtrans); the verified payload lands on `payment_event.raw_payload` (migration 0010), never on `payment.raw_payload` (the Snap session a pay-retry reads back). Unknown provider → 404; verified-but-unknown order → 200+WARN; NO `@ThrottleSensitive` (a 429 to a provider = retry storm); FR-NOTIF-1 email is a post-commit seam (logged no-op, can't fail the webhook), deferred alongside the owner-facing paid-but-lapsed inbox (follow-ups filed). **SEO tier 2 shipped** (#87/#122, ADR-0019, dispatch-workflow independent-reviewed): link-preview crawlers (WhatsApp/`facebookexternalhit`/…, never Googlebot - it renders JS) get real per-property OG tags via a NARROW Caddy UA-allowlist → a static, HTML-escaped OG stub route (`GET /public/properties/:slug/og`) that reuses the JSON page's `getBySlug` (archived→404, malformed→404) and shares `buildPropertyOgTags` (`packages/shared`) with the SPA's `<meta>` tags, so crawler card and rendered page can't drift; a `<meta http-equiv=refresh>` bounces a human to `/p/:slug`. **M3's confirmation page shipped** (#54/#123, ADR-0020, dispatch-workflow, independent-reviewed + one fix round): `GET /public/bookings/:id` reconciles ON READ through the IDENTICAL idempotent webhook transaction (`PaymentWebhookService.reconcile` pulls a `PaymentGateway.fetchStatus` through the same status-guarded confirm - a lost webhook still confirms on page load, the `payment_event` unique constraint no-ops the rest, so the FR-NOTIF-1 email fires once-per-confirmation by construction), a best-effort exactly-once email behind a `Mailer` port (`LogMailer` no-op - invariant #8; real Resend/SMTP = one rebind, #119), the guest's `wa.me` deeplink, and country-aware guest-phone capture (an international field defaulting to ID → E.164 storage → `wa.me` correct for ANY country; `normalizeWaPhone` requires canonical E.164 so a lenient walk-in phone OMITS the button rather than emit a broken link; `libphonenumber-js` `/min` confined to the web funnel bundle). Both merged via the dispatch workflow; the ADR-0019 collision (both lanes picked 0019) was resolved by renumbering #54's ADR to 0020 on merge. Review follow-ups filed: #124 (per-country server-side phone validation), #125 (funnel code-split + lazy `libphonenumber`), #126 (email per-recipient isolation), #127 (OG-stub LINE-UA + canonical-origin hardening). **M3 confirmation email shipped** (#119/#129, ADR-0021, dispatch-workflow independent-reviewed): the real `ResendMailer` over native `fetch` (no SDK, no new dep) behind #54's existing `Mailer` port, selected by an env-gated factory that falls back to `LogMailer` when unconfigured (so no test or unconfigured prod touches a live provider); real creds (`RESEND_API_KEY` + `MAIL_FROM`) are a deploy step. Folded in **#126** (per-recipient send isolation - each recipient's `send` in its own try/catch so a guest-address bounce no longer silently drops the owner's booking email; closed by #129). **Paid-but-lapsed owner inbox shipped** (#120/#130, ADR-0022, migration 0011, dispatch-workflow independent-reviewed with an RLS-only cross-tenant probe): `GET /payments/lapsed` (tenant-scoped) surfaces `paid` payments whose booking is `expired`/`cancelled`; `POST /payments/:id/handle` sets ONLY a nullable `payment.handled_at` marker - the row leaves the inbox because the predicate excludes it, never by mutating the ledger (ADR-0002). #119/#120 shipped in parallel via the dispatch workflow, ADR numbers pre-assigned (0021/0022) so no renumber. **Two more #54-review follow-ups (#124/#132, #125/#133) shipped in parallel via dispatch, each independent-reviewed (PASS), with the review's own non-blocking recommendations applied before merge**: **#124 - a per-country phone validity gate** (no ADR): a server-only `.refine(isValidPhoneNumber)` in `apps/api` (`public-booking.schema.ts`) layered over the shared E.164 *shape* schema, so a request crafted to bypass the SPA can no longer store a shape-valid-but-implausible number; `libphonenumber-js` stays confined to `apps/api` (never `packages/shared` or the web bundle - AC-proven). **#125 - the funnel and dashboard are two bundles** (ADR-0023): every route component loads via `lazyRouteComponent` (route tree/`validateSearch`/`beforeLoad` stay static, only leaf components defer), and `libphonenumber-js` is a dynamic import at the checkout phone step; property-page initial JS drops ~234 → ~161 KB gzipped with no dashboard/libphonenumber, verified from the Vite manifest. The `scripts/check-bundle.mjs` structural guard is **wired into the web `build`** so a re-merge fails the build (no cloud CI; deploy always builds), and a **Retry** affordance covers the lazy phone chunk failing to fetch mid-funnel. **SEO OG-stub hardening shipped** (#127/#135, dispatch-workflow independent-reviewed): the LINE crawler token corrected (`Line/` matched the LINE in-app browser - a human; now `line-poker`, LINE's real link-preview scraper, asserted against the committed Caddyfile regex so it can't drift) and the OG `og:url`/canonical now derives from the trusted `WEB_BASE_URL` origin instead of the spoofable request `Host`/proto, with a production boot-time fail-fast (`validateEnv` in `main.ts`) when it is unset (the review's own non-blocking recommendation, applied before merge). **M5 i18n shipped** (#58/#136, ADR-0024, dispatch-workflow independent-reviewed): the public funnel speaks EN/ID/ZH with **no i18n library** (invariant #8) - a dependency-free typed catalog + a `useSyncExternalStore` locale store + platform `Intl`; a language switcher persisted per visitor in `localStorage` (the token rule unaffected - tokens stay in memory + httpOnly cookie), locale-formatted display while the wire stays `YYYY-MM-DD` (proven by asserting the `POST /public/bookings` body), availability blocked-reasons localized client-side from the slug-on-the-wire contract (api-spec §5.1, no server change), and a two-layer untranslated-key guard (compile-time `: Messages` typing + a runtime parity test) wired into typecheck/build/test so copy cannot ship EN-only. #127 and #58 shipped in parallel via dispatch, disjoint (`area:property` vs `area:i18n`) so no ADR/decision-log collision; each lane's review-raised non-blocking recommendations (gallery `alt` i18n + an ID end-to-end booking test on #58; the boot-time `WEB_BASE_URL` guard on #127) were applied before merge. Next: M4 - the iCal IMPORT pipeline + conflict inbox (#38, boss fight #3); M5 polish (#60 demo - blocked on #38) + the R2 cutover checklist (#68, manual prod-ops) still open.
- **Design system decided** (2026-07-18): "the gracious host" - stone neutrals + terracotta, Plus Jakarta Sans + Fraunces, lowercase `sambung` wordmark, two-surface component doctrine. Source of truth: [docs/design-system.md](docs/design-system.md) + ADR-0007. Foundation **shipped** in #91/#99; all M2 UI now builds on the semantic tokens (the calendar #49 is the first).
- **SEO tier 2 shipped** (2026-07-20, #87/#122, ADR-0019): link-preview crawlers (WhatsApp/`facebookexternalhit`/… - this product's actual channel) now get real per-property OG tags via a narrow Caddy UA-allowlist → a static OG stub route sharing `buildPropertyOgTags` with the SPA `<meta>` tags. Live Facebook/WhatsApp preview verification is deferred to demo time (#127).
- **Repo**: `RacThug/sambung` (private).
- **Tracking**: GitHub **Issues + Milestones** (M0–M5).
- **Key documents** (read the relevant one before touching that area):
  - [`docs/prd.md`](docs/prd.md) — product source of truth (what/why, acceptance criteria).
  - [`docs/db-design.md`](docs/db-design.md) — schema, constraints, integrity rules (teaching edition).
  - [`docs/architecture.md`](docs/architecture.md) — FE/BE split, modules, data flows (teaching edition).
  - [`docs/README.md`](docs/README.md) — doc index.
