# CLAUDE.md — Sambung operating contract

> Keep this file lean. It's read every session and consumes context.
> The operating contract on top is the standing law; project facts live at the bottom.
> The companion design docs are the source of truth — read the relevant one before implementing:
> [`docs/prd.md`](docs/prd.md) · [`docs/db-design.md`](docs/db-design.md) · [`docs/architecture.md`](docs/architecture.md)

---

## Context

- **Project**: Sambung (Indonesian: *to connect*) — a multi-tenant **direct-booking engine** + lightweight **channel manager** for Bali accommodation owners. Commission-free direct bookings while OTA calendars stay in sync via iCal.
- **Type**: Portfolio **and** learning project (RacThug's own — not client work).
- **Two consequences that shape everything:**
  - **Portfolio** → code quality, clear decisions, and a demoable result matter more than feature count.
  - **Learning** → the owner is here to sharpen engineering. Your job is to **teach while building**, not to silently produce code.

---

## Who's Who

- **RacThug ("the owner")** = CEO / Product Owner. Owns WHAT & WHY. Also the **student** — wants to understand the *how*, not just receive it.
- **You (Claude Code)** = senior pair-programmer and teacher. Own HOW. Propose, explain, recommend, execute, review — but never a code vending machine.

---

## Operating mode — EXPLAIN MODE (the most important rule)

This is where Sambung differs from a normal delivery project: **the explanation is part of the deliverable.**

**For every non-trivial change:**
1. State your approach in 2–4 sentences **before** writing code.
2. Name 1–2 alternatives and the trade-off you're making. Say *why* this one.
3. When you use a pattern or concept, teach it briefly (the *why*, not just the *what*).
4. After implementing, summarize what to look at and what to verify.

**Two task types — treat them differently:**
- **Scaffold tasks** (CRUD, boilerplate, config, wiring): just do them, then summarize. Don't over-explain plumbing.
- **Boss-fight tasks** (see below): explain deeply *first*. Default to letting the owner attempt the core logic; you guide and review. If asked to implement, narrate the reasoning as you go. **Never silently auto-implement a boss fight.**

When unsure which type, ask. Bias toward explaining the *thinking*, not lecturing syntax.

---

## Operating Principles

1. **Teach, don't just ship.** EXPLAIN MODE above overrides the usual "one recommendation, no menu" — here the trade-offs *are* the value. Still: give a clear recommendation, don't dump a neutral menu.
2. **Plan before code.** Non-trivial task → propose approach first.
3. **Senior mindset.** Think scalability, security, maintainability, edge cases — even when not asked.
4. **Challenge bad requirements.** If something is technically unsound or over-engineered for the goal, say so before building.
5. **Clarify, don't assume.** Ambiguous → ask one sharp question.
6. **Right-size the solution.** A portfolio MVP ≠ a corporate deliverable. Match effort to stage.
7. **Translate to business terms when it matters.** Cost (time/effort), risk, user impact — no raw jargon at decision points.

---

## Modes (which "hat" to wear)

Operate at a **senior/staff level** in every mode. Default to whatever the task needs; switch automatically. The owner can also say "as [mode], …".

- **Architect** → stack, system design, build-vs-buy, scaling decisions
- **Engineer** → backend (API, DB, logic), frontend (UI, state), tests, deployment
- **Designer** → layout, user flow, usability
- **Product/BA** → break features into issues, write acceptance criteria, spot scope gaps
- **Writer** → docs, copy — adapt formality to audience

---

## Non-negotiable invariants

Violating any of these is a bug **even if tests pass**:

1. **The frontend never touches the database.** All data goes through the NestJS API. `packages/web` must not import `packages/db`.
2. **Every tenant-owned query is scoped by `tenant_id`.** No exceptions.
3. **Availability is derived from `booking` rows — never a separate table.** (DB doc §4.1)
4. **Dates are half-open `daterange` `[check_in, check_out)`.** (DB doc §4.2)
5. **The exclusion constraint is the real overlap guard.** App checks are for UX, not correctness. (DB doc §4.3)
6. **Money is integer rupiah (`bigint`), never float.**
7. **Integration points are idempotent** — iCal imports by `external_uid`, webhooks by `payment_event`.
8. **No paid third-party services.** iCal (free), Midtrans/Xendit sandbox. The one allowed recurring cost is a single cheap VPS (~$5/mo) hosting web + api + db. Flag anything else that would cost money.

---

## Stack & conventions

- **FE:** Vite + React + TypeScript + Tailwind. TanStack Router (typed routes, zod-validated search params). TanStack Query for server state (not Redux). i18n EN/ID/中文. Design system: [docs/design-system.md](docs/design-system.md) - pages speak semantic tokens only; shadcn rethemed (dashboard) / custom-on-headless (public funnel), ADR-0007.
- **BE:** NestJS + TypeScript. Drizzle (drizzle-orm + drizzle-kit + pg; exclusion constraint + RLS live as hand-written SQL in the migration - drift-safe, kit diffs snapshots not the DB). `@nestjs/schedule` for cron.
- **DB:** PostgreSQL 14+.
- **Storage:** S3-compatible - Garage (dev, docker compose) / Cloudflare R2 free tier (prod). Photo uploads via presigned PUT URLs.
- **Mono:** pnpm workspaces + Turborepo.
- **Deploy:** single VPS - Caddy (auto-TLS, serves the SPA, proxies `/api`) + Docker Compose (api + Postgres). Same origin, so the refresh cookie stays first-party. (Architecture doc §7.)
- **Layering (BE):** controller (HTTP only) → service (logic, transactions) → repository (Drizzle). Thin controllers, fat services, dumb repositories.
- **Shared contract:** request/response types + zod schemas live in `packages/shared`; both sides import them.
- **Auth:** access token in memory + `Authorization: Bearer`; refresh token in httpOnly Secure cookie. Never `localStorage`.
- **Naming:** tables/columns `snake_case`; TS `camelCase`; types/components `PascalCase`. Files `kebab-case`.
- **Validation:** validate all external input (HTTP body, webhook payload, iCal feed) at the boundary with zod.
- **Language:** code, config, and comments in English. Bahasa Indonesia / 中文 only for user-facing copy (i18n).

---

## The boss fights (explain first, don't auto-build)

| # | What | Lives in |
|---|---|---|
| 1 | Race condition / double-booking (TXN + exclusion constraint + hold sweeper) | `booking` |
| 2 | Availability interval logic | `booking` |
| 3 | iCal sync reliability + reconciliation | `channel-sync` |
| 4 | Idempotent payment webhook | `payment` |
| 5 | Multi-tenant isolation (guard + interceptor + RLS) | `common` + all modules |

For these: walk through the design, surface the edge cases, then let the owner drive unless told otherwise.

---

## Workflow Loop

1. Owner points to a GitHub issue → "do #N". **Tasks = GitHub Issues**, one per requirement, labeled by milestone (M0–M5).
2. You read the issue + relevant code + the relevant design doc + this file.
3. Plan (if non-trivial) → owner approves.
4. Execute → self-review → run/verify.
5. For boss fights / risky work → independent review (see Two-Session Review) before merge.
6. Report, update the issue/milestone, log any architecture decision below.

- **Commits:** imperative, scoped: `feat(booking): add hold expiry sweeper`. Small and frequent.
- **Definition of done:** acceptance criteria met + tests for the logic + invariants upheld + a one-paragraph "what I did and why" in the PR.

---

## Git workflow (HARD RULE)

**Never commit or push to `main` directly. Ever.** All work flows: branch → push → Pull Request → merge to `main`. Model = **GitHub Flow** (`main` is always deployable; no `develop`).

1. Branch off `main`: `git switch -c m0/monorepo-setup` (name: `m<milestone>/<short-task>`, e.g. `m2/booking-availability`).
2. Commit on the branch; push with `git push -u origin <branch>`.
3. Open a PR into `main` (`gh pr create`), referencing the issue #.
4. Merge the PR (squash). Delete the branch. `git switch main && git pull`.

- **Enforcement:** a `pre-push` hook (`.githooks/pre-push`) blocks direct pushes to `main`/`develop`. After cloning, enable it once: `git config core.hooksPath .githooks`. (Server-side branch protection needs a public repo or GitHub Pro — skipped per invariant #8; the local hook is the free guard.)
- **Emergency bypass** only, and say so out loud: `git push --no-verify`.

---

## Two-Session Review protocol

For boss-fight / risky work, split implementation from review across two separate sessions — fresh eyes catch what self-review misses.

- **Use it for:** the 5 boss fights, migrations, anything touching tenant data or payments.
- **Skip it for:** typos, copy tweaks, trivial scaffold. (Double review = double token cost.)

**Rules that make it real (not theatre):**
1. **Session 1 builds** the code + a short rationale.
2. **Session 2 reviews** as a fresh subagent with **no access to Session 1's reasoning** — independence is the whole point. It gets the **issue + acceptance criteria** as ground truth, checks the diff against those *and* the invariants above, and **runs it** (checkout, tests, exercise the feature) rather than only reading.
3. Reviewer is **skeptical by default** — its job is to find the missing edge case ("does a duplicate webhook double-confirm?", "can tenant A read tenant B?"), not to approve.
4. Fix loop: reviewer finds issue → back to Session 1 → re-review → merge only when clean. **Don't merge a boss fight on one session's say-so.**

**How to run it** - there is no dedicated command; this is a protocol, not a skill. Pick a mechanism, in decreasing strictness:
- **A fresh Claude Code session** - paste the prompt below + the issue + branch name. Truest clean-room, zero shared context. The gold standard rule 2 describes.
- **`/code-review`** - spawns Standards + Spec reviewers as subagents that see only their prompt, not the builder's reasoning. Convenient and nearly as independent; run it from the builder's session.
- **`/code-review ultra`** - multi-agent **cloud** review of the branch. User-triggered and billed; the agent cannot launch it (offer it, don't attempt it).
- **An independent reviewer subagent** - fresh context fed only the issue + ACs, launchable mid-session; same independence as `/code-review`.

Feed whichever you pick the **issue + acceptance criteria** as ground truth, and **never** the builder's rationale - that hand-off is the independence.

**Reviewer prompt template:**
> You are a skeptical Staff QA Engineer. Assume this PR has bugs — find them, don't approve it.
> Issue + acceptance criteria: [paste]. Branch: [name].
> Checkout, run the tests, exercise the feature. Then report: (1) does it meet every criterion — cite what you ran; (2) bugs / missed edge cases; (3) security or tenant-isolation risks; (4) verdict: PASS, or exactly what must change.

**Limit:** both sessions are the same model — great at implementation errors, blind to *shared* blind spots (e.g. a misunderstanding of the spec). Domain truth still needs the human.

---

## Safety & Verification

- **Make it verifiable, not "trust me".** When reporting done, give a concrete way to check it — steps + an edge case — not just "completed".
- **Git is the safety net.** Branch, never `main`. Small commits so anything can be reverted. Before risky changes (deletes, migrations, refactors), say what could break and how to roll back.
- **Secrets never leak.** `.env` and credentials stay in `.gitignore` — never hardcoded, never committed.
- **Trust no external input.** Validate every HTTP body, webhook payload, and iCal feed at the boundary with zod.

---

## Commands

```
pnpm install
docker compose up -d                         # local Postgres (needed for migrate/seed/db tests)
pnpm dev                                      # turbo: web + api
pnpm lint && pnpm typecheck                  # whole workspace
pnpm test                                    # turbo: web (vitest) + api (jest) + db (vitest)
pnpm test:e2e                                # Playwright (apps/e2e); needs docker up + `playwright install`; NOT in `pnpm test`
pnpm --filter @sambung/db db:generate        # diff schema.ts -> new SQL migration
pnpm --filter @sambung/db db:migrate         # apply pending migrations
pnpm --filter @sambung/db db:seed            # 2 tenants, 3 properties, sample bookings (idempotent)
pnpm --filter @sambung/db db:studio          # browse data
```

---

## Guardrails — do NOT

- Add an `availability` table (invariant #3).
- Use floats for money, or store cents — IDR is integer rupiah.
- Put tokens in `localStorage`.
- Let the SPA call the DB or skip the API.
- Reach for Redux/global store for server state.
- Add a heavy dependency or a paid service without flagging it first.
- Implement a boss fight without explaining it first (EXPLAIN MODE).
- Trust external input (HTTP, webhook, iCal) without validating it.
- **Commit or push to `main` directly — always branch + PR (see Git workflow).**

---

## Agent skills

### Issue tracker

Issues live in the `RacThug/sambung` GitHub repo (GitHub Issues + Milestones M0–M5), via the `gh` CLI. External PRs are **not** a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

---

## Architecture Decision Log

| Date | Decision | Why | Approved |
|------|----------|-----|----------|
| 2026-06-24 | Repo = **private** GitHub repo `RacThug/sambung` | Portfolio project; go public when demo-ready | Yes (owner) |
| 2026-06-24 | Docs live in `docs/` (`prd.md`, `db-design.md`, `architecture.md`) + index | AI- and human-navigable; one source of truth per subsystem | Yes (owner) |
| 2026-06-24 | Git = **GitHub Flow**; never push to `main`, always branch + PR. Enforced by a local `pre-push` hook (`.githooks/`) | Server-side branch protection needs public repo / Pro (invariant #8). Local hook blocks accidental direct pushes, free, keeps repo private | Yes (owner) |
| 2026-07-16 | DB = **PostgreSQL**, reaffirmed vs MySQL | Exclusion constraint + RLS are load-bearing (boss fights #1, #5) and already merged; MySQL has neither. Learning the Postgres delta is the point | Yes (owner) |
| 2026-07-16 | Routing = **TanStack Router**, replacing React Router | Typed routes + zod-validated search params fit the URL-driven booking funnel; pairs with TanStack Query. Swap cost near zero (2 files) | Yes (owner) |
| 2026-07-16 | Deploy = **single VPS** (Caddy + Docker Compose: SPA + api + Postgres); amends invariant #8 | Cron needs an always-on process (free tiers sleep); one origin kills cross-site cookie/CORS complexity; ops skills = portfolio value | Yes (owner) |
| 2026-07-16 | iCal conflict policy: per-VEVENT savepoints, `sync_conflict` inbox, never auto-cancel a confirmed booking, reconcile only on healthy parse (#38) | The exclusion constraint rejects real-world overbookings; a human must pick the loser; a truncated feed must never mass-cancel | Yes (owner) |
| 2026-07-16 | Photos = S3-compatible storage: MinIO (dev) + Cloudflare R2 free tier (prod), presigned PUT uploads (#39) | Dev/prod parity via one client; R2 is forever-free with zero egress; card-on-file caveat flagged | Yes (owner) |
| 2026-07-16 | Composite FKs enforce the `tenant_id` denormalization (property→unit→booking/channel_connection) (#40) | Wrong tenant_id under RLS = silent cross-tenant leak; make it unrepresentable | Yes (owner) |
| 2026-07-16 | ORM = **Drizzle** (drizzle-orm + drizzle-kit + pg), replacing Prisma; migrations re-baselined (#41) | Owner preference + SQL-first fit: composite FKs modeled natively; hand-written SQL is drift-immune (kit diffs snapshots, not the DB); no query-engine binary | Yes (owner) |
| 2026-07-16 | Dev/fallback object storage = **Garage**, replacing MinIO in the photos decision (#39); prod stays R2 | MinIO community edition retired upstream (archived Apr 2026, source-only, no patches); Garage is actively maintained, lightweight, built for small self-hosted. SeaweedFS = documented plan B | Yes (owner) |
| 2026-07-16 | **Dev-fixture credentials may be committed** (Garage keypair + rpc_secret in compose/`garage.toml`/`.env.example`, like the Postgres `sambung/sambung`); "secrets never leak" reads as credentials that guard something real. Prod secrets live only in the VPS env, never in the repo | Fresh clone must work with `.env.example` values (#39 AC); localhost-only, loudly annotated. Real risk is pattern drift, mitigated by comments + gitignored `.env` | Yes (owner) |
| 2026-07-17 | **Transaction seam = the service, not the repository.** `TenantDbService.run` joins an open transaction (dedicated `AsyncLocalStorage`) instead of always opening one; services own the unit of work (#72) | Per-method transactions made composition impossible, so rules migrated *into* the repository (`deleteWithGuard`) and `auth.service` reached past the seam to get a real transaction. api-spec §5.3 needs check-then-insert in one txn; without this, boss fights #1+#2 land in the layer architecture §8 assigns to the service. CLS = who asks / ALS = which txn, because CLS is request-scoped and the M2 sweeper has no request. Flat join, not savepoints: `savepoint()` waits for M4's real caller (#38) | Yes (owner) |
| 2026-07-17 | **One module owns the tenant principal.** `Principal` + the CLS key live with `TenantContext`; `TenantDbService` asks it instead of reading CLS; `run` throws with no principal; repositories drop the `tenantId` param but keep every `WHERE` (#76) | The owner module was bypassed by its most important reader, so a key rename broke RLS scoping to silent zero rows. The param was a second path for an id the implementation already reached - now both read one source and cannot disagree. The `WHERE` stays because it guards RLS *not being in force* (boot on `DATABASE_URL` → owner role → no policies), which is one env var; a test now proves each layer holds alone. Throwing beats "fail-closed" because that claim is false on a warm connection (#74). JWT's `sub` stays in `AccessPayload` - the domain doesn't learn the token's vocabulary | Yes (owner) |
| 2026-07-17 | **RLS policies use `nullif(current_setting('app.tenant_id', true), '')::uuid`**; all 9 rewritten by migration `0002` (#74) | "Unset GUC → NULL → fail-closed" was false: `set_config(..., true)` reverts at COMMIT to the *reset* value, which for a custom GUC already set once is `''`, not NULL. So a no-tenant query returned zero rows on a cold connection but errored 22P02 on a warm one - the second layer of defense-in-depth didn't behave as documented, and which failure you got depended on the pool. `nullif` maps both unset and reset to NULL; the comparison stays `uuid = uuid`, so the tenant indexes survive (a `::text` compare would fail closed too, at the cost of a seq scan per query). `0000_init.sql` left as applied history - migrations are append-only, and `0002` supersedes its comment | Yes (owner) |
| 2026-07-17 | **Constraint violations get one map: constraint NAME → the response it means**, applied by a global interceptor; services no longer catch (#80) | The DB names the domain fact - `booking_no_overlap` IS "those dates are taken" - so mapping the name once is what makes api-spec §5.3's "the client cannot tell which layer refused" true by construction rather than by two copies of a string. Keyed on name, not SQLSTATE: 23505 means "some unique thing exists", which is not an answer; the three `is*Violation` predicates are deleted, having had 0 real callers. Unmapped → 500, loudly: a constraint nobody considered is a bug, not a 409. The interceptor TRANSLATES (rethrows an HttpException) rather than renders, so Nest's exception layer is untouched; a service that wants its own outcome (M3's webhook: duplicate `payment_event` → 200) just catches first, outside the transaction (25P02) | Yes (owner) |
| 2026-07-17 | **A Unit is one sellable thing, not a room type with a quantity** ([ADR-0001](docs/adr/0001-unit-is-one-sellable-thing.md), #45). PRD FR-PROP-2's "units/room types" is corrected; 3 identical rooms = 3 rows | Quantity inventory can't be guarded by the exclusion constraint - you'd count overlaps and compare, which is the read-then-write race boss fight #1 exists to make unrepresentable. Cost: 8 rooms = 8 Units + 8 OTA calendars, so bulk entry drives the UI and `unique(property_id, name)` stops the owner wiring Airbnb's feed into the wrong "Garden Room" | Yes (owner) |
| 2026-07-17 | **Deleting inventory never destroys the ledger** ([ADR-0002](docs/adr/0002-deleting-inventory-never-destroys-the-ledger.md), #45). Guard blocks on *any* booking ever, not just future occupying; both `booking→unit` FKs → `no action`. Supersedes api-spec §4.4 | The guard protected the calendar, not the ledger: `DELETE /properties/:id` returned 204 while cascading away years of past bookings and their `payment` rows. Cancelling is a domain event; deleting is amnesia. Under CASCADE the DB was an accomplice, inverting invariant #5 (app = UX, DB = correctness). Free at M1 (zero bookings exist), a live-data change after M2. **Both** FKs or the survivor cascades first and the guard silently does nothing. `no action` vs `restrict` is only a tie-break (default + defer-able later) - the "restrict would break tenant deletion" rationale was folklore, disproven by measurement in review; the load-bearing change is cascade → not-cascade. Makes archive an M2 blocker (#84) | Yes (owner) |

| 2026-07-17 | **A Visitor is a principal, scoped by the slug they opened** ([ADR-0003](docs/adr/0003-a-visitor-is-a-principal.md), #77/#46). `PublicScope.enterFromSlug` resolves one column on the owner connection, then seeds `{ kind: 'visitor', tenantId }`; everything after runs under RLS unchanged. `Principal` becomes a union | The funnel's entry is cross-tenant - you can't scope by tenant before finding the property, because the property tells you the tenant. #77's `runAsTenant(tenantId, fn)` would put a tenant id back on a parameter list, on the class that IS boss fight #5, one issue after #76 removed it; any caller could then name any tenant, whereas a resolved Visitor is confined to the tenant whose URL the guest already typed. The union makes `principal.role` a compile error until the Visitor case is handled. The unscoped statement reads one column keyed by a deliberately-public value, so a future `payout_account` can't leak through it. `sambung_public` role = documented upgrade path | Yes (owner) |
| 2026-07-17 | **A property's public URL is an address, not a view of its state** ([ADR-0004](docs/adr/0004-a-public-url-is-an-address-not-a-view-of-state.md), #46). Slug minted once at create, never moved by a rename; `publishable` never gates the public page | One principle, two applications: a live URL must not die as a side effect of an edit nobody thought was about the URL. The product IS a link pasted into an OTA profile or forwarded on WhatsApp - a name-tracking slug breaks every one silently, and a `publishable` gate does the same when someone deletes a photo. `publishable` was never a gate anywhere it's used (page-spec §4.4/§4.5 = a checklist); api-spec §4.7 specifies one failure, unknown slug. Slug uniqueness is minted by `ON CONFLICT (slug) DO NOTHING` + random suffix, never a 409: the owner typed a name, and telling them it's taken is a cross-tenant existence oracle. Redirects stay additive; "follows the name" would not have | Yes (owner) |
| 2026-07-18 | **Archived inventory is derived up the hierarchy, not cascaded** ([ADR-0005](docs/adr/0005-archived-is-derived-not-cascaded.md), #84). `archived_at` (nullable) on `unit`+`property`; effective-archived = a Unit's own flag OR its Property's. Archive/unarchive = idempotent POST verb-subresources | ADR-0002 made a booked Unit undeletable *and* permanently public; archive is the retirement verb it deferred. Derive, not cascade, because of unarchive: one `archived_at` per row can't tell self-archived from parent-archived, so cascading either resurrects a one-off retirement on the property's unarchive or needs a second marker - deriving makes the round-trip correct by construction, matching the codebase's derive-don't-store grain (availability, `verified`, `publishable`). The guard against a forgotten `WHERE` is the booking chokepoint (§5.3 availability re-validation), not a partial index (a perf tool, not a correctness guard) or a view (fights Drizzle/RLS, and the owner must still see history). No RLS policy (intra-tenant visibility ≠ cross-tenant isolation); no lock (single-row flag write, in-flight bookings honoured) | Yes (owner) |
| 2026-07-18 | **An archived Property is retired, not just incomplete** ([ADR-0006](docs/adr/0006-an-archived-property-is-retired-not-addressed.md), #84). Archived Property → public page `404` (slug stays reserved); an archived Unit merely drops out of the unit list | Reconciles ADR-0004's "the page always renders". The axis is intent: `publishable` is an incomplete checklist (never a gate - the *surprising* death ADR-0004 forbids); archive is a *deliberate* take-down (a gate, on purpose). `404` not `410`/tombstone - matches the "hidden = 404, never an existence oracle" convention (api-spec §1). Enforced in `findPublicBySlug`, not `enterFromSlug`, which stays a pure tenant-resolver so §5.3 can resolve-then-`409` an archived Unit. The slug row persists → unarchive restores the exact URL, so ADR-0004's permanent-address guarantee holds | Yes (owner) |
| 2026-07-18 | **Design system: one brand, two surfaces** ([ADR-0007](docs/adr/0007-two-surface-design-system.md), [design-system.md](docs/design-system.md), #91). Brand = "the gracious host": stone neutrals + terracotta accent, Plus Jakarta Sans + Fraunces (system CJK fallback for ZH), lowercase `sambung` wordmark; name stays Sambung, closing PRD §10.4. System: dashboard = shadcn/ui rethemed (copy-in, `components/ui/`); public funnel = custom components on headless behavior (react-day-picker, Radix - no shadcn imports in `public-booking`); pages speak semantic tokens only, ramps live only in `packages/config/tailwind.css`; the 7 existing pages migrate in one sweep before M2 UI | The funnel is where a stranger decides to pay: hand-design where guests look, dependable boring components where owners work; copy-in = owned a11y without the templated skin (never hand-write calendar ARIA); semantic-only pages = derive-don't-store applied to CSS, so retheming is a token swap; sweeping at 7 pages is the cheapest it will ever be | Yes (owner) |
| 2026-07-18 | **A public resolver resolves, it does not judge** ([ADR-0008](docs/adr/0008-a-public-resolver-resolves-it-does-not-judge.md), #47). `PublicScope.enterFromUnitId` (the second public entry, for the `GET availability` quote) resolves the tenant for any *existing* unit and never checks archived; the archived judgement is made downstream at the chokepoint - #47 read → `404`, #48 write → `409`. Broadens ADR-0003 (a Visitor is scoped by the public identifier they opened - slug *or* unit id) | A resolver that 404'd archived at the door makes #48's specified resolve-then-`409` impossible and splits one cross-tenant entry point into two that drift; keeping it pure (one column, a public key, judges nothing) is what a reviewer greps and what stops a future `payout_account` leaking through it | Yes (owner) |
| 2026-07-18 | **A hold is cleared by a sweep - at two scopes** ([ADR-0009](docs/adr/0009-a-hold-is-cleared-by-a-sweep-at-two-scopes.md), #48). Pessimistic 15-min hold; a lapsed hold is flipped to `expired` by (1) an **opportunistic intra-tenant** `UPDATE` inside the booking txn (Visitor's RLS scope), run before the re-check, and (2) a **5-min cross-tenant cron** on the owner connection as backstop. Both idempotent (WHERE matches only past-TTL holds) | The exclusion predicate is immutable, so it can't check `now()` - a sweep is unavoidable. Cron-only leaves a ≤5-min false-block at the funnel's decision moment; the opportunistic sweep makes freed dates bookable *there*. The two scopes are forced by isolation (intra-tenant fits the Visitor's scope with no special connection; all-tenants *needs* the owner connection) and each explains why one sweep can't be both. Single VPS = one process = no distributed lock; read stays ≤5-min pessimistic on purpose (a GET must not write) | Yes (owner) |
| 2026-07-18 | **The Calendar is composed, not served** ([ADR-0010](docs/adr/0010-the-calendar-is-composed-not-served.md), #49). The owner's unified calendar is assembled client-side from neutral endpoints - one shared `GET /bookings` (full rows, **repeatable** `status`, the caller names the two occupying statuses via a shared `OCCUPYING_STATUSES` constant), a flat `GET /units` (effective-archived flag, ADR-0005), `GET /properties` - not a bespoke `GET /calendar` aggregate. Disclosure follows the audience: the owner read returns whole rows, the public availability read (#47) clips for privacy - same overlap-window filter, opposite rule | A `GET /calendar` would be a second booking-read path that drifts from #51's; neutral primitives serve #49/#50/#51 while a per-page tree serves one. The exclusion constraint makes each Unit row non-overlapping, so there is no server-side layout to centralize; occupancy is *derived* by joining bookings onto Units in the view, the same grain as invariant #3's derived availability | Yes (owner) |

| 2026-07-19 | **The owner is an authority, not a customer** ([ADR-0011](docs/adr/0011-the-owner-is-an-authority-not-a-customer.md), #50). `POST /bookings` (manual block / walk-in) reuses the guest funnel's *one* overlap chokepoint - the opportunistic in-txn hold-sweep + `quote()`'s re-check + `booking_no_overlap` → the **identical** `409 {reasons:['overlap']}` - but is born `confirmed` with no hold, authenticates (owner RLS connection, **no** `enterFromUnitId`; cross-tenant/unknown unit → 404), and enforces **only** the physical invariant (overlap), **not** the guest-protection policy checks (min_stay, max_guests). A walk-in is `source=direct` (entry-method derived, not a new enum); a block is `manual_block` with `total_price_idr=NULL`; an archived Unit → `409 {reasons:['archived']}`. Cancel (`POST /bookings/:id/cancel`) is an atomic FSM-guarded UPDATE and the universal free-the-dates verb (holds, walk-ins, blocks); `refund:"manual"` iff a paid payment exists (always `"none"` at M2). Detail read is net-new `GET /bookings/:id` (owner full disclosure - phone/email - 404-over-403) | Overlap is physics that binds everyone, so the read-can't-disagree-with-write authority (one `quote()`) must hold for the owner too; a second owner-only check would be a second definition of "taken" that drifts from #48's. min_stay/max_guests are the owner's own rules - forcing them on hand-entry would make the tool refuse to record reality (a 1-night maintenance block, an over-capacity walk-in taken by phone). `source` = channel not entry-method: a walk-in *is* direct business, and M3's "no provider session" is one derivation away (confirmed + direct + no payment row). Corrects the stale §5.4 `guestContact?` body (pre-migration-0007) | Yes (owner) |

| 2026-07-19 | **A 409 carries a code, not a sentence** ([ADR-0012](docs/adr/0012-a-409-carries-a-code-not-a-sentence.md), #82, deferred out of #80). Every 409 - whichever layer refuses - has body `{ statusCode, error, code, message, ...detail }`: `code` is a slug from a closed `conflictCodeSchema` in `packages/shared` imported by both sides (rename = compile error); typed detail rides beside it (delete guard `count`, booking `reasons`, cancel `status`); `message` stays a human default the web NEVER renders. The web owns all copy (`describeConflict`, an exhaustive switch over the shared union). Chose a dedicated `code` over slug-in-`message` (§8.2's literal wording, now refined): the two already-structured errors (`datesUnavailable`/`bookingNotCancellable`) put the machine field beside a human `message`, so `code` generalizes that instead of overwriting the prose; keeps machine-identity and human-default as separate concerns. Delete guards moved inline→factories (`propertyHasBookings`/`unitHasBookings`); `ApiError` gained `body`; `apps/api` pins every factory to `conflictBodySchema` (like §8.6's pgEnum pin); §5.3's both-layers-indistinguishable test still holds (same factory → byte-identical bodies) | "Two conventions split by which layer refused" is what §5.3 forbids; prose-only 409s also blocked i18n (server prose can't be translated client-side) and welded the delete count into an unreadable English sentence. One slug set, one body shape, copy on the client = the contract §8.2 promised, made whole | Yes (owner) |
| 2026-07-19 | **Rate limits are tiered, and a 429 follows the envelope** ([ADR-0014](docs/adr/0014-rate-limits-are-tiered-and-429-follows-the-envelope.md), #59). Two named throttlers: the generous `default` on every route (#48) + a TIGHTER `sensitive` (env-driven, protective 10/60s) gated to the abuse-prone few - `login`/`register`/`POST /public/bookings` - by a greppable `@ThrottleSensitive()` marker + the throttler's `skipIf` (a no-op everywhere else, so a legit owner browsing never trips it); own per-handler bucket each. An `EnvelopeThrottlerGuard` renders the 429 as `{ statusCode, error, message }` (NOT a conflict `code` - ADR-0012's enum is closed to domain 409s; a 429 is infra back-pressure) + a standard `Retry-After`. Both limits stay in the ConfigService factory (not a decorator constant) so they're env-off in test - the throttle spec drops one env var, compiles its own app, fires `limit+1` (no sleep) | A hard-wired tight limit breaks normal dashboard use AND 429s the concurrency suite; a decorator constant can't read ConfigService, so it can't be env-off in test. Marker+`skipIf` keeps the decision next to the route; envelope+`Retry-After` makes a 429 shape-identical to every other refusal, so the web branches on `statusCode`, not a special case | Yes (owner) |

| 2026-07-19 | **The picker advises, the server decides** ([ADR-0013](docs/adr/0013-the-picker-advises-the-server-decides.md), #93). The `/p/:slug` availability calendar (react-day-picker headless + Sambung components, ADR-0007) hard-disables **only** the past (`{before: browser-local-today}` + `startMonth`=this month, so no paging backwards); booked nights are a greyed **modifier**, never `disabled`, so a range spanning one is sent to `GET availability` and the quote card renders the server's `overlap` + the clipped booked nights (AC). Two query modes on one endpoint, one cache key `["availability", unitId, from, to]` (visible-month sweep greys; debounced ~300 ms selection quotes); selection state lives entirely in `?from&to&unit` (a shared link reproduces the quote view). Thin `/p/:slug/book` placeholder added so the Book CTA lands somewhere real (checkout form/payment is a later issue) | A client that disabled booked nights would be a second, drift-prone definition of "taken" contradicting the exclusion-constraint authority (invariants #3/#5, ADR-0009), and would forbid a valid checkout-on-a-changeover (half-open `[from,to)`, db-design §4.2); greying keeps one authority and makes "highlight the clipped nights" possible. Past is the one guard that is genuinely the UI's, keyed to the guest's local day | Yes (owner) |
| 2026-07-19 | **A payment session is one reused row, addressed by its own id** ([ADR-0015](docs/adr/0015-a-payment-session-is-one-reused-row.md), #52). `POST /public/bookings/:id/pay` opens a Midtrans Snap **sandbox** session for a live Hold: one transaction under the tenant `PublicScope.enterFromBookingId` (the third pure resolver, ADR-0008) resolves; the opportunistic hold-sweep (ADR-0009) runs first so "hold lapsed" and "wrong status" become ONE post-sweep read → `409 booking_not_payable {status}` (new closed-set code, ADR-0012); a `FOR UPDATE` on the booking serialises concurrent pays so a retry REUSES the booking's open (`pending`) payment row - **`payment.id` is the Provider `order_id`** (the key #53's webhook resolves by), the session `{token,redirectUrl}` lives in `raw_payload` (a Snap token is Provider vocabulary, not a typed column), and `amount_idr` is snapshotted. Amount = Deposit share, BigInt-floored (invariant #6): `property.deposit_pct smallint 1-100 default 100` (migration 0008), editable on the property edit page (api #10); `deposit` flag = amount < total. Provider behind an injected `PaymentGateway` port (MidtransGateway over `fetch`, no SDK; `FakePaymentGateway` bound in tests via `.overrideProvider` - AC #3). Web: the real checkout form (create → pay → redirect to Snap) + a thin `/booking/:id` landing (reconcile page is #54) | A pay endpoint must be idempotent (invariant #7): one open row + a booking-row lock makes "retry ≠ double-charge" true by construction, not luck. `payment.id` as `order_id` is globally-unique-for-free AND pre-wires #53's lookup; the session in `raw_payload` keeps the Provider-agnostic table from calcifying a Midtrans concept into a column (the §8.6 enum-drift smell in table form); the amount snapshot is the payment-row analogue of a booking snapshotting its price. A port tested by a fake is the only honest way "tests run without live Midtrans" holds - an env stub is a second path that can ship to prod | Yes (owner) |
| 2026-07-19 | **The .ics export feed is addressed - and authenticated - by its unit UUID** ([ADR-0016](docs/adr/0016-the-ics-export-feed-is-addressed-by-its-unit-uuid.md), #55). `GET /public/units/:id/calendar.ics` is unauthenticated (an OTA's subscriber has no token), so the unguessable unit UUID is BOTH the address and the capability: it resolves the tenant via `PublicScope.enterFromUnitId` (the pure resolver, ADR-0008) and reads confirmed bookings UNDER RLS, so the feed is structurally incapable of becoming a cross-tenant read path (invariant #2). Deliberately **archive-blind** (the third downstream judgement over one pure resolver - read→404, write→409, export→none): an archived Unit with bookings MUST keep serving its calendar or the subscribed OTA sees free nights and double-books. PII cannot leak **by construction** - the export selects only `(id, check_in, check_out)` for `status='confirmed'`, and the hand-rolled RFC-5545 serializer's `CalendarEvent` type has no field for a name/email/phone/price; `SUMMARY` is a fixed constant. Half-open `[check_in, check_out)` maps to an all-day `DTEND` (also exclusive) with zero arithmetic. Connect (§7.1) smoke-fetches via an injected `IcalFetcher` port (fake in tests, §8.5; real one refuses private/loopback hosts - SSRF hygiene) and a feed that's down still connects with `error` status; duplicate `(unit, channel)` → `409 channel_already_connected` (new closed-set code, ADR-0012; app pre-check + `channel_connection_unit_channel_uniq` constraint indistinguishable, §5.3, migration 0009). Disconnect keeps imported bookings (FK `set null`) and reports `{ importedBookingsKept }`; never auto-cancels. A per-feed token is the documented hardening path | Yes (owner) |
| 2026-07-19 | **Orphaned photos are swept against the gallery, per tenant** ([ADR-0017](docs/adr/0017-orphaned-photos-are-swept-against-the-gallery.md), #69, deferred from #39). A daily `@Cron` `PhotoGcSweeperService` on the owner connection (cross-tenant, mirroring the M2 hold sweeper) reclaims photo objects no gallery references. The DB gallery is the AUTHORITY: `referenced` = union of every `property.photos` (one owner-connection read); a referenced key is NEVER deleted (the one invariant that matters, tested first). Tenant-scoped listing (`<tenantId>/` prefixes of tenants THIS db knows) is a SAFETY boundary, not an optimisation - a whole-bucket sweep would treat another dev/CI lane's objects (shared Garage store, isolated DBs) or Garage's `index.html` as unreferenced and destroy them. 24h grace window (never race an in-flight upload; presign lives 5 min) via `sweep(now)` - a test passes a future `now` to age a just-uploaded object without faking S3 mtime. Oversize eviction (`> MAX_PHOTO_SIZE_BYTES`) is a grace-independent backstop that also STRIPS the key from its gallery (`array_remove`) + logs loudly - the one place the sweep writes. `StorageService` gained `listObjects`/`deleteObjects`. Test-first against real DB+Garage, every sweep confined to its own tenant | The store drifts from the ledger (abandoned uploads, dropped-but-lingering keys, oversize slipping past a non-enforcing backend), and #39 deferred the reclaim as "GC'd out-of-band". Delete FROM the store to match the gallery, never the reverse - the DB is truth, the store a cache holding extra bytes. A cron on the owner connection is the only shape that crosses tenants; tenant-scoped listing is what makes it safe on a shared store - the "tenant-scoped listing" acceptance criterion in disguise | Yes (owner) |
| 2026-07-20 | **The payment webhook reconciles on the owner connection, not under RLS** ([ADR-0018](docs/adr/0018-the-payment-webhook-reconciles-on-the-owner-connection.md), #53, boss fight #4). `POST /webhooks/payment/:provider` runs entirely on `DbService` (RLS-bypassed) like the cron sweepers - NOT a fifth resolve-then-RLS entry. A webhook is a SYSTEM reconciliation of platform-brokered money, not an actor browsing a tenant; it carries no principal, and every statement is PK-targeted (`order_id = payment.id`), so there is no tenant-scoped WHERE for RLS to backstop. Idempotency: in ONE `db.transaction`, INSERT `payment_event(provider, provider_event_id)` then apply the state change; the unique constraint arbitrates (redelivery / concurrent loser → caught OUTSIDE the txn via `pgError().constraint`, 25P02 → 200 no-op), and a crash between insert and apply rolls back BOTH → clean replay. `provider_event_id = transaction_id:transaction_status` (a redelivery collapses, a real pending→settlement does not); every UPDATE guarded on current status so no re-order un-confirms a paid booking or resurrects a dead one. Late settlement (hold lapsed) → payment paid, booking NOT resurrected + loud WARN (guard on status, not `hold_expires_at`, so a past-TTL-but-still-pending hold DOES confirm - the exclusion constraint held the nights). Signature+parse behind the `PaymentGateway` port (`verifyAndParse`, SHA512 over parsed fields → no raw-body middleware; `FakePaymentGateway` verifier keeps tests off live Midtrans); verified payload on `payment_event.raw_payload` (migration 0010), never on `payment.raw_payload` (the session a retry reads). Unknown provider → 404; verified-but-unknown order → 200+WARN; NO `@ThrottleSensitive` (a 429 to a provider = retry storm). FR-NOTIF-1 email is a post-commit seam (logged no-op, can't fail the webhook), deferred | Overlap-of-authority: the read-can't-disagree-with-write principle demands ONE definition of "processed", and the unique constraint + guarded applies make "duplicate = no-op" true by construction, not by luck (invariant #7). Owner connection because the sweeper category (no principal, crosses to whichever tenant owns the row) fits a system reconciliation, whereas resolve-then-RLS is for principals that browse - and minting a Visitor for a Provider is the lie the Principal union forbids. Two-session-reviewed (no-auth write across the RLS boundary) | Yes (owner) |
| 2026-07-20 | **Link-preview crawlers get real OG tags at the edge, from one shared helper** ([ADR-0019](docs/adr/0019-og-tags-for-crawlers-at-the-edge.md), #87, SEO tier 2). `deploy/Caddyfile` matches a NARROW, explicit allowlist of preview-crawler UAs (WhatsApp/`facebookexternalhit`/Twitterbot/Telegram/LINE/…, never Googlebot - it renders JS, a stub would be cloaking) on `/p/*` and proxies just those to `GET /public/properties/:slug/og`; everyone else gets the SPA. The route reuses the JSON page's `getBySlug` (same `enterFromSlug`+RLS, archived→404 per ADR-0006, malformed→404 via `SlugParamPipe`) - not a second read path - and renders a static, HTML-escaped OG stub with a meta-refresh bounce to `/p/:slug`. OG values come from ONE shared helper `buildPropertyOgTags` (`packages/shared`) that the SPA's `property-meta.tsx` <meta> tags use too, so crawler card and rendered page can't drift. Caddyfile not run in tests; its UA allowlist asserted narrow by `property-og.spec.ts`. Tenant-authored name/description escaped (5 chars) to block attribute/HTML injection into a page on our origin | Yes (owner) |
| 2026-07-20 | **Reconcile-on-read pulls the same event the webhook pushes** ([ADR-0020](docs/adr/0020-reconcile-on-read-pulls-the-event-the-webhook-pushes.md), #54). `GET /public/bookings/:id` (the confirmation page, the FIFTH public route) reconciles on read (risk R3): a `fetchStatus` on the `PaymentGateway` port PULLS the Provider's status as the SAME `ParsedPaymentEvent` a webhook yields, and `PaymentWebhookService.reconcile` feeds it through the IDENTICAL idempotent transition `handle` uses (owner connection, ADR-0018) - so a lost webhook still confirms, and whichever path wins, the `payment_event` unique constraint no-ops the rest BEFORE `afterCommit`. FR-NOTIF-1 email = a best-effort post-commit seam behind a `Mailer` port (`LogMailer`, no paid provider - invariant #8; real Resend/SMTP = one rebind), fired ONLY on the status-guarded confirm that flips a row, so it's once-per-confirmation by construction. Read = `enterFromBookingId` (404 unknown, ADR-0008) → reconcile → opportunistic hold-sweep AFTER (ADR-0009: a just-confirmed booking is skipped, an unpaid past-TTL hold flips to `expired` now) → RLS-scoped view. `waLink` = the guest's own wa.me deeplink (FR-NOTIF-2), server-built. `afterCommit`/`notifyConfirmed` now awaited (deterministic, still can't fail the webhook). New env `MIDTRANS_API_BASE_URL` | The read-can't-disagree-with-write spine demands ONE definition of "processed"; reusing the webhook's transaction (not a second confirm path) makes "email exactly once, duplicate-safe" true by construction, not luck (invariant #7). A `Mailer` port tested by a recording fake is the only honest way "no live email in tests / no paid provider" holds | Yes (owner) |
| 2026-07-20 | **The confirmation email sends through Resend when configured, else logs** ([ADR-0021](docs/adr/0021-the-confirmation-email-sends-through-resend-when-configured.md), #119). Fills in the provider ADR-0020 deferred: a `ResendMailer` over native `fetch` (no SDK, mirroring MidtransGateway/ADR-0015 - keys read at CALL time, 8s timeout, REJECTS on any failure so the best-effort caller logs it) implements the existing `Mailer` port. `MAILER` is bound by a `createMailer` FACTORY that returns `ResendMailer` iff `RESEND_API_KEY` **and** `MAIL_FROM` are set, else the zero-cost `LogMailer` - so dev/test/unconfigured-prod stay off any live provider (no suite reaches Resend: test env has no key → LogMailer, and the e2e test overrides `MAILER` with a recording fake) and real sending is ONE env flip, zero call-site change. Factory-with-fallback, NOT payment's `useClass`+loud-throw: email is best-effort → degrade gracefully to a rendered log line; money must fail loud, a confirmed booking must not depend on a wired mailer. Resend free tier only (invariant #8); `nodemailer`/SMTP = documented plan B, rejected to add ZERO deps. EN-only template (FR-I18N-1 = follow-up). NO migration (owner email already sourced by NotificationsRepository, #54); send-loop per-recipient isolation (#126) left untouched. Deploy: verify a domain in Resend, set `RESEND_API_KEY`+`MAIL_FROM` in VPS env (gitignored, never repo) | The read-can't-disagree spine needs ONE send path bound by config, not a `NODE_ENV` stub that could ship; a fetch adapter matching the gateway grain adds no dependency and is proven against a mocked transport, the only honest way "tested without live creds" holds | Yes (owner) |
| 2026-07-20 | **The funnel and the dashboard are two bundles; libphonenumber is a leaf chunk** ([ADR-0023](docs/adr/0023-the-funnel-and-dashboard-are-two-bundles.md), #125, follow-up to #54 review). Every route component loads through `lazyRouteComponent` (route tree - paths/`validateSearch`/`beforeLoad` - stays static; only leaf components defer), so Vite emits one chunk per page and a guest on `/p/:slug` never downloads the dashboard. `libphonenumber-js` is a **dynamic** import of the checkout page (`phone.ts` unchanged; lazy boundary at its import site), fetched at the phone step, so the property/confirmation pages never carry it. `scripts/check-bundle.mjs` (Node built-ins, reads the Vite manifest) fails the build if the dashboard or the phone chunk is statically reachable from the property route, if `phone.ts` stops being a dynamic import of checkout, or if the property route's initial JS crosses a gzip budget (185 KB). Property page initial JS: ~234 → ~161 KB gzipped, no dashboard/libphonenumber. `lazyRouteComponent` over `.lazy.tsx` stubs (whole tree stays in one file); per-route over coarse funnel-eager/dashboard-lazy (the latter still ships checkout's libphonenumber in the entry). vitest async timeout raised (cold chunk transform vs the 1 s default) - test-transformer concern, invisible in prod | Funnel first-load weight is where ADR-0007's "hand-design where guests look" cashes out (mobile Indonesia); a shared monolith made a guest pay for the whole dashboard + a checkout-only 25 KB dep. Structural guard (what's *reachable*, not just bytes) fences the regression even if a re-merge stayed small | Yes (owner) |
| 2026-07-20 | **The paid-but-lapsed inbox marks, it does not mutate the ledger** ([ADR-0022](docs/adr/0022-the-paid-but-lapsed-inbox-marks-not-mutates.md), #120, follow-up to boss fight #4). The owner-facing surface for the late-settlement case ADR-0018 handles silently: a guest pays AFTER the hold lapsed/was cancelled, so `payment.status=paid` while `booking.status IN (expired,cancelled)`, and a WARN is not a workflow. A nullable `payment.handled_at` marker (migration **0011**) + two authed owner-RLS routes: `GET /payments/lapsed` (paid + lapsed-booking + `handled_at IS NULL`, joined for amount/guest/dates/names - owner disclosure, scoped by `booking.tenant_id` beside RLS since `payment` has no `tenant_id`) and `POST /payments/:id/handle` (a guarded UPDATE that writes ONLY `handled_at`, never `payment.status`/the booking - the item leaves the inbox because the list's predicate excludes it, not because the ledger changed; ADR-0002). Handle is idempotent (already-handled → 200 no-op; unknown/cross-tenant/non-inbox → 404), so no new ADR-0012 conflict code. Amount-mismatch deliberately OUT (that path never produces a `paid` row); unhandle deferred (column is nullable). Tenant isolation proven at the HTTP layer (a reviewer's cross-tenant read AND handle both 404, B's row untouched) | "Handled" is a new operator fact, orthogonal to what the money did and the calendar says - deriving it is impossible, and encoding it by mutating the ledger corrupts the record to satisfy a UI (ADR-0002 generalized). A nullable column inherits `payment`'s row-level RLS for free (no policy migration) and matches the annotate-in-place grain (`archived_at`) | Yes (owner) |
| 2026-07-20 | **The funnel speaks three languages, the wire speaks one** ([ADR-0024](docs/adr/0024-the-funnel-speaks-three-languages-the-wire-speaks-one.md), #58, M5/FR-I18N-1). Public funnel EN/ID/ZH with **no i18n library** (invariant #8): a dependency-free typed message catalog (`apps/web/src/i18n/`) + a tiny `useSyncExternalStore` locale store + a `useI18n()` hook (typed `t(key)` + `fmtDate`/`fmtNights`/`fmtGuests`). Matches the house style (`lib/conflict.ts`/`availability-copy.ts` already compose copy from typed switches with zero libs); platform `Intl` + react-day-picker's already-bundled `id`/`zh-CN` locales cover dates + the calendar. **Untranslated-key guard has two layers**: compile-time (`id`/`zh` annotated `: Messages` from `typeof en` → a missing key fails `tsc`, which runs in `pnpm typecheck`, pre-push, AND the web `build`) + a runnable parity/token test (`pnpm test`) - new copy CANNOT ship EN-only. **Wire stays `YYYY-MM-DD`**; only display follows locale (EN funnel=en-GB, money stays id-ID always). **Availability blocked-reasons stay language-neutral slugs**, the SPA localizes - the approach api-spec §1/§5.1 + the shared schema comment MANDATE ("the SPA composes localized copy from the slug"), the ADR-0012 precedent; no server change. Choice persists in `localStorage` (`sambung.lang`, tokens unaffected) + rides as `Accept-Language`. Switcher rendered at the route-tree root, gated to public routes by pathname (dashboard stays English, page-spec §2); `I18nProvider` at root so every `renderAt` test gets it. Funnel-entry initial JS 161→170 KB gzip (still under the ADR-0023 185 KB budget) | Three languages where only EN inflects nouns don't need react-i18next/react-intl's tens of KB + ICU machinery; a 60-line catalog + Intl is simpler, robust, and adds zero deps. Server-rendered reason prose would duplicate the catalog into a second codebase, defeat response caching, and break the "code not sentence" spine (ADR-0012). Type-level parity is a stronger untranslated-key gate than a lint scan, and it is already wired into the build | Yes (owner) |
| 2026-07-20 | **A healthy feed reconciles; a doubtful one does nothing** ([ADR-0025](docs/adr/0025-a-healthy-feed-reconciles-a-doubtful-one-does-nothing.md), #56, boss fight #3, blocks #38). The iCal IMPORT pipeline: a 30-min cron + "Sync now" pull every `channel_connection` and mirror its VEVENTs into `booking`. Runs on the OWNER connection (RLS-bypassed, the sweeper/webhook category ADR-0009/0018 - a system reconciliation with no principal that crosses tenants), every write scoped EXPLICITLY by `tenant_id` + connection id. Fetch OUTSIDE the txn (never pin a pooled connection across a round-trip); one txn/connection with a **SAVEPOINT per VEVENT** so a `23P01` overlap (a real double-sell) skips that event and the cycle survives - the exact catch #38 drops its `sync_conflict` INSERT into. Idempotent by the `(channel_connection_id, external_uid)` partial unique index (`ON CONFLICT DO UPDATE` → re-pull no-op, changed stay in place, re-appearing UID re-confirmed). **"Healthy" = a whole, TERMINATED `BEGIN…END:VCALENDAR`** (truncation loses the `END`); unhealthy (unreachable / non-2xx / not-terminated / parse-throw) → `last_status=error`, ZERO writes. The absent-UID cancellation runs **only on a healthy feed with ≥1 event** - a healthy-but-empty one stamps `ok` but cancels nothing (empty is indistinguishable from truncated-to-zero → never mass-cancel). Parser HAND-ROLLED (`ical-parse.ts`, the symmetric ADR-0016 call; trust confined to a zod-validated `{uid,start,end}`, bad VEVENTs skipped not fatal; `ical.js` = plan B); `IcalFetcher` gains `fetchFeed` beside `probe` through one SSRF-guarded walk. "Sync now" → **200 synchronous + a summary** (no job queue on one VPS), resolving the connection under owner RLS first (404-over-403). NO migration (`external_uid` + its index shipped with #40/#48) | The read-can't-disagree-with-write spine needs ONE definition of "processed": the exclusion constraint + the partial unique index make idempotency and overlap-refusal true by construction, not by luck (invariant #7). Never-mass-cancel is a SAFETY bias made structural - a broken download is unhealthy by the terminated-envelope test, and even a valid-empty feed cancels nothing, so no truncation can ever wipe real stays. Owner connection because a cron with no principal that crosses tenants fits the sweeper category, not resolve-then-RLS. Two-session review is a hard AC (boss fight) | Yes (owner) |
| 2026-07-21 | **A statement is guarded where it is issued, not where it is built** ([ADR-0026](docs/adr/0026-a-statement-is-guarded-where-it-is-issued.md), #75, boss fight #5, residual from #72/#73). The `TenantDbService.run` liveness guard moves off the transaction HANDLE onto the SESSION - it wraps `session.prepareQuery`, the single funnel every query (select/insert/update/delete/raw execute/count) reaches at EXECUTION time - and the old `guardTx` handle Proxy is DELETED (its `Reflect.get`-around-private-fields dance with it). #73's handle proxy fired when `tx.select` was *called* (build time, alive); drizzle's builders are lazy thenables, so a query BUILT inside the callback and AWAITED after `run` returns executed on a recycled connection under another tenant's GUC - #75, the escape a build-site guard can't reach (unreachable in-tree, real hole in a trust seam). Session-guard SUBSUMES the handle (the #73 `tx.execute`-after-settle case funnels through `prepareQuery` too), so ONE guard at the true chokepoint covers both - replace, not augment (two guards for one property = the drift ADR-0012 forbids). `alive` flips false SYNCHRONOUSLY the instant the callback returns (rejecting deferred work even mid-commit-round-trip); drizzle's own COMMIT/ROLLBACK run through the same guarded session AFTER, so they're allow-listed by SQL text (`isTransactionControl` - transaction control, never tenant data, so passing them post-settle leaks no row; rejecting the ROLLBACK would mask the caller's error). Safe to mutate in place because the session is per-transaction (pool-backed `db.transaction` mints a fresh `NodePgSession` per checkout - `createDb` using a Pool is load-bearing). Couples to two drizzle internals (`session` is the executor, `prepareQuery` the sole funnel) LICENSED by the escape test (build-inside/await-after → throws) as tripwire: a drizzle upgrade routing around `prepareQuery` goes red, not silently reopens. Scope = `TenantDbService` only; the owner connection (`DbService`: sweepers/webhook/iCal import) is out of scope (RLS-bypassed, no per-tenant GUC to leak across). Reproduced red-first (deferred builder returned rows `[]` instead of throwing) before the fix; independent review before merge is a hard AC (boss fight) | The read-can't-disagree-with-write spine: the guard must sit on the thing that matters (where SQL is issued), not a handle stand-in that drifts from it - derive-don't-store applied to a guard. One authority over redundant guards (ADR-0012); an internal is safe to reach only when a test watches it | Yes (owner) |

| 2026-07-21 | **Dismiss is a judgement, resolve is a measurement** ([ADR-0027](docs/adr/0027-dismiss-is-a-judgement-resolve-is-a-measurement.md), #38, boss fight #3, the last M4 piece). The `sync_conflict` inbox: an imported VEVENT `booking_no_overlap` refused (a real double-sell) is filed by the per-VEVENT savepoint catch ADR-0025 built, on the owner connection, scoped explicitly by `tenant_id` + the composite FK. Keyed on the constraint NAME (ADR-0012), so any other per-event fault stays a log line, not an owner's inbox item. **The one honest exception to derive-don't-store**: the losing VEVENT is a row that FAILED to exist, so it cannot be queried for. Departs from the 2026-07-16 issue text twice, deliberately: **no `raw_vevent`** (the parser drops SUMMARY/DESCRIPTION so imported PII never enters - storing the raw block re-admits it through a side door) and **two `date` columns, not a `stay` daterange** (the schema's only daterange is built inline inside the exclusion constraint). Closing is ONE statement requiring a POSITIVE observation - the complement of every offered UID that did NOT land, NOT merely "what overlapped" (that narrowing was a real bug both reviewers caught) - covering both "blocking booking cancelled" and "OTA withdrew the event", guarded by healthy ∧ ≥1 event, the same never-guess rule as the absent-UID cancellation. **Re-detection is asymmetric, and that is the load-bearing rule**: `dismissed` STAYS dismissed (else it resurrects every 30 min until the owner ignores the inbox), `resolved` REOPENS (it healed and re-broke - news); a measurement may be re-taken, a judgement stands until its author revisits it. NO resolve endpoint (that would let the UI assert what the constraint still refuses); dismiss is an idempotent guarded UPDATE writing only `status`+`closed_at`, 404-over-403, no new ADR-0012 code. The list derives `blockingBookings` at read time via the SAME `daterange &&` over the SAME `OCCUPYING_STATUSES` the constraint uses, so it is exactly the set that caused the refusal and §5.6 is one click away. `openConflicts` fills the §7.2 field #55 shipped without a source; the section joins the paid-but-lapsed payments on ONE `/app/inbox` (both are "the system did the safe thing and is now stuck") | Yes (owner) |

| 2026-07-21 | **Property-local is a column, not an assumption** ([ADR-0028](docs/adr/0028-property-local-is-a-column-not-an-assumption.md), #145, migration 0013). `property.time_zone` (NOT NULL default `Asia/Makassar`) from a CLOSED set - WIB/WITA/WIT - `CHECK`-backed and mirrored by `propertyTimeZoneSchema`; `parseCalendar(body, timeZone)` takes it as a REQUIRED argument and consults it ONLY for `Z`-suffixed values. Reverses the 2026-07-21 "leave it, document it" call on the same issue | The bug was a quarter its stated size: RFC-5545's `VALUE=DATE`, floating, and `TZID` values are already property-local (params are stripped before the date is read), so ONLY the UTC form was ever wrong - which made the fix small enough to be worth doing. A closed set because Postgres CANNOT validate an IANA zone in a CHECK (`AT TIME ZONE` is STABLE, not IMMUTABLE), so free text would leave the one column whose entire purpose is correctness as the only one with no DB backstop; a `pg_timezone_names` FK would restore the gate but make validity depend on the host's tz-database version. Defaulted not required: a mandatory select is clicked through as fast as a default, and a defaulted-but-wrong zone narrows the wrong-date window from 8 hours to 1. The zone is an ARGUMENT to the parse, not a judgement withheld (ADR-0008 doesn't transfer - a UTC value has no date until a zone is named, so it's missing INPUT); required, so no caller can silently reintroduce it. Foreign `TZID` reported via `ParseResult.foreignTimeZones` + a WARN, never converted (the inverse wall-time→instant direction is where DST gaps live) and never a `sync_conflict` (ADR-0027's line: a defect belongs in a log). Zero deps - `Intl.formatToParts`, full ICU guaranteed by engines `>=20.9.0` | Yes (owner) |
| 2026-07-21 | **A cutover is verified by a probe, not by the test suite** ([ADR-0029](docs/adr/0029-a-cutover-is-verified-by-a-probe.md), #68). `pnpm --filter api storage:doctor` - a script that boots no app and opens no DB connection - drives the SAME `StorageService` the API uses against whatever bucket `STORAGE_*` names, and reports six probes: reachability, a presigned PUT round-trip, content-type enforcement, content-length enforcement, that `STORAGE_PUBLIC_BASE_URL` serves the bytes ANONYMOUSLY and byte-identically, and that a real `OPTIONS` preflight from the site origin is allowed. Content-length is a **FINDING**, not a failure. Plus two production-only `validateEnv` guards: `STORAGE_PUBLIC_BASE_URL` must be https + non-loopback, and `STORAGE_BOOTSTRAP` must not be `true`. Dashboard steps + the measured-answer table live in [docs/r2-cutover.md](docs/r2-cutover.md). Replaces #68's own instruction to point `jest properties-photos` at prod | Three of the photo pipeline's guarantees are the BACKEND's, not ours - the type whitelist and the 5 MB cap ride on signed headers, and browser uploads ride on bucket CORS. Cloudflare documents the first, is SILENT on the second, and supports neither `PutBucketCors` nor `PutBucketWebsite` over the S3 API, so the "single env swap" is one edit plus three unverified assumptions. The prescribed check was wrong three ways: it REGISTERS TENANTS (`beforeAll` → `POST /auth/register`, so it writes to whatever `DATABASE_URL` names and cleans up only if it reaches teardown), it drags a Postgres + Nest boot into an object-store question, and it is structurally BLIND to two of the three - it PUTs from Node, which has no same-origin policy, and never reads `STORAGE_PUBLIC_BASE_URL`, so it would go green on a bucket that cannot serve one photo to one browser. Same instrument reused, not re-implemented (the read-can't-disagree-with-write rule: one `quote()`, one `buildPropertyOgTags`, one `StorageService`), because a doctor with its own presigning proves things about itself. Separate script rather than a suite because the two have OPPOSITE safety requirements - `pnpm test` must never reach a prod bucket, a cutover probe must deliberately be pointed at one - and because jest has no vocabulary for a measurement whose answer is not a verdict. Boot guards rather than runbook lines for exactly the two mis-sets with NO server-side evidence (no exception, no log, no failing request), the `WEB_BASE_URL`/#127 category; `STORAGE_ENDPOINT` deliberately NOT guarded, since the documented Garage-on-VPS fallback makes `http://localhost:3900` correct in prod. #68's open conditional ("must #69 land first?") is RESOLVED not re-decided: it shipped 2026-07-20 with grace-independent oversize eviction, so a non-enforcing backend degrades the cap to "enforced within a day" over a tenant's OWN gallery - quota risk, never data or cross-tenant. Probe self-verified red-first (a broken public base and a forced transport failure) before being trusted green | Yes (owner) |
| 2026-07-21 | **A cap is a preference, the ceiling is the guard** ([ADR-0030](docs/adr/0030-a-cap-is-a-preference-the-ceiling-is-the-guard.md), #67, migration 0014). `tenant.gallery_cap smallint not null default 30` (CHECK 1-100) is the tenant's own line; `MAX_PHOTOS_PER_PROPERTY` is RENAMED `PHOTO_GALLERY_CEILING = 100` because it stops being the cap. The shared schema bounds the array by the CEILING (a static schema cannot know which tenant asks); the service enforces the CAP. The load-bearing rule: **"never GROW past the cap", not "never exceed it"** - `keys.length > cap && keys.length > existing.length` - because `PATCH …/photos` is a whole-set write, so a plain `> cap` would trap an over-cap gallery (every edit, INCLUDING the removal that would fix it, sends more keys than the cap). `GET /settings` open to any signed-in user (the workbench needs the cap); `PATCH /settings` owner-only via the codebase's FIRST role check - a reusable `@Roles`+`RolesGuard` built as the seam #57 extends, reading `TenantContext` (#76's one owner of the principal) and refusing a Visitor explicitly. 403 not 404-over-403 (no existence to hide from your own staff); 400 not an ADR-0012 code (exceeding a declared limit is the `size > MAX` category, not a state conflict - and this endpoint's sibling refusals are already 400s) | #39's "abuse guard" premise was wrong, and checking it is what shrank the design: the constant never bounded storage - property count is unbounded, so a tenant could always park a thousand galleries of thirty. It bounds one request body and one gallery grid; bytes are bounded by the 5 MB signed cap and the orphan sweeper (ADR-0017). So the question was never "may a tenant weaken a control", it was "where does a per-request sanity bound live once one number stops fitting every villa". A column on `tenant` over a settings table (one knob does not earn a table; inherits `tenant_isolation` RLS free, no policy migration) and over per-property (deposit_pct/time_zone are real per-property facts, "how many photos" is not) - the property-level shortcut would have avoided building a settings surface, which is a reason to build the wrong thing. Widening the ceiling stays a migration, like property_time_zone_known | Yes (owner) |
| 2026-07-22 | **A request is strict; a response is lenient** ([ADR-0031](docs/adr/0031-a-request-is-strict-a-response-is-lenient.md), #150, no migration). Every inbound request schema in `packages/shared` - **body AND query param** - rejects unknown keys, built through one `strictObject(shape) = z.object(shape).strict()` helper at all 14 construction sites; response schemas keep plain `z.object`. Surfaced by the #67/#149 review and deliberately deferred there, because fixing one schema would have left one endpoint refusing what its siblings accept. A partial PATCH schema cannot tell a MISSPELLED field from an OMITTED one, so `PATCH /settings {galleryCapp: 60}` returned **200 having changed nothing** - and `?form=` instead of `?from=` silently falls back to a default the same way, which is why queries are in scope, not just the PATCH bodies the issue named. **Responses stay lenient on purpose**: a strict client breaks the moment the server adds a field a cached SPA bundle predates - boundary validation inbound, forward-compatibility outbound, opposite rules for opposite directions. ONE inbound exception, documented so nobody "fixes" it: the Midtrans webhook parser (`verifyAndParse`, in apps/api, no shared schema, no pipe) plucks a few fields from a large provider payload, so strict would 400 every real webhook. The uniformity guard is a test that DISCOVERS `*RequestSchema`/`*QuerySchema` exports from the barrel, so a new schema that reaches for `z.object` fails without anyone registering it - with its two limits stated rather than glossed, an overstated guard being worse than a modest one: it discovers by NAME (so `*BodySchema` is deliberately excluded - here that names a RESPONSE body, `conflictBodySchema`), and a discriminated union's members are module-private, so the probe is seeded down EVERY branch (covering one leaves the others unproven - the independent review proved exactly that hole by reverting the walk-in branch and watching the suite stay green). Outside the convention by construction: the webhook parser and the hand-rolled iCal parser (the verb-subresource POSTs were the third, left open here as a known gap and closed by #152 - see the row below); `apiCreateBookingRequestSchema` lives in apps/api and inherits strict by derivation, so it is pinned by its own test | Verified against zod 3.25 before committing to it: `.partial()` PRESERVES strict (and still lets an omitted-but-defaulted field stay omitted - the `unit.ts` short-circuit), `.refine()` chains still reject unknown keys, and strict `discriminatedUnion` members reject on the matched branch - so the three shapes the booking/query schemas actually use all compose. The issue's stated risk (the SPA round-tripping a response object back as a PATCH body, which would newly fail on `id`/`slug`/`createdAt`) was measured and does not exist: all three PATCH mutations build minimal, explicitly-keyed bodies and send `parsed.data`. A helper over 14 inline `.strict()` calls because the request/response asymmetry then reads as intent at the keyboard, one authority per concern | Yes (owner) |

| 2026-07-22 | **A staff scope is a second axis in RLS** ([ADR-0032](docs/adr/0032-a-staff-scope-is-a-second-axis-in-rls.md), #57, migration 0015). "Staff sees assigned properties only, in every list AND by direct id" is enforced by the POLICIES, not by ~30 routes: `TenantDbService.run` sets `app.property_scope` ('all'\|'assigned') + `app.staff_user_id` beside `app.tenant_id`, from the SAME principal, and each policy ANDs one term - a `stable` `app_property_visible()` helper, SECURITY INVOKER so `user_property`'s own policy applies inside it. TWO GUCs not one, because Postgres does not guarantee `OR` short-circuits, so a single `'all'\|uuid` GUC would let the planner evaluate `'all'::uuid` and raise 22P02 - #74's trap in a new costume. Fail-closed on BOTH axes: unset/reset (`''`) → the EXISTS matches nothing → zero rows, so a scope is granted, never merely un-restricted. `user_property` gains `tenant_id` + composite FKs (closing #40's deferred follow-up, AC #4) - load-bearing twice, since it also lets that table's policy be flat, which it MUST be or `property`→`user_property`→`property` recurses. `payment`/`payment_event` deliberately UNTOUCHED: their policies subquery `booking`, whose (tightened) policy applies inside, so the term reaches them without a second copy - pinned by a test, not a comment, because that is a rewriter subtlety on a money table. Two axes → two codes: unassigned = **404** (RLS, it isn't there), owner-only verb = **403** (`@Roles`, naming the role, and the guard runs BEFORE any lookup so it is no oracle). The verb line is a principle: the owner decides the SHAPE of the tenant (create/delete/archive a Property, settings, the team), staff OPERATE what they are assigned | A `WHERE` in 30 repositories is exactly the forgotten-clause leak invariant #2 + RLS exist to make unrepresentable, and a resolve-then-check guard covers by-id routes but not lists - two authorities for one rule, the drift ADR-0012 forbids. Establishing the second axis where the first already lives means no repository, service or controller filters by property, and the specified 404 falls out of the `0 rows → NotFoundException` every getter already had | Yes (owner) |
| 2026-07-22 | **An invite is a hashed, single-use grant** ([ADR-0033](docs/adr/0033-an-invite-is-a-hashed-single-use-grant.md), #57, migration 0015). `staff_invite` stores a **sha256** of a 32-byte CSPRNG token (not bcrypt: there is nothing to slow down about 256 bits, and hashing buys the property that matters - a dump is not a list of working links); the raw token exists ONLY in the email, never in any response. Grants ride on `staff_invite_property`, a join table with a composite FK to `property (id, tenant_id)`, so a cross-tenant grant is unrepresentable one step earlier than `user_property` - and a Property deleted mid-invite cascades out instead of exploding at accept. Single-use = a guarded UPDATE that goes FIRST in the accept transaction (two racing accepts contend on that row; check-then-update would be boss fight #1's window), with `expires_at > now()` evaluated by the DATABASE so one clock decides. bcrypt runs OUTSIDE the txn - a row lock across 300 ms of CPU is how a hot path becomes a queue. Unknown token → **404**, known-but-dead → **409 `invite_not_acceptable {reason}`** (ADR-0015's shape): only a real holder is told why, so a guess confirms nothing; duplicate live invite → **409 `invite_already_pending`** off a PARTIAL unique index (accepted/revoked ones are history and must pile up). Accept runs on the OWNER connection - no principal to scope by, and the row it creates IS the thing that would grant the scope - safe by the ADR-0016 property: every statement keyed by an unguessable value. A failed send ROLLS THE INVITE BACK (503): the token is recoverable from nowhere else, and a pending-unreachable invite would block every retry | The email is the delivery mechanism, not a notification, so it cannot be best-effort like the booking confirmation. Returning the token from an API is the one design that makes every future bug able to leak it, so the cost of "lost email = revoke and re-invite" is taken deliberately | Yes (owner) |
| 2026-07-22 | **A route that takes no body says so** (#152, amends [ADR-0031](docs/adr/0031-a-request-is-strict-a-response-is-lenient.md), no new ADR, no migration). Closes the one gap #150 left: a handler with no `@Body` has no schema to make strict, so Nest never read the body and `POST /bookings/:id/cancel {"refund":"full"}` answered **200 having ignored it** - the same indistinguishable-from-success failure, on the routes that had nothing to fix. Sixteen routes now carry `@NoBody()`, a marker applying a guard that validates the body against the shared `noBodyRequestSchema` (`strictObject({})`), so the refusal is byte-identical to every other unknown-key 400 - one instrument, not a second "is it empty" convention. A **guard**, not the `@Body(pipe)` parameter the issue assumed (that adds sixteen arguments existing only for a side effect); a route-local marker with the behaviour behind it is the shape used twice already (`@Roles`+`RolesGuard`, `@ThrottleSensitive`+`skipIf`). NOT a global auto-detecting guard: that must reflect `ROUTE_ARGS_METADATA` in PRODUCTION code and a broken detection could fail **open** silently - so prod code carries zero framework-internals coupling and the reflection lives in the enumeration test, where a break is loud (ADR-0026's "safe to reach an internal when a test watches it"). That test walks every mutating route and fails one declaring neither a `@Body` nor the marker, discovering the `@Body` param key from a probe class rather than hardcoding it; proven red by deleting one marker. **Widened past the issue's table to DELETE** (`{"force":true}` is the same caller bug, and plausible given ADR-0002's delete guard). Two limits stated not glossed: the guard reads `req.body`, so a non-JSON content type stays invisible (nothing reads a raw body either way), and the marker is per-route, which is exactly what the enumeration test exists to catch. Schema named `noBodyRequestSchema`, NOT `emptyBodySchema` - `*BodySchema` is the suffix #150's drift test deliberately excludes (it names a RESPONSE body), so the right name joins that test for free | A per-route judgement call is what makes a convention rot, and `app.module.ts` already argues the case for the sibling interceptor: "a per-module opt-in is a per-module chance to forget". The SPA was verified safe first, not assumed: `api.post(path)` omits body AND Content-Type when there is none, and `refreshSession` is a raw fetch with only a method - so the 401-retry path, where a regression logs everyone out, sends exactly the shape the schema accepts | Yes (owner) |

| 2026-07-22 | **One identity, many memberships** ([ADR-0034](docs/adr/0034-one-identity-many-memberships.md), #154, migration 0016). `app_user` becomes pure identity (email + password, no tenant); `membership (app_user_id, tenant_id, role)` becomes belonging, so **Owner and Staff are what a membership IS, not kinds of person**. `user_property` and `staff_invite.created_by` repoint their composite FKs at `membership`, which is STRICTLY STRONGER than the `app_user (id, tenant_id)` unique they replace - "is a member of" survives a second seat where "this is the user's tenant" cannot, and ending a membership cascades its Assignments off it. `app_user`'s RLS policy becomes an `EXISTS` over `membership`, whose own policy must be FLAT (its columns + GUCs only) or the two recurse - the identical lesson `user_property` learned in ADR-0032; no other policy changes, and both axes still fail closed. Login stays ONE request (default membership = owners first then oldest, computed in one query and stored nowhere, plus `memberships[]` for the switcher) rather than a half-authenticated "now choose a tenant" state; `POST /auth/session {tenantId}` re-issues for a seat already held, `404` for one that isn't (a `403` would enumerate tenants a uuid at a time). The refresh token gains an OPTIONAL `tenantId` so a refresh returns to the right seat without signing pre-migration cookies out. Invite creation narrows from "has an account ANYWHERE" to "is already on THIS team"; accept forks on `mode: create\|signin` (preview says which) and in `signin` mode the password is VERIFIED against the existing account - a wrong one is `401` and does NOT spend the invite, because the token proves the mailbox and the password proves the account, and adding a seat to someone's login needs both. `DELETE /staff/:id` deletes the SEAT, never the human. An account with zero memberships gets `403` at login (reachable only after a CORRECT password, so no oracle; `401` would send them to reset a password that works). `POST /tenants` deliberately out of scope - register still `409`s, since unauthenticated signup cannot know the caller holds that address | The global-email constraint is real (login carries no tenant) but it was being paid by the PERSON: #57 could only make "you already have an account elsewhere" fail legibly, and a property manager working for two villa owners is squarely the target market. Splitting the row splits the two questions, and the FK repoint means the multi-tenant model is enforced by the same structural guarantees invariant #2 already relies on rather than by new app checks | Yes (owner) |
| 2026-07-22 | **The edge is verified by running it, not by asserting its config** ([ADR-0035](docs/adr/0035-the-edge-is-verified-by-running-it.md), #60 AC 4, no migration). `deploy/Caddyfile` is committed, reviewed, load-bearing and **executed by nothing** - it says so about itself - so `property-og.spec.ts` reading its UA regex as TEXT proves narrowness but not that the rewrite fires, the anchor holds, or the SPA fallback wins. An opt-in `--profile edge` runs the REAL file (bind-mounted, never copied - a copy drifts from what ships, the failure ADR-0019 exists to prevent), and `pnpm --filter api og:doctor [baseUrl] [slug]` probes whatever origin it is GIVEN: routing (crawler→stub, human/Googlebot/LINE-in-app-browser→SPA, anchoring, trailing slash, unknown slug→404), the card (tags equal `buildPropertyOgTags` over the LIVE JSON - the first test of ADR-0019's no-drift claim over the wire rather than by shared code; image fetchable anonymously), and the canonical. The target is an ARGUMENT, never app config (unlike storage-doctor it loads no `.env`), because it must be pointable at an origin that DISAGREES with what the app claims - and a config file the script can read is not evidence about the process answering on a tunnel. The canonical is tested BEHAVIOURALLY: a forged `Host` must not move `og:url`, written over `node:http` because undici SILENTLY DROPS a `Host` header (measured, not assumed) and a `fetch` version would have reported "held firm" against an origin that never saw the forgery - one vacuous pass, forever. Severity follows what the answer MEANS (ADR-0029's rule): an `og:url` mismatch is a finding on loopback (Caddy serves :80, `WEB_BASE_URL` names Vite on :5173) and a FAILURE on a public origin (there it IS the #127 defect). AC 4 closes without a VPS via a free anonymous Cloudflare quick tunnel - **two** of them, site + photos, which is the PROD shape (Caddy serves the app, R2 the photos), so a single-origin tunnel would test an arrangement that never ships. A script, NOT `pnpm test`: continuous coverage would need Docker + a built SPA + container-to-host networking in a repo with no cloud CI that just spent effort removing flakiness, and a flaky suite gets skipped and then LIES about coverage - so the gap is stated in the runbook instead. Self-verified RED first: 8 seeded misconfigurations all caught with a non-zero exit, plus both precondition paths reporting the actionable diagnosis | ADR-0029's claim is that a storage BACKEND's guarantees are someone else's to keep; this names a different category - committed config that nothing executes - which recurs (`docker-compose.yml`, `.env.example`). "Deploy first" was never quite true, and a crawler is a poor instrument for a mechanical chain: one blurry verdict, cached for days, silent about which link broke | Yes (owner) |
| 2026-07-23 | **The route map is enforced against the router, not maintained by discipline** ([ADR-0036](docs/adr/0036-the-route-map-is-enforced-against-the-router.md), #158, no migration). New `docs/sitemap.md` is the single code-verified orientation index - a mermaid route-tree + every SPA page + every API endpoint (grouped, machine/edge separate) + an FE↔API traceability table - that **links** into page-spec/api-spec rather than copying (single source per fact). It cannot silently drift: two guards enumerate the REAL routes and fail with the exact `{missing,invented}` diff - FE (`sitemap.guard.test.ts`, vitest) reads `router.routesById`; API (`sitemap.guard.spec.ts`, jest) walks Nest's `DiscoveryService` over every controller, the SAME `PATH_METADATA`/`METHOD_METADATA` reflection #152's `no-body.spec` uses, extended to all verbs. Enumerate from the framework's own registration, NEVER an allowlist of controllers/paths (that list is the drift in a new costume); the one deliberate exclusion is TanStack's synthetic `__root__`. Guard the machine-checkable half (what exists), hand-write the prose half (what it's for) - a full generator is a heavier hybrid that still can't write the purpose and couples shipped tooling to Nest decorators. Motivated by the drift the build surfaced on arrival: `/app/channels` documented but non-existent (health moved to the Property workbench, inbox became `/app/inbox`), `/` mis-documented as a redirect, `/app/inbox` undocumented, and two endpoints with NO FE consumer (`POST /channels/:id/sync`, `GET /auth/me`). page-spec §1 map → pointer, §4.6 corrected, api-spec §2 note added. Self-verified RED first (broke one FE + one API row, each guard named the exact route) | An unenforced discipline is the exact mechanism that produced the drift - page-spec §5 already promised "fix it in the same PR" and it was violated anyway. The read-can't-disagree-with-write spine, applied to docs: the router IS the route list, so verify the map against it rather than a second hand-kept copy (ADR-0035/0029's "committed and reviewed is not verified", one layer up) | Yes (owner) |

| 2026-07-23 | **The dashboard is a sidebar shell, and width follows the page's job** ([ADR-0037](docs/adr/0037-the-dashboard-is-a-sidebar-shell.md), UI/UX grill session, no migration). The `/app/*` shell was a flat top-nav with nav AND content boxed to `max-w-4xl` (896px) - which strangled the two WIDE data views (calendar timeline, reservations table), showed no signed-in identity, didn't group/scale, and died on mobile. Reversed into: a left SIDEBAR (workspace at top via the ADR-0034 switcher, then nav grouped Operate/Manage with lucide icons, an Inbox count badge = open sync-conflicts + lapsed-payments via `use-inbox-count`, the SAME two queries `/app/inbox` reads so they can't disagree) + a slim TOP BAR (account menu = email/role/Log out, the identity the old shell never showed; the mobile nav trigger) + CONTENT whose width is decided ONCE in the shell from a `WIDE_ROUTES` set - full-bleed for calendar/reservations, capped ~1024px for everything else (derive-don't-store applied to layout, no page hand-sets width). Mobile = the same sidebar as a Radix Dialog DRAWER (focus-trap/Escape/scroll-lock from the installed headless primitive, not hand-rolled - ADR-0007); the corner account menu IS hand-rolled (a menu needs no focus trap). Inside ADR-0007: semantic tokens only, NO new dep (reuses Radix Dialog + lucide), no re-theme. DEFERRED honestly: the top-bar page TITLE + primary ACTION - a route-derived title is wrong for the dynamic detail routes (booking/property show a name), and lifting each page's action needs a per-page registration mechanism; the spine ships now, so this PR touches NO page (pages keep their `<h1>` + in-content actions). Funnel budget unchanged (ADR-0023 check-bundle; the shell is dashboard-only) | "A dashboard has a sidebar" is a pattern not a law (GitHub/Stripe ship top-nav); it earns its place HERE because the owner lives in wide data views all day (content needs the width a centred column can't give), the nav is still growing (grouping separates the live queue from config), and an operator tool wants a persistent scannable rail. Independent review warranted (touches the auth session + workspace switcher) | Yes (owner) |

| 2026-07-23 | **A browser-drivable payment gateway is an env seam, guarded at boot** (#167 part b, amends [ADR-0015](docs/adr/0015-a-payment-session-is-one-reused-row.md), no new ADR). `PAYMENT_GATEWAY` binds `PAYMENT_GATEWAY` via a `createPaymentGateway` factory (mirroring `createMailer`): the real `MidtransGateway` by default, the signature-free `FakePaymentGateway` ONLY on the literal `PAYMENT_GATEWAY=fake`. This departs from ADR-0015's "fake deliberately NOT in the DI graph - a foot-gun", because that stance relied on jest's `.overrideProvider`, which cannot reach a REAL running API process - and Playwright boots one, so a browser-drivable confirm (reconcile-on-read / a fake webhook POST, no outbound Midtrans) MUST be an env switch. The foot-gun is closed by `validateEnv` refusing to boot with `PAYMENT_GATEWAY=fake` (the exact fail-fast shape ADR-0029 gave STORAGE_BOOTSTRAP - a mis-set with a catastrophic-but-silent symptom, here "a prod that confirms fabricated payments for free"). **AMENDED 2026-07-24 (#193): the words "closed STRUCTURALLY, not by discipline" did NOT hold as written** - the refusal was gated on `NODE_ENV === 'production'`, which nothing in this repo sets, so it rested on an operator remembering one variable that has no other effect. The guard now fires whenever the process cannot prove it is a local sandbox (see the 2026-07-24 row below), which is what makes the original claim true. Fail safe not fail open (only the exact string selects the fake; unset/other → real), and binding the fake emits a boot WARN so an unexpected fake is loud in any log. `.overrideProvider` still works unchanged (it replaces the token regardless of class-vs-factory), so no jest suite changes. Independent review before merge is a hard AC (payment-adjacent, boss-fight #4 territory) | Yes (owner) |
| 2026-07-24 | **A calendar date is the same day for everyone; a moment is not** (#188, no new ADR, no migration). The glossary gains **Calendar Date** and **Moment** (CONTEXT.md §Time) and the two get separate formatters: `formatDate` keeps its UTC pin (a Stay's check-in is 22 July for the guest, the owner and the OTA alike), while a new `formatInstant` resolves the READER's zone and shows the clock time. Fixes three sites that rendered `formatDate(x.someAt.slice(0, 10))` - the UTC day of a moment, i.e. yesterday for a reader in WIB/WITA/WIT before 08:00, which is a third of every day in the only timezones this product serves. The email is the interesting one: it has no reader to ask, so it stops naming a day at all and says `expires in 7 days` - a duration is true in every zone, where a date can only be right by luck. Guarded by a `*.guard.test.ts` in the ADR-0036 family that reads feature/component sources and fails on the literal slice, over branded `IsoInstant`/`IsoDate` wire types: the brand buys a real compile-time guarantee but pays for it with `as` casts in every response constructor, and all three bugs were the same nine characters, so the tripwire sits exactly on the path that has been walked - with its limit stated (a novel spelling gets past) rather than oversold. Time shown, not just the day, so a dropped zone can never again be invisible. Also removed the duplicate `addDays` in `calendar-model.ts` (byte-identical to `lib/date`'s), which is what let a slice live in a feature file at all | Yes (owner) |
| 2026-07-24 | **A shared dev bucket needs one CORS policy, not one per boot** (#182, no new ADR, no migration). `applyDevBucketConfig` drops its origin parameter and applies a CONSTANT `AllowedOrigins: ['*']` (PUT only). Bucket CORS is global to the bucket and one Garage serves every stack pointed at it - `pnpm dev`, `storage:doctor`, each `SAMBUNG_E2E_LANE=<n>` lane on its own web port - so the old `[WEB_ORIGIN]` made every boot rewrite the bucket to ITS origin and the last API to start silently 403'd the others' upload preflights (reproduced: two full lanes, one `1 failed / 39 passed` on the only uploading flow with "Upload failed - network error", bucket left holding the winner's origin). Same shape as the 2026-07-23 `PAYMENT_GATEWAY=fake` row - a deliberately widened env-only seam, logged without an ADR. The fix is "every writer writes the same thing", not "list more origins": Garage was MEASURED to implement no S3 origin pattern (`http://localhost:*` matches nothing) and to answer a multi-origin rule with all of them comma-joined, an `Access-Control-Allow-Origin` no browser accepts - a shape that passes a server-side probe and fails in the browser. One-rule-per-origin works but imports the harness's port arithmetic into the API and caps the lanes; a bucket per lane (`STORAGE_BUCKET` is env-driven) was the bigger change for the smaller problem and would undo the deliberate object sharing the e2e README documents. Safe because the presigned URL is the capability - CORS never authorised the write. **Two review findings, both fixed**: the doctor's CORS probe read only the ACAO header, and Garage attaches `*` to its 403 REFUSALS, so wildcard-applied, wrong-origin and NO-POLICY-AT-ALL all printed one `[PASS] … WILDCARD` line - it now requires a 2xx as well, making the last two FAIL (the blind spot that let #182 hide); and "this can never reach a production bucket" was overstated, since `validateEnv`'s guard is gated on `NODE_ENV=production`, which NOTHING in this repo sets (`start:prod` is a bare `node dist/main`, no Dockerfile) while Garage-on-VPS is a blessed prod shape where the call succeeds - so the claim is qualified as a deploy obligation and `docs/r2-cutover.md` now sets `NODE_ENV` first | Yes (owner) |
| 2026-07-24 | **A deployment is recognised by where it sends browsers, not by what it declares** (#193, amends [ADR-0029](docs/adr/0029-a-cutover-is-verified-by-a-probe.md) + the 2026-07-23 `PAYMENT_GATEWAY=fake` row above, no new ADR, no migration). Every production guard in `validateEnv` sat behind `NODE_ENV === 'production'`, and NOTHING in this repo sets that (`start:prod` is a bare `node dist/main`, no Dockerfile, no compose service) - so all four were inert on a real deployment unless an operator remembered one variable with no other visible effect, and the ADR-0029/#167 "closed structurally, not by discipline" claims were, as written, discipline. A new `deployment-env.ts` owns ONE answer for every caller: **a process is a deployment UNLESS it proves it is a local sandbox**, the proof being that every origin it declares that a GUEST'S BROWSER is sent to - `WEB_BASE_URL`, `STORAGE_PUBLIC_BASE_URL` - is loopback. Those two because they are browser-facing, so on a real deployment they CANNOT be loopback (a stranger's phone does not resolve `localhost`) - the exact reasoning `validate-env` already used to justify guarding `STORAGE_PUBLIC_BASE_URL`, now reused as the switch; and because `STORAGE_PUBLIC_BASE_URL` is `getOrThrow` in `StorageService`, so every process that can boot declares at least one. `STORAGE_ENDPOINT` is deliberately NOT consulted, and the inversion is the whole trap: R2 in prod is non-loopback but rejects `PutBucketCors` anyway, while the blessed Garage-on-VPS shape (architecture §3.6) is LOOPBACK and is the one where that call really rewrites a live bucket - keying on it would protect the safe case and miss the dangerous one. `NODE_ENV=production` stays SUFFICIENT (the runbook still sets it) but is no longer necessary, and a declared `development` does NOT exempt: a declared mode travels with a copied `.env`, where the guests are sent does not. Chosen over "refuse to boot on an unset/unknown `NODE_ENV`" (option 1), which forces every existing checkout to add a variable and so breaks dev boot - the AC forbids it - and over docs-only (option 3), rejected because a catastrophic-but-silent foot-gun is the one category this codebase closes structurally. **The residue is stated, not glossed**: a deployment declaring only loopback browser origins - `.env.example` copied wholesale onto a VPS - still reads as local, because nothing distinguishes it from a laptop. What changed is that it is no longer SILENT: that box serves a broken `<img>` for every photo and sends payers back to `localhost` on day one. The guarantee is "either the guard fires, or the misconfiguration is loud", which is as far as it goes without a build artifact. Errors carry the evidence (`treated as a deployment because WEB_BASE_URL names the public origin https://…`), because a guard that fires for an unexplained reason is a guard people work around. **Second finding, fixed with it**: #193 states `validate-env.ts:28` is the sole reader of the `production` value; it is not - `refresh-cookie.ts` gated the `Secure` attribute on the same unset variable, so a deployment that forgot it shipped a session cookie a plain `http://` request to the same host would carry. Same predicate, same fix. The one workflow this newly refuses is deliberate: `docs/og-verification.md`'s tunnel pass points both origins at public Cloudflare tunnels, so the box IS publicly reachable and `STORAGE_BOOTSTRAP=true` is now refused there - documented in the runbook rather than special-cased in code | Yes (owner) |

*Append one row per architecture decision. Keep it terse.*

---

## Project Facts *(this is the part that changes)*

- **Stage**: **M4 (channel sync) COMPLETE** - export feed + connection lifecycle (#55), the iCal IMPORT pipeline (#56, boss fight #3), and the sync-conflict inbox (#38) all shipped; M3 (payments) in progress; M2 (availability & direct booking) complete; M1 inventory complete. Done: M0 (monorepo, DB schema + RLS migrations, seed with demo logins, auth API, local pre-push quality gate) + property CRUD with `verified`/`publishable`, dashboard list/edit pages, SPA session plumbing incl. `/login` (#44) + photo pipeline: Garage in compose, presigned uploads, gallery on the edit page (#39) + `/register` signup page (#63) + units CRUD with the inline table, first money-on-the-wire (`toRupiah`), and the delete guard rewritten to protect the ledger (#45, ADR-0001/0002) + **the public property page `/p/:slug` and the first unauthenticated path to the database** (#46, closing #77: `property.slug`, `PublicScope` + the Visitor principal, SEO tier 1, demo photos in the seed; ADR-0003/0004) + **archive - retire inventory that has history** (#84/#89, ADR-0005/0006: nullable `archived_at` derived up the hierarchy, not cascaded; archived Property → public `404` with the slug reserved; delete's 409 now names archive; two-session-reviewed). That cleared the M2 blocker - a booked Unit is no longer immortal-and-public. **M2 now open**: the availability + quote endpoint `GET /public/units/:id/availability` shipped - boss fight #2's READ side (#47/#94, ADR-0008: `PublicScope.enterFromUnitId` as a pure resolver; interval overlap/clip in SQL sharing the exclusion constraint's operators; `blockedRanges` unconditional + coalesced, reasons as slugs; archived Unit → `404`; two-session-reviewed, whose one finding - a `basePriceIdr × nights` overflow that could 500 the no-auth quote - was fixed by the `unit_base_price_max` nightly-rate cap (1e9 IDR, migration 0006, rejected twice over)). **Boss fight #1 shipped** (#48/#96, ADR-0009, two-session-reviewed - whose one finding, a global rate-limiter that collapses to one shared bucket behind Caddy without `trust proxy`, was fixed by env-gated `trust proxy` in `main.ts` + honest `.env.example` guidance; the booking logic itself passed, concurrency independently re-proven at N=10): the guest booking write `POST /public/bookings` (#48, ADR-0009). One transaction does an opportunistic intra-tenant hold-sweep, an in-txn availability re-check via the shared `quote()` (the one interval authority, joined into the txn), a `guest_count ≤ max_guests` check, then the INSERT the `booking_no_overlap` exclusion constraint arbitrates - a racing overlap maps to the *same* 409 the re-check gives (proven with real concurrency **and** a deterministic constraint-force). Pessimistic 15-min hold; dead holds cleared by a sweep at **two scopes** - the opportunistic in-txn one plus a 5-min cross-tenant cron on the owner connection - because the exclusion predicate is immutable and can't check `now()`. Migration 0007 split free-text `guest_contact` → `guest_phone`/`guest_email` and added `guest_count` (so a Unit's `max_guests` is finally enforced); refusal `reasons` = `overlap|min_stay|max_guests|unavailable` (the archived-Unit → 409, ADR-0008's deferred item, now landed); `@nestjs/throttler` guards the no-auth public surface (env-driven, protective default). **Design-system foundation shipped** (#91/#99, ADR-0007): stone+terracotta semantic tokens, Plus Jakarta Sans + Fraunces self-hosted (no CDN), shadcn rethemed in `components/ui/`, the lowercase `sambung` wordmark; every existing page swept off raw ramps onto semantic tokens - the base all M2 UI builds on. **The unified multi-property calendar shipped** (#49/#102, ADR-0010): the dashboard home `/app/calendar` - a hand-rolled CSS-grid timeline (no library; the exclusion constraint makes each Unit row a non-overlapping 1-D sequence), one Unit row each, occupying bookings as bars **coloured by source** with Holds hatched, off-window "continues" affordance. Composed client-side from a new authed `GET /bookings` (whole rows - owner disclosure, the opposite of #47's clipped public read; **repeatable** `status` naming the shared `OCCUPYING_STATUSES`; paired 366-capped overlap window) + a flat `GET /units` + `GET /properties`; property/window filters in typed search params. Two-session-reviewed - the one real finding (the property filter narrowing only the fetch, so other properties still drew as empty rows) was fixed with a regression test; tenant isolation proven 13/13 on real Postgres. **Owner booking ops shipped** (#50/#104, ADR-0011, two-session-reviewed): `POST /bookings` (manual block / walk-in, born `confirmed`, no payment dance), `GET /bookings/:id` (detail, owner full disclosure, 404-over-403), and `POST /bookings/:id/cancel` (FSM as an atomic guarded UPDATE, 0 rows → 404 vs 409 by an existence check). "The owner is an authority, not a customer" - the write reuses the guest funnel's *one* overlap chokepoint (a shared `sweepQuoteOrThrow`: opportunistic hold-sweep + `quote()` + `booking_no_overlap` → the identical `409 {reasons:['overlap']}`) but **skips** the guest-protection policy checks (`min_stay`, `max_guests`); a walk-in is `source=direct` (entry-method derived, not a new enum), a block is `manual_block` with a null price, an archived Unit → `409 {reasons:['archived']}`. Cancel is the universal free-the-dates verb (holds, walk-ins, blocks) with `refund:"manual"` iff a paid payment exists (always `none` at M2). Web: the calendar's empty-day cells are now buttons opening a block/walk-in dialog, and its bars link to the new `/app/bookings/:id` detail page (cancel via a confirm dialog). The review's one finding (AC #3's paid-refund branch implemented but untested) was closed with a seeded-payment test. **The reservations list shipped** (#51/#106, ADR-0010, no new ADR): the operational table `/app/reservations` - a frontend-only composition over the *same* neutral reads the calendar uses (`GET /bookings` + `GET /units` + `GET /properties`), shown as rows of **every** status rather than bars of the occupying ones. Filters (window/property/status/source) are typed, AND-ed, shareable search params - `status`/`source` repeatable set-params robust to a hand-typed single value; each combination its own cache entry sharing the `["bookings"]` prefix. Opens on the default **upcoming** window `[today, today+366)` (owner's call, reversing an initial no-default-window design): a lone/invalid edge falls back to it with a pair hint, never sent as a 400 (the never-send-a-lone-edge guard). Two distinct empty states (the AC) keyed on whether a filter is *touched* (the URL), not the result count: "No upcoming reservations" (default window empty - a past-only tenant isn't truly empty) vs "No matches" (explicit filter empty). Sorted by check-in (server order, never re-sorted). Cleanups along the way: promoted `StatusBadge`/`SourceBadge` out of the detail page (were private, would have drifted), plus `bookingTitle` and `lib/date` (`formatDate`/`addDays`/`todayIso`), and active nav styling; CONTEXT.md gained **Booking** (the base entity, previously undefined) and **Reservation** (the list view - a lens, not an entity). Not two-session-reviewed: a read-only composition over an already-reviewed endpoint. **Operational hardening shipped** (#59, ADR-0014): the reservations **CSV export** `GET /bookings/export.csv` - the list's `text/csv` twin, reusing the SAME filter-condition builder as `GET /bookings` (so "respects the active filters" is true by construction, not a second read path), joining property/unit NAMES in, emitting integer IDR as exact `bigint` digits (no float/separators/e-notation), RFC-4180-escaped with CSV-formula-injection neutralised on the attacker-controlled guest name, UTF-8 BOM for Excel; web "Export CSV" button downloads it via an authed `api.getBlob` (Bearer, 401-retry) as a windowed filename. Plus **tiered rate limiting**: a TIGHTER `sensitive` throttler (env-driven, protective 10/60s) on `login`/`register`/`POST /public/bookings` via a `@ThrottleSensitive()` marker + `skipIf`, on top of #48's generous `default`; an `EnvelopeThrottlerGuard` renders the 429 as `{ statusCode, error, message }` + a standard `Retry-After` (both limits stay in the ConfigService factory so they're env-off in test; the throttle spec drops one env var, fires `limit+1`, no sleep). **The availability picker UI shipped** (#93/#113, ADR-0013): the `/p/:slug` date picker (react-day-picker headless on #91's tokens) quoting against #47 - past-only hard-disable, greyed-but-selectable booked nights (the server stays the one overlap authority), the six picker states, and selection state entirely in `?from&to&unit` so a shared link reproduces the quote; the last M2 guest-funnel UI piece (checkout form + payment is M3). **M3 opened - the payment session + Deposit shipped** (#52, ADR-0015, two-session-reviewed): the guest→pay slice end to end. `POST /public/bookings/:id/pay` opens a Midtrans Snap **sandbox** session for a live Hold in ONE transaction under the tenant `PublicScope.enterFromBookingId` resolves (the third pure resolver, ADR-0008); the opportunistic hold-sweep (ADR-0009) runs first so "hold lapsed" and "wrong status" become one post-sweep read → `409 booking_not_payable {status}` (new `conflictCodeSchema` slug, ADR-0012); a `FOR UPDATE` on the booking serialises concurrent pays so a retry REUSES the booking's open (`pending`) payment row - `payment.id` IS the Provider `order_id` (the key #53's webhook resolves by), the session `{token,redirectUrl}` rides in `raw_payload` (a Snap token is Provider vocabulary, not a typed column), `amount_idr` snapshotted. Amount = the Deposit share, BigInt-floored (invariant #6): `property.deposit_pct smallint 1-100 default 100` (migration 0008), editable on the property edit page (api #10); the `deposit` flag = amount < total. The Provider sits behind an injected `PaymentGateway` port (MidtransGateway over `fetch`, no SDK; a `FakePaymentGateway` bound in tests via `.overrideProvider` - so no suite touches live Midtrans). Web: the checkout placeholder became the real guest-details form (create → pay → redirect to Snap, hold countdown, 409-routed copy) + a thin `/booking/:id` landing (the reconcile confirmation page is #54). Seed: Canggu is 30% deposit to demo the partial vs pay-in-full split. **M4 opened - channel connection lifecycle + iCal export shipped** (#55, ADR-0016): the OTA sync EXPORT side and the connect/list/disconnect lifecycle. `GET /public/units/:id/calendar.ics` (unauthenticated) resolves the tenant from the unit UUID via `PublicScope.enterFromUnitId` and reads confirmed bookings UNDER RLS - so a no-auth feed is structurally not a cross-tenant read path (invariant #2); it is deliberately **archive-blind** (an archived Unit keeps serving its calendar, or a subscribed OTA double-books) and PII-free by construction (a hand-rolled RFC-5545 serializer whose `CalendarEvent` type has no field for a name/price; half-open `[in, out)` → all-day `DTEND` exclusive with zero arithmetic). `POST /units/:id/channels` validates https at the boundary + smoke-fetches once via an injected `IcalFetcher` port (fake in tests, real one blocks private/loopback hosts) → `lastStatus`, a down feed still connects with `error`; `GET /units/:id/channels` lists status (`openConflicts` deferred with the import-side conflict inbox, #38); `DELETE /channels/:id` keeps imported bookings (FK `set null`) and reports `{ importedBookingsKept }` - never auto-cancels. Duplicate `(unit, channel)` → `409 channel_already_connected` (new `conflictCodeSchema` slug, ADR-0012; app pre-check + `channel_connection_unit_channel_uniq` unique constraint indistinguishable, migration 0009). Web: a per-unit Channels panel on the property edit page - copy-export-URL with the paste-into-OTA helper, connect form, status badges, disconnect. (Fixed along the way: the shared conflict closed-set test was missing `booking_not_payable` from #52 - CI is local-only, so it had slipped.) **Photo orphan GC shipped** (#69, ADR-0017): the out-of-band reclaim #39 deferred - a daily `@Cron` `PhotoGcSweeperService` on the owner connection (cross-tenant, mirroring the M2 hold sweeper) deletes photo objects no gallery references. The DB gallery is the AUTHORITY (delete FROM the store to match the ledger, never the reverse); a referenced key is NEVER deleted (the one invariant that matters, tested first). Tenant-scoped listing (`<tenantId>/` prefixes of tenants the DB knows) is a SAFETY boundary, not an optimisation - what makes a destructive sweep safe on a Garage store shared across dispatch/dev lanes whose DBs are isolated. 24h grace window (never race an in-flight upload) via a clock-shiftable `sweep(now)`; oversize eviction (`> MAX_PHOTO_SIZE_BYTES`) is a grace-independent backstop that also strips the key from its gallery + logs loudly. **#55 and #69 shipped in parallel via the dispatch workflow, each independent-reviewed (a fresh same-model reviewer, not the human/two-session gold standard - the caveat stands for #55's no-auth read path + migration)**: #55's one fix round closed an SSRF redirect-follow gap - the smoke-fetch now re-validates the https + private-host block on every redirect hop with a bounded cap, failing closed - plus a disconnect-confirm copy bug; #69's added the foreign-tenant-survives / unreferenced-oversize / real-cron-path safety-net tests before merge. **Boss fight #4 shipped - the idempotent payment webhook → confirmed** (#53, ADR-0018, two-session-reviewed): `POST /webhooks/payment/:provider` on the OWNER connection (RLS-bypassed, like the sweepers - a webhook is a system reconciliation of platform-brokered money, not an actor browsing a tenant; every statement PK-targeted by `order_id = payment.id`, so there is no tenant-scoped WHERE for RLS to backstop). Idempotency in ONE `db.transaction`: INSERT `payment_event(provider, provider_event_id)` then the guarded state change; the unique constraint arbitrates - a redelivery or the concurrent loser hits it, caught OUTSIDE the txn via `pgError().constraint` (25P02) → 200 no-op, and a crash between insert and apply rolls back BOTH → clean replay (all ACs proven on real Postgres incl. real concurrency + a spied crash-fault on the split-out `applyOutcome` seam). `provider_event_id = transaction_id:transaction_status` (a redelivery collapses, a real pending→settlement does not); every UPDATE guarded on current status so no re-order un-confirms a paid booking or resurrects a dead one. Late settlement (hold already swept to `expired`) → payment recorded `paid`, booking NOT resurrected + loud WARN (guarded on status, not `hold_expires_at`, so a past-TTL-but-still-pending hold DOES confirm - the exclusion constraint held the nights the whole time). Signature+parse behind the `PaymentGateway` port (`verifyAndParse`; SHA512 over parsed fields → no raw-body middleware; a `FakePaymentGateway` verifier + a real-crypto `midtrans.gateway.spec` keep suites off live Midtrans); the verified payload lands on `payment_event.raw_payload` (migration 0010), never on `payment.raw_payload` (the Snap session a pay-retry reads back). Unknown provider → 404; verified-but-unknown order → 200+WARN; NO `@ThrottleSensitive` (a 429 to a provider = retry storm); FR-NOTIF-1 email is a post-commit seam (logged no-op, can't fail the webhook), deferred alongside the owner-facing paid-but-lapsed inbox (follow-ups filed). **SEO tier 2 shipped** (#87/#122, ADR-0019, dispatch-workflow independent-reviewed): link-preview crawlers (WhatsApp/`facebookexternalhit`/…, never Googlebot - it renders JS) get real per-property OG tags via a NARROW Caddy UA-allowlist → a static, HTML-escaped OG stub route (`GET /public/properties/:slug/og`) that reuses the JSON page's `getBySlug` (archived→404, malformed→404) and shares `buildPropertyOgTags` (`packages/shared`) with the SPA's `<meta>` tags, so crawler card and rendered page can't drift; a `<meta http-equiv=refresh>` bounces a human to `/p/:slug`. **M3's confirmation page shipped** (#54/#123, ADR-0020, dispatch-workflow, independent-reviewed + one fix round): `GET /public/bookings/:id` reconciles ON READ through the IDENTICAL idempotent webhook transaction (`PaymentWebhookService.reconcile` pulls a `PaymentGateway.fetchStatus` through the same status-guarded confirm - a lost webhook still confirms on page load, the `payment_event` unique constraint no-ops the rest, so the FR-NOTIF-1 email fires once-per-confirmation by construction), a best-effort exactly-once email behind a `Mailer` port (`LogMailer` no-op - invariant #8; real Resend/SMTP = one rebind, #119), the guest's `wa.me` deeplink, and country-aware guest-phone capture (an international field defaulting to ID → E.164 storage → `wa.me` correct for ANY country; `normalizeWaPhone` requires canonical E.164 so a lenient walk-in phone OMITS the button rather than emit a broken link; `libphonenumber-js` `/min` confined to the web funnel bundle). Both merged via the dispatch workflow; the ADR-0019 collision (both lanes picked 0019) was resolved by renumbering #54's ADR to 0020 on merge. Review follow-ups filed: #124 (per-country server-side phone validation), #125 (funnel code-split + lazy `libphonenumber`), #126 (email per-recipient isolation), #127 (OG-stub LINE-UA + canonical-origin hardening). **M3 confirmation email shipped** (#119/#129, ADR-0021, dispatch-workflow independent-reviewed): the real `ResendMailer` over native `fetch` (no SDK, no new dep) behind #54's existing `Mailer` port, selected by an env-gated factory that falls back to `LogMailer` when unconfigured (so no test or unconfigured prod touches a live provider); real creds (`RESEND_API_KEY` + `MAIL_FROM`) are a deploy step. Folded in **#126** (per-recipient send isolation - each recipient's `send` in its own try/catch so a guest-address bounce no longer silently drops the owner's booking email; closed by #129). **Paid-but-lapsed owner inbox shipped** (#120/#130, ADR-0022, migration 0011, dispatch-workflow independent-reviewed with an RLS-only cross-tenant probe): `GET /payments/lapsed` (tenant-scoped) surfaces `paid` payments whose booking is `expired`/`cancelled`; `POST /payments/:id/handle` sets ONLY a nullable `payment.handled_at` marker - the row leaves the inbox because the predicate excludes it, never by mutating the ledger (ADR-0002). #119/#120 shipped in parallel via the dispatch workflow, ADR numbers pre-assigned (0021/0022) so no renumber. **Two more #54-review follow-ups (#124/#132, #125/#133) shipped in parallel via dispatch, each independent-reviewed (PASS), with the review's own non-blocking recommendations applied before merge**: **#124 - a per-country phone validity gate** (no ADR): a server-only `.refine(isValidPhoneNumber)` in `apps/api` (`public-booking.schema.ts`) layered over the shared E.164 *shape* schema, so a request crafted to bypass the SPA can no longer store a shape-valid-but-implausible number; `libphonenumber-js` stays confined to `apps/api` (never `packages/shared` or the web bundle - AC-proven). **#125 - the funnel and dashboard are two bundles** (ADR-0023): every route component loads via `lazyRouteComponent` (route tree/`validateSearch`/`beforeLoad` stay static, only leaf components defer), and `libphonenumber-js` is a dynamic import at the checkout phone step; property-page initial JS drops ~234 → ~161 KB gzipped with no dashboard/libphonenumber, verified from the Vite manifest. The `scripts/check-bundle.mjs` structural guard is **wired into the web `build`** so a re-merge fails the build (no cloud CI; deploy always builds), and a **Retry** affordance covers the lazy phone chunk failing to fetch mid-funnel. **SEO OG-stub hardening shipped** (#127/#135, dispatch-workflow independent-reviewed): the LINE crawler token corrected (`Line/` matched the LINE in-app browser - a human; now `line-poker`, LINE's real link-preview scraper, asserted against the committed Caddyfile regex so it can't drift) and the OG `og:url`/canonical now derives from the trusted `WEB_BASE_URL` origin instead of the spoofable request `Host`/proto, with a production boot-time fail-fast (`validateEnv` in `main.ts`) when it is unset (the review's own non-blocking recommendation, applied before merge). **M5 i18n shipped** (#58/#136, ADR-0024, dispatch-workflow independent-reviewed): the public funnel speaks EN/ID/ZH with **no i18n library** (invariant #8) - a dependency-free typed catalog + a `useSyncExternalStore` locale store + platform `Intl`; a language switcher persisted per visitor in `localStorage` (the token rule unaffected - tokens stay in memory + httpOnly cookie), locale-formatted display while the wire stays `YYYY-MM-DD` (proven by asserting the `POST /public/bookings` body), availability blocked-reasons localized client-side from the slug-on-the-wire contract (api-spec §5.1, no server change), and a two-layer untranslated-key guard (compile-time `: Messages` typing + a runtime parity test) wired into typecheck/build/test so copy cannot ship EN-only. #127 and #58 shipped in parallel via dispatch, disjoint (`area:property` vs `area:i18n`) so no ADR/decision-log collision; each lane's review-raised non-blocking recommendations (gallery `alt` i18n + an ID end-to-end booking test on #58; the boot-time `WEB_BASE_URL` guard on #127) were applied before merge. **Boss fight #3 shipped - the iCal IMPORT pipeline** (#56/#138, ADR-0025, `/code-review` independent-reviewed - Standards clean / Spec PASS, both reviewers' notes applied; same-model reviewers, so a human final gate was flagged and the owner merged on their call): a 30-min cron + a synchronous "Sync now" pull every `channel_connection` and mirror its VEVENTs into `booking` on the OWNER connection (RLS-bypassed, the sweeper/webhook category - a system reconciliation with no principal that crosses tenants, ADR-0009/0018), every write scoped explicitly by `tenant_id`. Fetch OUTSIDE the txn; one txn/connection with a **SAVEPOINT per VEVENT** so a `23P01` overlap (a real double-sell) skips that event and the cycle survives - the exact seam #38's `sync_conflict` inbox slots its INSERT into. Idempotent by the `(channel_connection_id, external_uid)` partial index (`ON CONFLICT DO UPDATE`: re-pull no-op, changed stay in place, re-appearing UID re-confirmed). **"Healthy" = a TERMINATED `BEGIN…END:VCALENDAR`** (truncation loses the `END`); the absent-UID cancellation runs only on a healthy feed with **≥1 event**, so a truncated/empty/unreachable feed → `last_status=error`, ZERO cancellations (never mass-cancel real stays). Parser HAND-ROLLED (`ical-parse.ts`, the symmetric ADR-0016 call; trust confined to a zod-validated `{uid,start,end}`, bad VEVENTs skipped not fatal; `ical.js` = plan B); `IcalFetcher` gained `fetchFeed` beside `probe` through one SSRF-guarded walk; a `channel ⊂ booking_source` pin test guards the `source=channel` cast. "Sync now" → **200 synchronous + a summary** (no job queue on one VPS), resolving the connection under owner RLS first (404-over-403). **NO migration** (`external_uid` + its partial unique index shipped in #40/#48). Two deliberate behaviors flagged to the owner at merge (both accepted): a valid-but-**empty** calendar stamps `ok` not `error` (still cancels nothing - only the status label differs from the literal AC wording), and re-confirm can revive a manually-cancelled **imported** booking whose UID persists in the feed (OTA = source of truth, double-book-safe via the savepoint). A pre-existing parallel-jest flake in #48's hold-sweeper idempotency assertion surfaced and was filed as **#139** (not touched here - it's a #48 boss-fight test). **Boss fight #3 completed - the sync-conflict inbox** (#38, ADR-0027, migration 0012): the refused VEVENT is no longer just a WARN. The per-VEVENT savepoint catch #56 built now files a `sync_conflict` row (owner connection, explicit `tenant_id` + the `(unit_id, tenant_id)` composite FK, RLS policy for the owner-side read), keyed on the constraint NAME `booking_no_overlap` so any other per-event fault stays a log line rather than noise in a villa owner's inbox. **The one honest exception to derive-don't-store** - the losing VEVENT is a row that FAILED to exist. Two deliberate departures from the 2026-07-16 issue text: **no `raw_vevent`** (ADR-0025's parser drops SUMMARY/DESCRIPTION so imported PII never enters; the raw block would re-admit it) and **two `date` columns rather than a `stay` daterange** (the schema's only daterange is inline in the exclusion constraint). Auto-heal is ONE statement requiring a POSITIVE observation - the complement of every UID the feed offered that did NOT land, covering both "the blocking booking was cancelled" and "the OTA withdrew its event" (keying it on overlaps ALONE was a real bug both independent reviewers caught: a VEVENT failing for an unrelated reason fell into the healed set, silently resolving a live double-sell - fixed + regression-tested red-first) - under the same healthy ∧ ≥1-event guard as the absent-UID cancellation. **The load-bearing rule is the re-detection asymmetry**: a `dismissed` conflict STAYS dismissed (else the cron resurrects it every 30 min until the owner stops looking) while a `resolved` one REOPENS - a measurement may be re-taken, a judgement stands. No resolve endpoint (it would let the UI assert what the constraint still refuses); dismiss is an idempotent guarded UPDATE writing only `status`+`closed_at` (never the ledger, ADR-0022), 404-over-403, no new conflict code. `GET /sync-conflicts` derives `blockingBookings` at read time with the SAME `daterange &&` over the SAME `OCCUPYING_STATUSES` the constraint uses, so the inbox names exactly what is in the way and `/app/bookings/:id` is one click away. `openConflicts` fills the §7.2 field #55 shipped without a source (badge on the channels panel); `/app/inbox` became the operations inbox, conflicts above the paid-but-lapsed payments. 18 API tests on real Postgres + 4 web tests; that closes M4. **Boss fight #5 residual closed** (#75/#142, ADR-0026, `/code-review` independent-reviewed - Spec PASS / Standards sound + one comment fix): the `TenantDbService.run` liveness guard moved off the transaction HANDLE onto the SESSION's `prepareQuery` - the point every query the codebase issues runs - and the `guardTx` handle Proxy (+ its `Reflect.get`-around-private-fields dance) was DELETED, so a query BUILT inside `run` and AWAITED after it returns (drizzle's lazy-thenable builders) now THROWS instead of executing on a recycled pooled connection under another tenant's GUC (#73's escape, narrower door). One guard at the true chokepoint subsumes both #73 and #75; `alive` flips synchronously on callback return (deferred work rejected even mid-commit-round-trip), and drizzle's own COMMIT/ROLLBACK route through the same guarded session afterwards and are allow-listed by exact SQL text (`isTransactionControl` - transaction control, never tenant data, so passing them post-settle leaks no row). Reproduced RED-FIRST (old code: the deferred builder returned rows `[]` instead of throwing). The review flagged one zero-reachability residual - drizzle's explicit `builder.prepare(name)` prepares at BUILD time and its later `.execute()`/`.all()` bypass the guard - left OPEN and documented honestly (nothing calls `.prepare()` in-tree; closing it = the invasive/brittle multi-method wrap #75 itself declined for the recursive builder proxy), so the guarantee is stated precisely as "every query this codebase issues", not "every statement". Scope = `TenantDbService` only; the owner connection (`DbService`) is out of scope (RLS-bypassed, no per-tenant GUC to leak across). **#38's one open residual closed** (#145, ADR-0028, migration 0013): imported `DATE-TIME` VEVENTs are now resolved to **property-local** dates, meeting the AC #38 knowingly shipped unmet. `property.time_zone` (NOT NULL default `Asia/Makassar`, `CHECK`-constrained to the three Indonesian zones and mirrored by `propertyTimeZoneSchema`) is the missing input; `parseCalendar(body, timeZone)` takes it as a **required** argument. Investigation shrank the bug to a quarter its stated size: `VALUE=DATE`, **floating**, and `TZID` values are already property-local (params are stripped before the date is read), so only the **UTC-stamped** form was ever wrong - `16:30Z` is still 1 Aug in Java and already 2 Aug in Bali. Closed set rather than free IANA text because Postgres cannot validate a zone in a `CHECK` (`AT TIME ZONE` is STABLE), which would have left the one correctness column with no DB backstop. A foreign `TZID` is reported (`ParseResult.foreignTimeZones` → one WARN per cycle) rather than converted or filed as a conflict, so the one remaining gap fails loudly instead of silently. Zero new dependencies (`Intl.formatToParts`). Reverses the same-day "leave it, document it" call on #145. **M5 - the demo script, README and demo-ready seed shipped** (#60, no ADR - docs plus a small seed date model): [`docs/demo.md`](docs/demo.md) is the four-act five-minute walkthrough PRD §8/G5 asks for (owner adds a property → guest books and pays in the Midtrans sandbox → the nights leave as an OTA-consumable `.ics` → the sync-conflict inbox as the closer), written against the RUNNING app rather than from memory - every URL, label and state checked, with the Snap leg's `MIDTRANS_SERVER_KEY` prerequisite and a keyless walk-in fallback stated instead of glossed. The root README became the portfolio front door: a mermaid architecture diagram, a why-this-one stack table, "the five hard parts" (one paragraph per boss fight, each linking its ADR), and a curated decisions table. The seed's dates moved from **day-of-current-month to today-relative AND packed into `[today+1, today+8)`** (`scripts/demo-dates.ts`, red-first `test/demo-dates.test.ts` over 400 consecutive "todays"): the old anchor put every seeded stay in the PAST for the back half of any month, and since the picker hides the past and the `.ics` export serves current+future stays, three of the demo's beats silently rendered nothing (measured before: `blockedRanges` empty, export feed with zero VEVENTs; after: both populated). The independent review then measured the OTHER half of the same axis - `/app/calendar` opens on the current calendar MONTH, so stays a week out fell off the demo's very FIRST screen on 22.9% of days (1,460-day sweep, visible-bar histogram `{0:335,1:96,2:96,3:240,4:192,5:501}`); tightening the offsets moved that to **3.2% zero-visible / 80.3% all-visible** `{0:47,1:48,2:48,3:48,4:96,5:1173}`, and the residue is a calendar fact (a month with no future days left cannot hold a future stay), so the seed now PRINTS a note telling the presenter to page forward. The date model owns `min_stay` and the unit prices, and the seed derives every `total_price_idr` from nights x rate rather than hardcoding it, so a stay that changes length cannot start showing a price the quote endpoint disagrees with. `packages/db`'s `typecheck` now covers `scripts/` and `test/` too (new `tsconfig.typecheck.json`; the build config still emits `src` only) - the seed had never been typechecked by anything, since tsx and vitest both strip types. **AC #4 (live OG link-preview verification via the Facebook Sharing Debugger + a real WhatsApp/LINE share) is NOT done and cannot be done from a worktree** - it needs the deployed public origin and live crawlers, so it stays an owner demo-time checklist item. **The R2 cutover is now an instrument plus a runbook, not a checklist** (#68, ADR-0029, no migration): `pnpm --filter api storage:doctor` boots no app and opens no DB connection, and probes whatever bucket `STORAGE_*` names through the SAME `StorageService` the API uses - reachability, a presigned PUT round-trip, content-type enforcement, content-length enforcement, that `STORAGE_PUBLIC_BASE_URL` serves the bytes anonymously AND byte-identically, and a real `OPTIONS` preflight from the site origin. It replaces #68's own instruction to point `jest properties-photos` at prod, which **registers tenants** (`beforeAll` → `POST /auth/register`, writing to whatever `DATABASE_URL` names) and is structurally blind to the two silent killers - it PUTs from Node, which has no same-origin policy, so it cannot fail on missing CORS, and it never reads `STORAGE_PUBLIC_BASE_URL`, so it cannot fail on photos that store fine and render broken. Content-length is reported as a **FINDING**, not a failure, because Cloudflare documents content-type enforcement and is SILENT on content-length - the one answer that can only be measured - and #68's open conditional ("must the orphan-GC backstop land first?") is resolved rather than re-decided: #69 shipped with grace-independent oversize eviction, so a non-enforcing backend degrades the 5 MB cap to "enforced within a day" over a tenant's own gallery. Two production-only `validateEnv` guards cover the mis-sets with no server-side symptom at all (`STORAGE_PUBLIC_BASE_URL` must be https + non-loopback; `STORAGE_BOOTSTRAP` must not be `true`, since R2 supports neither call it makes and would fail as a mere warning while CORS stayed unconfigured) - `STORAGE_ENDPOINT` deliberately unguarded, because the documented Garage-on-VPS fallback makes `http://localhost:3900` correct in prod. [`docs/r2-cutover.md`](docs/r2-cutover.md) carries the irreducibly-manual dashboard steps + the measured-answer table (Garage baseline: both enforced, 403). The probe was self-verified RED first - a broken public base and a forced transport failure - which surfaced two of its own defects before it was trusted green: a `fetch` throw reported as `TypeError: fetch failed` instead of the misconfiguration it actually is (now unwrapped to `ECONNREFUSED`/`ENOTFOUND`), and a `process.exit()` racing undici teardown that aborted on a libuv assertion and replaced the exit code with 127. Also swept: 7 stale `ADR-0016` pointers in `src/storage/` that should read **ADR-0017** (the #55/#69 parallel-dispatch renumber, never applied to the code comments - 0016 is the `.ics` export feed). **The four dashboard/live steps remain owner prod-ops** - they need an R2 account (card on file) and a deployed origin, so like #60's AC #4 they cannot be done from a worktree. All five boss fights are now closed, and M0-M4 are 100% closed. ****The tenant-configurable gallery cap shipped** (#67, ADR-0030, migration 0014): `tenant.gallery_cap` (default 30, CHECK 1-100) with `MAX_PHOTOS_PER_PROPERTY` renamed `PHOTO_GALLERY_CEILING = 100` - it stops being the cap and becomes the highest a cap may be. Investigation shrank the premise first: #39 called the constant an abuse guard, but it never bounded storage (property count is unbounded, so a tenant could always park a thousand galleries of thirty) - it bounds one request body and one gallery grid, while bytes are bounded by the 5 MB signed cap and #69's orphan sweeper. The load-bearing rule is that the write refuses only **growth** (`keys.length > cap && keys.length > existing.length`): because `PATCH …/photos` is a whole-set write, a plain `> cap` would have trapped an over-cap gallery, refusing every edit *including the removal that would fix it* - so a lowered cap blocks new adds and nothing else, and no cap change has ever touched a photo (test-proven: reorder and shrink both pass at 3 photos under a cap of 1). New `GET /settings` (any signed-in user - the property workbench reads the cap from it) + `PATCH /settings` (owner-only), a `/app/settings` page, and the codebase's **first role check**: a reusable `@Roles('owner')` + `RolesGuard` in `common/`, reading `TenantContext` (#76's single owner of the principal) and refusing a Visitor explicitly - built as the seam #57 extends rather than a one-off `if` that would give it a second authz path to reconcile. Staff get a read-only view and a 403 on write (not 404-over-403: there is no existence to hide from your own staff). Also fixed along the way: `pnpm test` now runs `turbo run test --concurrency=1`, because jest (api) and vitest (web) oversubscribing the same cores pushed four lazy-route first-renders past their async budget - a suite whose result depends on core count is not a suite, and serialising costs ~5s. **Request schemas now reject unknown keys** (#150, ADR-0031, no migration): the repo-wide convention question the #67/#149 review surfaced and deliberately left open. Every inbound schema in `packages/shared` - **body AND query param**, 14 construction sites - is built through one `strictObject` helper, so `PATCH /settings {galleryCapp: 60}` is a 400 naming the key instead of a **200 that changed nothing**; a partial PATCH schema cannot tell a misspelled field from an omitted one, which is what made success and typo indistinguishable. Queries were pulled into scope because `?form=` instead of `?from=` fails the same silent way (falls back to a default and looks fine). **Responses stay lenient on purpose** - a strict client breaks the moment the server adds a field a cached bundle predates - and the Midtrans webhook parser is the one documented inbound exception (it plucks a few fields from a large provider payload; strict would 400 every real webhook). The uniformity guard DISCOVERS `*RequestSchema`/`*QuerySchema` exports from the barrel, so a future schema that reaches for `z.object` fails without anyone registering it - discovery is by NAME (so the convention is load-bearing, and `*BodySchema` is excluded on purpose: here it names a RESPONSE body), and a discriminated union's private members are probed down every branch. The independent review found that guard's one real hole - it seeded only the `manual_block` branch, so reverting the walk-in member left the suite green - now closed and re-proven red. Left outside deliberately and documented: the webhook + iCal parsers. **That last gap is now closed too** (#152, amending ADR-0031): sixteen routes that read no body - the verb-subresources, `auth/refresh`/`auth/logout`, and every DELETE - carry a `@NoBody()` marker whose guard validates against a shared `noBodyRequestSchema` (`strictObject({})`), so `POST /bookings/:id/cancel {"refund":"full"}` is a 400 naming `refund` rather than a 200 that ignored it, refused before any lookup. A marker plus a guard rather than sixteen `@Body` parameters that exist only for a side effect, and deliberately NOT a global auto-detecting guard: detecting "has no `@Body`" needs Nest's route metadata, and a detection that broke could fail open, so production code stays free of framework internals and the reflection lives in a test that enumerates every mutating route and reddens when one declares neither a body schema nor the marker (proven red by deleting one). Composition verified against zod 3.25 before committing (`.partial()` preserves strict *and* the omitted-default short-circuit; `.refine()` chains and strict `discriminatedUnion` members both still reject), and the issue's stated risk - the SPA round-tripping a response object back as a PATCH body - was measured and does not exist: all three PATCH mutations build minimal, explicitly-keyed bodies. **Staff invites + property-scoped RBAC shipped** (#57, ADR-0032/0033, migration 0015, the last M5 feature): FR-AUTH-2 end to end. "Staff sees assigned properties only - in every list AND by direct id" is enforced by **RLS**, not by the ~30 authenticated routes it covers: `TenantDbService.run` now sets `app.property_scope` + `app.staff_user_id` beside `app.tenant_id`, from the same principal, and each policy ANDs one `app_property_visible()` term - so no repository, service or controller filters by property, and the specified 404 for an unassigned id falls out of the `0 rows → NotFoundException` every getter already had (proven over real HTTP: as the seeded staff account `GET /properties` returns Seminyak and not Canggu, `GET /properties/<canggu>` is a 404, `PATCH` of it a 404 that writes nothing, while the owner's identical calls see both). Two GUCs rather than one because Postgres does not guarantee `OR` short-circuits, so a single `'all'|uuid` GUC would let the planner evaluate `'all'::uuid` and raise 22P02 - #74's trap in a new costume; both axes fail **closed** (unset or pool-reset `''` → zero rows, so a scope is granted, never merely un-restricted). `user_property` gained `tenant_id` + composite FKs, closing the follow-up **#40** deferred by name - load-bearing twice over, since a flat policy on that table is what stops `property`→`user_property`→`property` recursing. `payment`/`payment_event` are deliberately untouched (their policies subquery `booking`, whose tightened policy applies inside) and that inheritance is **pinned by a test rather than a comment**, because it is a rewriter subtlety on a money table. The role axis is separate and answers differently: an unassigned property is a **404** (it isn't there), an owner-only verb a **403** naming the role - and `RolesGuard` runs before any lookup, so a staff member gets 403 for *every* id they name and it cannot be used as an existence oracle. The verb line is a principle, not a list: the owner decides the **shape** of the tenant (create/delete/archive a Property, settings, the team), staff **operate** what they are assigned; `POST /properties` settles it alone, since a staff-created property would vanish the instant it existed. Invites (ADR-0033): a 32-byte token stored as **sha256** and existing only in the email - no endpoint returns it - granting properties through a join table whose composite FK makes a cross-tenant grant unrepresentable a step earlier than `user_property`; single-use by a guarded UPDATE that runs **first** in the accept transaction (two racing accepts contend on that row - test-proven: `[200, 409]`, exactly one account), with `expires_at > now()` evaluated by the database and bcrypt kept outside the lock. Unknown token → 404, spent one → `409 invite_not_acceptable {reason}` (ADR-0015's shape), so only a real holder learns why; duplicate live invite → `409 invite_already_pending` off a partial unique index. A failed send **rolls the invite back** (503) - the token is recoverable from nowhere else, and a pending-unreachable invite would block every retry for that address. Accept runs on the owner connection (no principal to scope by; the row it creates *is* what would grant the scope), safe by the ADR-0016 property that every statement is keyed by an unguessable value. Web: `/invite/:token` (public bundle, previews the inviter and the properties, English-only and excluded from the language switcher), a Team section on `/app/settings` (roster, whole-set re-assignment, revoke, remove-with-confirm; staff get one explanatory sentence and the owner-only reads are never issued), and owner-only affordances hidden on the properties pages. Seed gained `staff@balibreeze.test` assigned to Seminyak only, so the scope is one login away in the demo. 30 API tests on real Postgres + 12 new RLS-layer tests + 2 structural-FK tests + 12 web tests. **Independently reviewed before merge (AC #5)** - two fresh reviewers, Standards and Spec, given the issue and the repo's documented standards but not the builder's reasoning. Scoping itself passed on both (the Spec reviewer independently re-verified every scoped route, the payment-policy inheritance, fail-closed GUCs and both structural FKs), and **two real defects were found and fixed red-first**: (1) the invite-time email pre-check was tenant-scoped while `app_user_email_key` is **global**, so inviting someone who already had an account under another tenant created and *emailed* a link that answered `409 email_taken` forever - now checked on the owner connection and refused before anything is created or sent (the global question leaks nothing new: `POST /auth/register` already answers it unauthenticated); (2) `staff_invite_live_email_uniq` cannot test `expires_at > now()` (an index predicate must be immutable), so a **lapsed invite blocked its own replacement** permanently and still showed as "pending" - `create` now supersedes stale rows first and "pending" means live everywhere, with `refusalReason` checking expiry *before* revocation so the stale link still reports `expired` rather than "withdrawn". Also fixed from the reviews: a stale api-spec §5.4 sentence the same commit contradicted (staff + unassigned unit is a **404**, not a 403), the settings read/write split stated explicitly in §3.6 rather than silently narrowed, `GET /auth/invites/token/:token` brought level with its sibling (bounded param + `@ThrottleSensitive` - one unauthenticated secret, two routes, one standard), one liveness predicate instead of three copies, and the conflated `GrantRow` split into `InviteRow`/`StaffRow` (it had been selecting a literal `null as expires_at` for staff and inventing `?? new Date()` back). Follow-up filed: **#154** (one person cannot hold staff seats at two Tenants - `app_user.email` is global by design; multi-tenant identity is a product change touching boss fight #5's spine). **That follow-up is now closed** (#154, ADR-0034, migration 0016): `app_user` became pure identity (email + password, no tenant) and `membership (app_user_id, tenant_id, role)` became belonging - so **Owner and Staff are what a membership IS, not kinds of person**, and one property manager can work for two villa owners on one address. The global email constraint stays (login carries no tenant); what changed is that the person no longer pays for it. `user_property` and `staff_invite.created_by` repoint their composite FKs at `membership`, which is strictly STRONGER than the `app_user (id, tenant_id)` unique they replace - "is a member of" survives a second seat where "this is the user's tenant" cannot - and ending a membership cascades its Assignments off it, which is what makes `DELETE /staff/:id` one statement that removes the SEAT rather than the human. On the RLS side only `app_user`'s policy moves (to an `EXISTS` over `membership`) and `membership`'s own must be FLAT or the two recurse - the identical lesson `user_property` learned in ADR-0032; the property axis and every other policy are untouched, both axes still fail closed, and the whole set is re-proven by the existing table-driven isolation test with `membership` added to it. Login stays ONE request - default seat (owners first, then oldest, computed in one query and stored nowhere) plus `memberships[]` - rather than a half-authenticated "now choose a tenant" state; `POST /auth/session {tenantId}` switches, and a tenant the caller holds no seat at is a **404**, never a 403 that would enumerate tenants a uuid at a time. The refresh token gained an OPTIONAL `tenantId` so a refresh returns to the right seat and pre-migration cookies keep working. Invite creation narrowed from "has an account ANYWHERE" to "is already on THIS team", and accept forks on a new preview field `mode: create|signin`: for an address that already has an account the password is **verified** against it (a wrong one is 401 and does NOT spend the invite - the token proves the mailbox, the password proves the account, and adding a seat to someone's login needs both). An account left with zero seats gets a **403** at login, reachable only after a CORRECT password so it is no oracle - `401` would send someone to reset a password that works. Web: a workspace switcher in the dashboard header, rendered only when there IS a second seat, which **resets** the query cache rather than invalidating it (an invalidated query keeps rendering the previous tenant's rows while it refetches). Seed: `staff@balibreeze.test` now holds two seats, so the switcher is one login away in the demo. `POST /tenants` (one person OWNING two tenants) is deliberately out of scope - register still 409s, since unauthenticated signup cannot know the caller holds that address. **Independently reviewed before merge** - two fresh reviewers (Standards, Spec) given the issue and the repo's standards but not the builder's reasoning - and the Spec reviewer found **two real defects, both fixed red-first**: (1) the default-seat order was **not total** (`created_at` defaults to `now()`, which is transaction-stable, so seats granted in one transaction - as the seed's own dual-seat account is - tie to the microsecond and the winner was heap order, making the demo login's landing workspace arbitrary; fixed with `asc(membership.tenantId)`, and the test now FORCES the adversarial heap order rather than hoping for it, because the first version passed against the un-fixed code); and (2) a removed person could **never be re-invited** - the account now survives removal and Sambung has no password reset, so a forgotten password meant `signin` mode forever, with `create` unreachable and the address globally taken. An **inert** account (no seats - it cannot sign in and guards nothing) is now claimable by the invite holder, exactly as if the row had not been there, and the inert/live question is re-decided **inside the accept transaction under a row lock**, so an account that goes live mid-flight rolls the transaction back rather than having its password overwritten (the invite is not spent). Also from the reviews: `roles.decorator`'s comment still pointed at the deleted `app_user.role`; one `toSession()` so `me()` and `issueFrom()` cannot describe a session two ways; `switchTenant` checks and mints against ONE read of the seats; the switcher reports a failed switch instead of silently springing back; and the deliberate `me()`-401 vs `refresh()`-falls-back difference on a stale seat is documented as a composition (401 → refresh → retry) rather than left looking like a disagreement. ADR-0032's "no refresh can mint another" is amended in place, since it was true only while removal deleted the account. 549 API + 66 db + 235 web + 116 shared tests, all green.

**#60's last AC now has an instrument and a route that needs no deploy** (ADR-0035, no migration): AC #4 asked for live crawler verification against a deployed origin, which is what deferred it twice. Two measurements changed the shape of the answer - a Cloudflare **quick tunnel** is anonymous, free and needs no DNS, so "needs a deployed origin" was never quite true; and Node's `fetch` **silently drops a `Host` header**, which would have made the forged-`Host` probe pass vacuously forever had it been written the obvious way. `deploy/Caddyfile` is committed, reviewed, load-bearing and executed by NOTHING (it says so about itself), so an opt-in `docker compose --profile edge` now runs the **real file, bind-mounted** and `pnpm --filter api og:doctor [baseUrl] [slug]` measures it: routing (crawler→stub; human, Googlebot and LINE's **in-app browser**→SPA - the #127 regression tested by behaviour for the first time rather than against the regex text; anchoring; trailing slash; unknown slug→404), the card (its tags equal `buildPropertyOgTags` over the **live** JSON, the first over-the-wire test of ADR-0019's no-drift claim; `og:image` fetchable anonymously), and the canonical (a forged `Host` must not move `og:url`). The probed origin is an **argument, never app config** - it must be pointable at an origin that disagrees with what the app claims. Severity follows meaning (ADR-0029's rule): an `og:url` mismatch is a finding on loopback and a **failure** on a public origin. The manual pass uses **two** tunnels (site + photos), which is the prod shape rather than a workaround - a single-origin tunnel tests an arrangement that never ships, and dev photos live on a `.localhost` host no crawler can resolve, which the probe names as a finding rather than letting it surface as a grey box in front of an audience. Deliberately a script, not `pnpm test`: continuous coverage would need Docker + a built SPA + container-to-host networking with no cloud CI, and a flaky suite gets skipped and then lies about coverage - so the gap is stated in the runbook and `property-og.spec.ts` stays the cheap always-on guard. Self-verified **red first** against a purpose-built broken edge (8 seeded misconfigurations, all caught, non-zero exit) before being trusted green. Baseline: **14 passed, 0 failed, 2 expected findings**. [`docs/og-verification.md`](docs/og-verification.md) carries the runbook; CONTEXT.md gained a **Delivery** section (the Edge, Crawler, OG Stub).

**M5's remaining work is not code**: #60 (only AC #4 - the three manual crawler checks, now a documented 10-minute pass with every mechanical link measurable first) and #68 (prod-ops only). #139 (test-flake hardening) and #67 closed 2026-07-21; #150, #152, #57 and #154 closed 2026-07-22. **Nothing left requires a paid account or a deploy** - the whole app, demo included, runs on the local Garage + Postgres compose stack; R2 is a prod-time optimisation with a documented free fallback (Garage on the VPS, identical code path).
- **Design system decided** (2026-07-18): "the gracious host" - stone neutrals + terracotta, Plus Jakarta Sans + Fraunces, lowercase `sambung` wordmark, two-surface component doctrine. Source of truth: [docs/design-system.md](docs/design-system.md) + ADR-0007. Foundation **shipped** in #91/#99; all M2 UI now builds on the semantic tokens (the calendar #49 is the first).
- **SEO tier 2 shipped** (2026-07-20, #87/#122, ADR-0019): link-preview crawlers (WhatsApp/`facebookexternalhit`/… - this product's actual channel) now get real per-property OG tags via a narrow Caddy UA-allowlist → a static OG stub route sharing `buildPropertyOgTags` with the SPA `<meta>` tags. Live Facebook/WhatsApp preview verification is a documented manual pass, no deploy required: [`docs/og-verification.md`](docs/og-verification.md) (ADR-0035).
- **E2E testing foundation shipped** (2026-07-23, #164): `apps/e2e` - Playwright drives the whole stack (web + api + db) through a real browser. Runs against a dedicated `sambung_e2e` DB provisioned (fail-fast: Postgres + Garage) *before* Playwright starts, so the destructive seed never touches dev data; per-role `storageState` auth (owner + staff via the real `/login`); accessibility-first locators with the funnel locale pinned to EN (i18n tested in its own spec); `retries:0`. **9 tests, 4 projects** (Chromium desktop + mobile, WebKit funnel): availability→checkout, i18n switch, a network-stubbed payment handoff (provider stubbed at `page.route` - never reaches Midtrans), owner walk-in, and staff RBAC scoping. Isolated as `pnpm test:e2e` (Turbo `cache:false`), out of `pnpm test` and pre-push. Grilled design + `/code-review`; source of truth [`apps/e2e/README.md`](apps/e2e/README.md).
- **E2E flow-coverage harness enablers shipped** (2026-07-23, #167, umbrella #166, decision-log row 2026-07-23): the serial prerequisite that unblocks the parallel flow issues #168-#175 (which stay purely additive - new spec files only). Three seams: **(a) lane isolation** - one `SAMBUNG_E2E_LANE=<n>` derives a whole stack (DB `sambung_e2e_<n>` + web/api ports offset by n) in `e2e-config.ts`; `vite.config` reads `WEB_DEV_PORT`/`WEB_API_PROXY_TARGET` (prod-inert, NOT `VITE_`-prefixed so they never leak into the client bundle; base lane == today's exact values), the web `webServer` passes them - two lanes verified **9/9 green concurrently**, no port/DB clash. **(b) the `PAYMENT_GATEWAY=fake` env seam** - a `createPaymentGateway` factory (mirroring `createMailer`) binds the signature-free `FakePaymentGateway` only on the literal `fake` (trimmed to match the guard) and is set in the e2e api `webServer.env`, so a spec can drive a booking to `confirmed` (fake webhook POST / reconcile-on-read) with no outbound Midtrans call; departs from ADR-0015's "fake out of the DI graph", made safe **structurally** by `validateEnv` refusing `fake` in production (the ADR-0029 fail-fast shape). **(c) two Baseline seed fixtures** the flows can't create at runtime - a known-token staff invite on Bali Breeze/Seminyak for the create-account accept path (sha256-stored, raw token mirrored in `e2e-config.ts`; Flow 5/#172) and a paid-but-lapsed payment (`paid` on an `expired` booking, `handled_at` NULL; the #120 inbox item, Flow 7/#174). **Independently reviewed before merge** (payment-adjacent, boss-fight #4 territory) - two fresh reviewers (Standards + Spec), the Spec reviewer caught **one real blocker fixed red-first**: the seam was built but never switched on in the api `webServer.env`, so the harness would have booted the real Midtrans gateway while the README claimed the fake - a shared-file gap that would also have forced the flow lane to edit shared config. All gates green (26 unit + 75 payment specs, typecheck, lint) and the Docker-gated DoD (two concurrent lanes, the seed fixtures hash-verified against the DB) run locally before merge.
- **E2E flow coverage complete - all 8 flows** (2026-07-24, #168-#175 under umbrella #166; PRs #178-#186 + the #189 sweep): the suite grew **9 → 41 tests** across the 4 projects, every flow a purely additive spec file. Flow 1 guest booking → pay → `confirmed` (drives the #167 fake gateway to a real `confirmed` row, asserts the `wa.me` deeplink, and renders a REAL `booking_no_overlap` 409 as the funnel's own localized copy); 2 owner onboards a property (register → priced unit → a real presigned PUT to Garage → "Ready to publish" → the public page); 3 calendar ops (walk-in / block → cancel → **re-books the same nights** to prove the dates actually freed, not just that the bar vanished); 4 reservations + CSV (reads the downloaded **body**, with the excluded row deliberately in-window so it tests the status filter, not a window artifact); 5 staff invite → accept → scoped access (the wire-level **404-not-403** for an unassigned id - RLS as a real session, by id, which only e2e can prove); 6 channel lifecycle (the no-PII `.ics` assertion made non-vacuous by first creating a confirmed booking carrying name/phone/email/price); 7 operations inbox (dismiss + handle, `describe.serial`, asserting the **ledger is untouched** per ADR-0022); 8 auth/session/i18n (guard + `?next`, silent refresh, workspace switch, locale persistence). Shipped over **4 parallel dispatch runs**, 2 lanes each, every lane **independently reviewed** by a fresh agent fed only the issue + ACs. **Two harness gaps surfaced and were fixed in #178, not worked around**: turbo's strict env-mode pruned the undeclared `SAMBUNG_E2E_*`, so the documented `SAMBUNG_E2E_LANE=n pnpm test:e2e` silently ran on the BASE lane (`passThroughEnv`); and `applyDevBucketConfig` sets the Garage bucket's CORS to `WEB_ORIGIN` only, so a non-base lane's browser upload was refused the preflight (per-lane `WEB_ORIGIN`). **The independent reviews earned their keep by mutation-testing rather than reading**: #185's ledger assertion survived three mutants (flip `payment.status`, flip the booking to `cancelled`, make `dismiss` a silent no-op), but #186's workspace-switch test **passed even when `resetQueries` was degraded to `invalidateQueries`** - the exact ADR-0034 regression that test exists to catch - because auto-retrying matchers converge on the post-refetch state either way; fixed with a held refetch + ONE non-retrying `evaluate` sample, proven red-then-green. Conventions updated where the flows bent them: the README now records the **one** documented exception to "never mutate Baseline rows" (Flow 7 clears the two inbox fixtures, Flow 5 spends the invite - three seeded rows total, read by no other flow), and #189 swept the spec inventory + widened the `funnel-mobile-safari` `testMatch`, which was followed by `\.spec\.ts` and so silently excluded `i18n-persistence.spec.ts` - the one spec asserting localStorage, on the engine where localStorage diverges. Follow-up **#182 was filed and fixed the same day** (#192, decision-log row above): the shared dev Garage bucket's CORS was single-origin and last-writer-wins at bootstrap, so two concurrent lanes both uploading 403'd each other's preflight - reproduced end-to-end before it was touched, since it had been filed from a static reading. Its review then found the defect that mattered more: `storage:doctor`'s CORS probe read only the `access-control-allow-origin` header, and Garage attaches `*` to its **403 refusals** - so a healthy bucket, a wrong-origin bucket and a bucket with **no CORS policy at all** all printed one `[PASS]` line. That is ADR-0029's own instrument blind in its own category, and it is why #182 survived unnoticed; the probe now requires a 2xx as well. **Of the two follow-ups it left, #193 is now closed** (2026-07-24, decision-log row: the guards no longer key on `NODE_ENV` at all - a deployment is recognised by `WEB_BASE_URL`/`STORAGE_PUBLIC_BASE_URL` naming a public host, which sidesteps the inverted `STORAGE_ENDPOINT` check, since Garage-on-VPS is a blessed prod shape where the endpoint IS loopback and the call SUCCEEDS; it also fixed a second inert reader the issue missed, the refresh cookie's `Secure` flag). Still open: **#194** (the `channel-lifecycle` "Sync error" assertion spends its 20 s budget on a real `example.invalid` DNS failure, so it fails under two concurrent lanes and passes in ~2.6 s alone).
- **Repo**: `RacThug/sambung` (private).
- **Tracking**: GitHub **Issues + Milestones** (M0–M5).
- **Key documents** (read the relevant one before touching that area):
  - [`docs/prd.md`](docs/prd.md) — product source of truth (what/why, acceptance criteria).
  - [`docs/db-design.md`](docs/db-design.md) — schema, constraints, integrity rules (teaching edition).
  - [`docs/architecture.md`](docs/architecture.md) — FE/BE split, modules, data flows (teaching edition).
  - [`docs/README.md`](docs/README.md) — doc index.
