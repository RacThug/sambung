// Owner dashboard shell. Auth guard + real views (calendar, reservations,
// channels) arrive in M1+. (architecture.md §4.2)
export function DashboardPage() {
  return (
    <main className="mx-auto max-w-2xl p-8">
      <h1 className="text-2xl font-bold">Dashboard</h1>
      <p className="mt-2 text-gray-600">Owner dashboard — coming in M1.</p>
    </main>
  );
}
